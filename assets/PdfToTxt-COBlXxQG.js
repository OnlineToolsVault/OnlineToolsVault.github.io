import { r as s, j as e, d as m } from "./index-BtmU1OS0.js";
import { R as k } from "./RelatedTools-GVPazTWJ.js";
import { T } from "./ToolLayout-CgcEif7J.js";
import { u as P } from "./index-BhP_zCBa.js";
import { _ as D, p as F, a as B } from "./pdf.worker.min-C2jAzU1L.js";
import { F as E } from "./FileSaver.min-2_N9Q3K6.js";
import { F as i } from "./tools-DOXC7sEs.js";
import { D as p } from "./download-Cb6qc09_.js";
import "./shield-C_IpXjfc.js";
B.workerSrc = F;
const N = [{ title: "The text layer, verbatim", desc: "Every text-showing operation on every page is read out of the content stream and written to a UTF-8 .txt file. No re-typing, no recognition, no interpretation \u2014 the characters the document declares are the characters you get.", icon: e.jsx(i, { color: "var(--primary)", size: 24 }) }, { title: "Page markers you can split on", desc: "Each page is introduced by a line reading --- Page 3 --- with blank lines around it, so a script can chunk the output per page and a human can find where something came from.", icon: e.jsx(m, { color: "var(--primary)", size: 24 }) }, { title: "Nothing leaves the tab", desc: "The PDF is parsed by JavaScript on your own machine and the .txt is created as a local Blob. For contracts, medical notes or anything under an NDA, that removes the upload from the equation entirely.", icon: e.jsx(p, { color: "var(--primary)", size: 24 }) }], R = [{ question: "What does the output file look like?", answer: "Plain UTF-8 text, one section per page. Each section begins with --- Page N --- followed by a blank line, then the text of that page, then another blank line. Nothing is escaped or wrapped, so it opens cleanly in any editor and pipes straight into a script." }, { question: "Why is a whole page on one long line?", answer: "Because line breaks in a PDF are not characters. The file positions each fragment of text at a coordinate; where a line ends is a matter of geometry, not of anything stored in the stream. This tool joins the fragments in the order the page draws them, separated by spaces, which gives you the words reliably but not the line structure. If line breaks matter to you, **PDF to Word** reconstructs them by comparing vertical positions." }, { question: "I got an empty file, or almost nothing.", answer: "The document has no text layer \u2014 it is a scan, a photograph of a page, or an export that converted type to outlines. There is nothing to read out, and no amount of retrying will change that. Run the pages through recognition instead: convert with **PDF to PNG** at 3x and feed the images to **Image to Text**, which performs OCR in the browser." }, { question: "The words came out jumbled or interleaved.", answer: "Extraction follows the order in which the page draws its text, which for a straightforward document is reading order and for a complicated one is whatever the generating software decided. Two-column layouts, sidebars, headers repeated mid-page and tables are the usual culprits \u2014 you may get the left column and the right column woven together. Nothing can fix that from the text layer alone; for tabular data, **PDF to Excel** groups by vertical position instead, which handles rows far better." }, { question: "Are hyphens at line ends joined back up?", answer: "No. A word split across two lines as inter- and pretation stays split, with the hyphen intact, because the file genuinely contains two fragments. Likewise ligatures may come through as the single characters the font uses, and some fonts with unusual encodings produce the wrong characters entirely. A quick find-and-replace pass in your editor deals with the common cases." }, { question: "Does it extract images, tables or formatting?", answer: "None of them \u2014 this is a text-only extractor by design. Bold, italic, font sizes, colours and positions are all discarded. For the pictures inside the document use **Extract Images from PDF**; for tabular data use **PDF to Excel**; for something you can edit with its line structure intact use **PDF to Word**." }, { question: "How large a document can it handle?", answer: "Very large ones, comfortably. Text extraction is far cheaper than rendering because no page is ever drawn \u2014 only the text operators are parsed \u2014 so a thousand-page report processes in seconds and uses a fraction of the memory an image conversion would. A progress percentage tracks the pass so you can see it working." }, { question: "Can it read a password-protected PDF?", answer: "No. Encrypted documents cannot be parsed, so extraction fails. Remove the protection with **Unlock PDF** first, using the password, and then extract from the unlocked copy." }], G = () => {
  const [r, l] = s.useState(null), [n, c] = s.useState(false), [g, d] = s.useState(0), u = async () => {
    if (r) {
      c(true), d(0);
      try {
        const t = await r.arrayBuffer(), o = await D(t).promise;
        let h = "";
        for (let a = 1; a <= o.numPages; a++) {
          const v = (await (await o.getPage(a)).getTextContent()).items.map((j) => j.str).join(" ");
          h += `--- Page ${a} ---

${v}

`, d(Math.round(a / o.numPages * 100));
        }
        const b = new Blob([h], { type: "text/plain;charset=utf-8" });
        E.saveAs(b, `${r.name.replace(/\.pdf$/i, "")}.txt`);
      } catch (t) {
        console.error(t), alert("Failed to extract text.");
      } finally {
        c(false);
      }
    }
  }, x = (t) => {
    (t == null ? void 0 : t.length) > 0 && l(t[0]);
  }, { getRootProps: f, getInputProps: y, isDragActive: w } = P({ onDrop: x, accept: { "application/pdf": [".pdf"] }, multiple: false });
  return e.jsx(T, { title: "PDF to Text", description: "Extract readable text from PDF documents.", seoTitle: "PDF to TXT Converter - Extract Text Online", seoDescription: "Convert PDF to plain text (TXT) format. Extract all text from PDF files for editing or analysis.", faqs: R, children: e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [e.jsx("div", { style: { background: "white", border: "1px solid var(--border)", borderRadius: "1rem", padding: "2rem" }, children: r ? e.jsxs("div", { style: { maxWidth: "600px", margin: "0 auto", padding: "3rem", background: "#f8fafc", borderRadius: "1rem", border: "1px solid var(--border)", textAlign: "center" }, children: [e.jsxs("div", { style: { marginBottom: "2rem" }, children: [e.jsx("div", { style: { width: "64px", height: "64px", background: "#e0f2fe", borderRadius: "50%", display: "flex", alignItems: "center", justifyContent: "center", margin: "0 auto 1rem", color: "#0284c7" }, children: e.jsx(i, { size: 48 }) }), e.jsx("p", { style: { marginTop: "1rem", fontWeight: "bold" }, children: r.name })] }), e.jsxs("button", { id: "pdf-txt-download-btn", onClick: u, disabled: n, className: "tool-btn-primary", style: { padding: "1rem 2rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", cursor: n ? "wait" : "pointer", fontWeight: "bold", display: "inline-flex", alignItems: "center", gap: "0.5rem" }, children: [n ? e.jsx(m, { className: "spin", size: 20, style: { animation: "spin 1s linear infinite" } }) : e.jsx(p, { size: 20 }), n ? `Extracting... ${g}%` : "Extract Text & Download"] }), e.jsx("style", { children: "@keyframes spin { 100% { transform: rotate(360deg); } }" }), e.jsx("div", { style: { marginTop: "1.5rem" }, children: e.jsx("button", { id: "pdf-txt-reset-btn", onClick: () => l(null), style: { background: "none", border: "none", color: "#64748b", textDecoration: "underline", cursor: "pointer" }, children: "Cancel" }) })] }) : e.jsxs("div", { id: "pdf-txt-dropzone", className: "tool-upload-area", ...f(), style: { border: "2px dashed var(--border)", borderRadius: "0.75rem", padding: "3rem 2rem", textAlign: "center", cursor: "pointer", background: w ? "var(--secondary)" : "#f8fafc", transition: "all 0.2s ease" }, children: [e.jsx("input", { ...y(), "aria-label": "Choose a file for PDF to Text" }), e.jsx("div", { style: { width: "64px", height: "64px", background: "#e0f2fe", borderRadius: "50%", display: "flex", alignItems: "center", justifyContent: "center", margin: "0 auto 1rem", color: "#0284c7" }, children: e.jsx(i, { size: 32 }) }), e.jsx("h3", { style: { fontSize: "1.25rem", fontWeight: "600", marginBottom: "0.5rem" }, children: "Drag & drop PDF here" }), e.jsx("p", { style: { color: "#64748b" }, children: "or click to select file" })] }) }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(k, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About PDF to Text" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["This reads the text layer out of a PDF and writes it to a UTF-8 .txt file named after the source document, with each page introduced by a ", e.jsx("strong", { children: "--- Page N ---" }), " marker. No styling, no images, no layout \u2014 just the characters. Everything is parsed in this browser tab and nothing is uploaded."] }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "Where the text in a PDF actually is" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "A PDF page does not contain a paragraph. It contains a sequence of instructions along the lines of: select this font at this size, move to this coordinate, show these character codes. Those codes are mapped back to Unicode through the font's encoding, and the result is what gets written to your .txt. Two consequences follow, and understanding them explains almost every surprise people have with text extraction." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "First, there are no line breaks and no paragraphs to recover. A line ends because the next fragment was placed lower down, not because a newline was stored. Fragments here are joined with spaces, so each page arrives as one continuous run of text \u2014 reliable for the words, useless for the shape. Second, the order is the order the page draws in. Most documents draw top-to-bottom, left-to-right and extract perfectly; a two-column journal article or a page with pull quotes may draw in an order that reads as nonsense once flattened." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "Extraction is not recognition" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["This distinction decides whether the tool will work on your file at all. Extraction reads text the document already declares and is exact and instant. Recognition looks at a picture of a page and guesses at the letters, which is slow and fallible. A scanned document contains images, not text \u2014 the characters exist only as ink in a bitmap \u2014 so extraction returns nothing at all. That empty output is not a failure of the tool; it is a correct report that there is no text layer. Convert the pages with ", e.jsx("strong", { children: "PDF to PNG" }), " and run them through ", e.jsx("strong", { children: "Image to Text" }), " if you need recognition."] }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "Rough edges you should expect" }), e.jsxs("ul", { style: { lineHeight: "1.7", color: "var(--text-secondary)", marginBottom: "1rem", paddingLeft: "1.25rem" }, children: [e.jsxs("li", { children: [e.jsx("strong", { children: "Hyphenation survives." }), " Words broken across lines keep their hyphen, because the file really does contain two pieces."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Tables collapse." }), " Cells become a stream of words with no delimiters. Use ", e.jsx("strong", { children: "PDF to Excel" }), " for anything grid-shaped."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Running heads and page numbers appear" }), " in the middle of the flow, once per page, exactly where the page drew them."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Odd characters" }), " can appear where a font uses a non-standard encoding or a subset without a proper Unicode mapping \u2014 most often with mathematical symbols and older typesetting output."] })] }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "Good uses for a plain text dump" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: ["Searching a stack of reports with grep, counting words, feeding a document to a language model, checking whether a PDF has a text layer at all, pulling quotes for citation, or diffing two revisions of a contract with ", e.jsx("strong", { children: "Diff Viewer" }), " once you have both as text. Because no page is ever rendered, this is by far the cheapest operation of the PDF converters \u2014 a thousand pages costs seconds and very little memory, where an image conversion of the same document would run into gigabytes. When you need structure rather than raw words, ", e.jsx("strong", { children: "PDF to Word" }), " reconstructs line breaks from vertical positions and ", e.jsx("strong", { children: "PDF to Excel" }), " reconstructs rows."] })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: N.map((t, o) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: t.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: t.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: t.desc })] }, o)) })] })] }) });
};
export {
  G as default
};
