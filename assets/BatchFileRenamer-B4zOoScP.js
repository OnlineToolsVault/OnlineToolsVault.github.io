import { c as I, r as l, j as e, X as S, L as R } from "./index-OUpguYFg.js";
import { R as z } from "./RelatedTools-dQ1AUZ0r.js";
import { T } from "./ToolLayout-CuKFTkh4.js";
import { F as w } from "./FileUploader-Dhw1e8vw.js";
import { J as P } from "./jszip.min-BycgvNgQ.js";
import { F as B } from "./FileSaver.min-DzHDzKVl.js";
import { A as W } from "./arrow-right-BvMEROXY.js";
import { D as C } from "./download-DqlBxbZM.js";
import { F as N } from "./files-CmqaNgok.js";
import { W as Z } from "./tools-B3OPepIK.js";
import { L as A } from "./list-ordered-CNk9zDfy.js";
import "./index-CBYUSgtG.js";
import "./_commonjs-dynamic-modules-TDtrdbi3.js";
import "./shield-BrCBnKXk.js";
/**
* @license lucide-react v0.344.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
const _ = I("FilePenLine", [["path", { d: "m18 5-3-3H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2", key: "h0fsxq" }], ["path", { d: "M8 18h1", key: "13wk12" }], ["path", { d: "M18.4 9.6a2 2 0 1 1 3 3L17 17l-4 1 1-4Z", key: "dyo8mm" }]]), q = [{ title: "Live preview of every name", desc: "The old name and the new one sit side by side and update on each keystroke, so you see the result of a rule on all your files before committing to it rather than after." }, { title: "Four rules, applied in order", desc: "Find and replace runs first, then a prefix, then a suffix inserted before the extension, then an optional counter at the very front. Predictable ordering means you can reason about the outcome." }, { title: "Collisions handled, originals untouched", desc: "Two files that end up with the same name get numbered rather than silently overwriting each other, and the results arrive as a new ZIP \u2014 the files on your disk are never modified." }], E = [{ question: "In what order are the rules applied?", answer: 'Find and replace first, then the prefix is added to the front, then the suffix is inserted just before the final dot, and finally the counter is placed at the very start. Working through an example makes it concrete: with find "e", replace "E", prefix "trip-", suffix "_v2" and the counter enabled, holiday.jpeg becomes 1_trip-holiday_v2.jpEg. Note where the counter landed \u2014 in front of the prefix, not after it.' }, { question: "Why did my extension change?", answer: 'Find and replace operates on the whole file name, extension included, because that is often what you want \u2014 stripping "_final" from every name, for instance. But searching for "e" also hits the "e" in .jpeg, as in the example above. If a rule is touching your extensions, make the search text longer and more specific so it cannot match inside the last few characters.' }, { question: "How does the numbering work?", answer: "It prefixes each file with its position in the list followed by an underscore, starting at 1, with no zero padding \u2014 so a set of twelve files runs 1_ through 12_. Because there is no padding, they will sort as 1, 10, 11, 12, 2 in most file managers. The order comes from the order the files were added; there is no drag-to-reorder, so add them in the sequence you want or remove and re-add the ones that are out of place." }, { question: "What if two files end up with the same name?", answer: "The second one gets a numbered suffix before its extension \u2014 report.pdf and report (1).pdf \u2014 so nothing is silently lost inside the archive. This matters more than it sounds, because a ZIP keyed by name would otherwise keep only the last file written under each name. It also comes up whenever you add files that came from different folders, since only the file name is used and any folder structure is flattened." }, { question: "Does this rename the files on my computer?", answer: "No. Your originals are never touched. The renamed copies are packed into a ZIP called renamed_files.zip which you then download and extract wherever you like. There is no undo to worry about, because there is nothing to undo \u2014 if a rule was wrong, adjust it and download again." }, { question: "Why is the ZIP the same size as my files?", answer: "Because the archive is written without compression. Renaming should not spend time re-compressing data that is usually already compressed \u2014 photos, video and PDFs barely shrink anyway \u2014 so entries are stored as-is and the download is roughly the sum of your files plus a small amount of overhead. Extract it as normal; a stored ZIP opens exactly like a compressed one." }, { question: "Can I use regular expressions or change capitalisation?", answer: "No. Find and replace is literal text, matching every occurrence, and there are no case conversion, date insertion or padding options. For anything conditional or pattern-based, a desktop utility or a shell loop will serve you better. What this handles well is the common case: strip a string, add a prefix, number a sequence." }, { question: "How many files can I do at once?", answer: "There is no fixed cap, but everything is held in memory: your files plus the ZIP being assembled, so peak usage is roughly twice the total size. A few hundred documents or photos is comfortable on a normal machine. Large video files are the practical limit \u2014 a handful of gigabytes will make the tab struggle long before the file count does." }, { question: "Are the files uploaded anywhere?", answer: "No. They are read into the tab, renamed, and packed into a ZIP by JavaScript running in your browser. Nothing is transmitted and nothing is stored, so batches containing client work or personal documents stay on your machine." }], H = (o, d) => {
  if (!d.has(o)) return d.add(o), o;
  const n = o.lastIndexOf("."), p = n > 0 ? o.slice(0, n) : o, a = n > 0 ? o.slice(n) : "";
  let c = 1;
  for (; d.has(`${p} (${c})${a}`); ) c += 1;
  const h = `${p} (${c})${a}`;
  return d.add(h), h;
}, te = () => {
  const [o, d] = l.useState([]), [n, p] = l.useState(""), [a, c] = l.useState(""), [h, v] = l.useState(""), [f, j] = l.useState(""), [y, k] = l.useState(false), [u, x] = l.useState(false), b = (t) => {
    const i = Array.isArray(t) ? t : [t];
    d((s) => [...s, ...i.map((r) => ({ file: r }))]);
  }, g = l.useMemo(() => {
    const t = /* @__PURE__ */ new Set();
    return o.map((i, s) => {
      let r = i.file.name;
      if (h && (r = r.split(h).join(f)), n && (r = n + r), a) {
        const m = r.lastIndexOf(".");
        m !== -1 ? r = r.substring(0, m) + a + r.substring(m) : r = r + a;
      }
      return y && (r = `${s + 1}_${r}`), { ...i, newName: H(r.trim() || "unnamed", t) };
    });
  }, [o, n, a, h, f, y]), F = async () => {
    if (g.length !== 0) {
      x(true);
      try {
        const t = new P();
        g.forEach((s) => {
          t.file(s.newName, s.file);
        });
        const i = await t.generateAsync({ type: "blob" });
        B.saveAs(i, "renamed_files.zip");
      } catch (t) {
        alert(`Error creating ZIP: ${(t == null ? void 0 : t.message) || "unknown error"}`);
      } finally {
        x(false);
      }
    }
  };
  return e.jsxs(T, { title: "Batch File Renamer", description: "Rename multiple files at once and download as ZIP.", seoTitle: "Batch File Renamer - Bulk Rename Utility", seoDescription: "Rename multiple files online. Add prefix, suffix, and counter. Batch rename images, documents, and lists.", faqs: E, children: [e.jsx("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto", padding: "2rem", background: "white", borderRadius: "1rem", border: "1px solid var(--border)" }, children: e.jsxs("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(300px, 1fr))", gap: "2rem" }, children: [e.jsxs("div", { style: { display: "flex", flexDirection: "column", gap: "1rem" }, children: [e.jsx("h3", { children: "Rename Rules" }), e.jsxs("div", { children: [e.jsx("label", { htmlFor: "prepend", style: { display: "block", marginBottom: "0.25rem", fontWeight: "bold", fontSize: "0.9rem" }, children: "Prepend Text" }), e.jsx("input", { id: "prepend", type: "text", value: n, onChange: (t) => p(t.target.value), style: { width: "100%", padding: "0.5rem", borderRadius: "0.5rem", border: "1px solid var(--border)" } })] }), e.jsxs("div", { children: [e.jsx("label", { htmlFor: "append", style: { display: "block", marginBottom: "0.25rem", fontWeight: "bold", fontSize: "0.9rem" }, children: "Append Text" }), e.jsx("input", { id: "append", type: "text", value: a, onChange: (t) => c(t.target.value), style: { width: "100%", padding: "0.5rem", borderRadius: "0.5rem", border: "1px solid var(--border)" } })] }), e.jsxs("div", { children: [e.jsx("label", { htmlFor: "find", style: { display: "block", marginBottom: "0.25rem", fontWeight: "bold", fontSize: "0.9rem" }, children: "Find" }), e.jsx("input", { id: "find", type: "text", value: h, onChange: (t) => v(t.target.value), style: { width: "100%", padding: "0.5rem", borderRadius: "0.5rem", border: "1px solid var(--border)" } })] }), e.jsxs("div", { children: [e.jsx("label", { htmlFor: "replace", style: { display: "block", marginBottom: "0.25rem", fontWeight: "bold", fontSize: "0.9rem" }, children: "Replace With" }), e.jsx("input", { id: "replace", type: "text", value: f, onChange: (t) => j(t.target.value), style: { width: "100%", padding: "0.5rem", borderRadius: "0.5rem", border: "1px solid var(--border)" } })] }), e.jsxs("div", { style: { display: "flex", alignItems: "center", gap: "0.5rem" }, children: [e.jsx("input", { type: "checkbox", checked: y, onChange: (t) => k(t.target.checked), id: "counter" }), e.jsx("label", { htmlFor: "counter", style: { cursor: "pointer" }, children: "Add Number Counter (1_, 2_...)" })] })] }), e.jsxs("div", { style: { display: "flex", flexDirection: "column", minHeight: "400px", maxHeight: "600px" }, children: [e.jsx("div", { style: { flex: 1, overflow: "auto", border: "1px solid var(--border)", borderRadius: "0.5rem", marginBottom: "1rem" }, children: o.length === 0 ? e.jsx("div", { style: { height: "100%", display: "flex", alignItems: "center", justifyContent: "center", padding: "2rem" }, children: e.jsx(w, { id: "batch-files-upload", onFileSelect: b, icon: _, label: "Add files", multiple: true, style: { border: "none" } }) }) : e.jsxs("div", { style: { padding: "1rem" }, children: [g.map((t, i) => e.jsxs("div", { style: { display: "flex", alignItems: "center", gap: "1rem", padding: "0.5rem", borderBottom: "1px solid #eee" }, children: [e.jsx("div", { style: { flex: 1, color: "#64748b", fontSize: "0.9rem" }, children: t.file.name }), e.jsx(W, { size: 16, color: "#cbd5e1" }), e.jsx("div", { style: { flex: 1, fontWeight: "bold", color: "var(--primary)" }, children: t.newName }), e.jsx("button", { onClick: () => d((s) => s.filter((r, m) => m !== i)), style: { border: "none", background: "none", color: "#ef4444", cursor: "pointer" }, children: e.jsx(S, { size: 16 }) })] }, i)), e.jsx("div", { style: { marginTop: "1rem", textAlign: "center" }, children: e.jsx(w, { onFileSelect: b, label: "Add more files", multiple: true, minimal: true }) })] }) }), o.length > 0 && e.jsxs("button", { onClick: F, disabled: u, className: "btn-primary", style: { width: "100%", padding: "1rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", cursor: u ? "wait" : "pointer", fontWeight: "bold", display: "flex", justifyContent: "center", alignItems: "center", gap: "0.5rem" }, children: [u ? e.jsx(R, { className: "spin", size: 20 }) : e.jsx(C, { size: 20 }), u ? "Processing..." : "Download Renamed Files (ZIP)"] })] })] }) }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem", maxWidth: "1000px", margin: "4rem auto 0" }, children: [e.jsx(z, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About Batch File Renamer" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Add a batch of files, describe the change once, and watch every new name appear next to its original as you type. When the preview looks right, the renamed copies come back as a single ZIP. Your own files are never modified, so there is no risk in experimenting with a rule to see what it does." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "The four rules and the order they run in" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Find and replace goes first, swapping every occurrence of a literal string anywhere in the name. The prefix is then added to the front. The suffix is inserted immediately before the final dot, so it lands at the end of the name rather than after the extension \u2014 and if a file has no dot at all, it simply goes on the end. The counter is applied last and sits at the very front, ahead of the prefix. Chaining all four turns holiday.jpeg into 1_trip-holiday_v2.jpEg, which also shows the one behaviour that surprises people: the search ran across the whole name, so the letter e inside .jpeg was replaced too." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Preview first, then download" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Every new name in the list is recomputed from your rules on each keystroke rather than stored, so what you see is exactly what the archive will contain \u2014 the preview cannot drift out of step with the result. Files can be added in several batches, and the small cross beside any row drops that file from the set. The numbering follows list order, so removing a row renumbers everything after it." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "How the ZIP is built" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Entries are stored without compression. Renaming should not cost the time it takes to re-compress data that is usually compressed already, so the download is about the size of your files added together and opens like any other ZIP. The archive is flat: only the file name is used, never the folder it came from. When that produces two identical names, the second gains a numbered suffix before its extension rather than overwriting the first, because a ZIP indexed by name would otherwise keep only the last one." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "What it will not do" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: "There are no regular expressions, no case conversion, no zero-padded counters and no date or EXIF tokens, and the rules apply uniformly to every file rather than conditionally. If your renaming scheme needs pattern matching, reach for a desktop utility or a shell loop. Everything also runs in memory \u2014 your files plus the archive being assembled \u2014 so a few hundred documents or photos is comfortable while several gigabytes of video is not. Nothing is uploaded at any point; the renaming and the ZIP are both produced by JavaScript in this tab." })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem", marginBottom: "4rem" }, children: q.map((t, i) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: i === 0 ? e.jsx(N, { color: "var(--primary)", size: 24 }) : i === 1 ? e.jsx(Z, { color: "var(--primary)", size: 24 }) : e.jsx(A, { color: "var(--primary)", size: 24 }) }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: t.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: t.desc })] }, i)) })] })] });
};
export {
  te as default
};
