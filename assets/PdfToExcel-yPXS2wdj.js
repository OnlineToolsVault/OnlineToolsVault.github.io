import { r as i, j as e, L as C } from "./index-OUpguYFg.js";
import { R as N } from "./RelatedTools-dQ1AUZ0r.js";
import { T as W } from "./ToolLayout-CuKFTkh4.js";
import { F as _ } from "./FileUploader-Dhw1e8vw.js";
import { _ as z, p as A, a as q } from "./pdf.worker.min-C2VdGDxB.js";
import { u, w as R } from "./xlsx-C4NO0l3T.js";
import { a as k, T as I, c as L } from "./tools-B3OPepIK.js";
import { D as H } from "./download-DqlBxbZM.js";
import { S as O } from "./shield-check-DCjAjSWE.js";
import "./index-CBYUSgtG.js";
import "./shield-BrCBnKXk.js";
q.workerSrc = A;
const U = [{ title: "Rows recovered from baselines", desc: "Text fragments sharing a rounded vertical position are treated as one row and sorted left to right. That is how a printed table gets its structure back: the rows were never stored, only the coordinates that imply them.", icon: e.jsx(I, { color: "var(--primary)", size: 24 }) }, { title: "A real .xlsx workbook", desc: "Output is a genuine Office Open XML spreadsheet on a single sheet named Sheet1, opening directly in Excel, Google Sheets, LibreOffice Calc and Numbers with no import dialogue.", icon: e.jsx(L, { color: "var(--primary)", size: 24 }) }, { title: "Financials stay on your machine", desc: "Bank statements, invoices and management accounts are parsed and written entirely in this browser tab. Nothing is transmitted, so there is no third-party copy of your numbers anywhere.", icon: e.jsx(O, { color: "var(--primary)", size: 24 }) }], G = [{ question: "How does it decide what a row is?", answer: "By vertical position. Every text fragment on a page reports the coordinate it was drawn at; fragments whose vertical coordinate rounds to the same whole number are collected into one row, the rows are ordered from the top of the page down, and the fragments within each row are ordered left to right. That is the whole algorithm, and knowing it tells you exactly when it will work." }, { question: "Why did one row split into two?", answer: "Because the baselines were not identical. Grouping is on the rounded coordinate, so two cells drawn a single unit apart land in separate rows. This shows up with superscripts and footnote markers, with mixed font sizes in the same row, and with slightly skewed output from some generators. The fix in the spreadsheet is quick \u2014 sort or merge the stray rows \u2014 but it is worth checking the top of a long extract before trusting the rest." }, { question: "My columns do not line up.", answer: "Each fragment becomes the next cell in its row, so alignment depends on every row containing the same number of fragments. A blank cell in the original produces no fragment at all, and everything after it shifts one column left. Empty-looking columns and rows with a stray extra value are the same symptom. For a table with gaps, expect to spend a minute repairing the grid \u2014 the data will all be there, in order, just not always in the column you expected." }, { question: "Where do multiple pages go?", answer: "Into one sheet, stacked in page order. There is no per-page tab and no attempt to detect a repeated header row, so a fifty-page statement produces one long sheet with the column headings recurring wherever they were printed. That is usually what you want for filtering and pivoting; a quick filter removes the repeats." }, { question: "Can it read a scanned statement?", answer: "No. Extraction reads the text layer the PDF already declares, and a scan has none \u2014 you would get an empty workbook. There is no character recognition here. For scanned tables, render the pages with **PDF to PNG** at 3x and run them through **Image to Text**, then paste and split the recognised text; accuracy on columns of figures is mixed, so check the totals." }, { question: "Are numbers imported as numbers?", answer: "They arrive as the text the page contained, so currency symbols, thousands separators and trailing minus signs come through as written and Excel may treat the cell as text. Use Text to Columns or a VALUE formula to convert once, on the whole column. Formatting, colours, borders, merged cells and formulas from the original are not preserved \u2014 this extracts data, not appearance." }, { question: "What if the document is mostly prose with one table in it?", answer: "Every line of text on every page becomes a row, so you will get the surrounding paragraphs as long single-cell rows around the table. That is easy to delete but tedious on a long document. Extract just the pages you need with **Split PDF** first, then convert \u2014 it is faster and the output is far cleaner." }, { question: "Is there a better tool for this document?", answer: "If the content is prose rather than data, **PDF to Word** rebuilds line breaks into an editable document. If you only need the raw words, **PDF to Text** is quicker. If the PDF is password-protected, nothing here can parse it until you run **Unlock PDF**. And if you finish in Excel and need to hand the result on as a CSV, **Excel to CSV** does the last step." }], oe = () => {
  const [m, p] = i.useState(null), [j, g] = i.useState(false), [F, l] = i.useState(0), [w, d] = i.useState(null), [x, c] = i.useState(null), T = async (o) => {
    p(o), g(true), l(0), c(null), d(null);
    try {
      const n = await o.arrayBuffer(), f = await z(n).promise, y = f.numPages, b = [];
      for (let h = 1; h <= y; h++) {
        const S = await (await f.getPage(h)).getTextContent(), s = {};
        for (const t of S.items) {
          const r = Math.round(t.transform[5]);
          s[r] || (s[r] = []), s[r].push({ str: t.str, x: t.transform[4] });
        }
        const E = Object.keys(s).map(Number).sort((t, r) => r - t);
        for (const t of E) {
          s[t].sort((a, B) => a.x - B.x);
          const r = s[t].map((a) => a.str).filter((a) => a.trim() !== "");
          r.length > 0 && b.push(r);
        }
        l(Math.round(h / y * 100));
      }
      const v = u.book_new(), D = u.aoa_to_sheet(b);
      u.book_append_sheet(v, D, "Sheet1"), d(v);
    } catch (n) {
      console.error(n), c((n == null ? void 0 : n.name) === "PasswordException" ? "This PDF is password protected. Unlock it first, then try again." : "Failed to convert this PDF. It may be corrupted or unsupported.");
    } finally {
      g(false);
    }
  }, P = () => {
    w && R(w, `${m.name.replace(/\.pdf$/i, "")}.xlsx`);
  };
  return e.jsx(W, { title: "PDF to Excel Converter", description: "Convert PDF tables and lists into editable Excel spreadsheets.", seoTitle: "PDF to Excel Converter - Free Online Tool", seoDescription: "Extract data from PDF files to Excel (XLSX) instantly. Free, client-side, and secure converter.", faqs: G, children: e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [m ? e.jsxs("div", { style: { textAlign: "center", padding: "3rem", background: "white", borderRadius: "1rem", border: "1px solid var(--border)" }, children: [e.jsxs("div", { style: { marginBottom: "2rem" }, children: [e.jsx(k, { size: 48, color: "var(--primary)" }), e.jsx("p", { style: { marginTop: "1rem", fontWeight: "bold" }, children: m.name })] }), j ? e.jsxs(e.Fragment, { children: [e.jsx(C, { className: "spin", size: 32, style: { display: "inline-block" } }), e.jsxs("p", { children: ["Converting... ", F, "%"] }), e.jsx("style", { children: "@keyframes spin { 100% { transform: rotate(360deg); } } .spin { animation: spin 1s linear infinite; }" })] }) : x ? e.jsxs(e.Fragment, { children: [e.jsx("p", { style: { color: "#dc2626", fontWeight: "bold", marginBottom: "1.5rem" }, children: x }), e.jsx("button", { id: "pdf-excel-reset-btn", onClick: () => {
    p(null), d(null), c(null), l(0);
  }, style: { background: "none", border: "none", color: "#64748b", textDecoration: "underline", cursor: "pointer" }, children: "Try another file" })] }) : e.jsxs(e.Fragment, { children: [e.jsx("p", { style: { color: "green", fontWeight: "bold", marginBottom: "1.5rem" }, children: "Conversion Ready!" }), e.jsxs("button", { id: "pdf-excel-download-btn", onClick: P, className: "tool-btn-primary", style: { padding: "0.75rem 1.5rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", cursor: "pointer", fontWeight: "bold" }, children: [e.jsx(H, { size: 20, style: { marginRight: "0.5rem", verticalAlign: "middle" } }), " Download Excel"] }), e.jsx("div", { style: { marginTop: "2rem" }, children: e.jsx("button", { id: "pdf-excel-reset-btn", onClick: () => {
    p(null), d(null), c(null), l(0);
  }, style: { background: "none", border: "none", color: "#64748b", textDecoration: "underline", cursor: "pointer" }, children: "Convert Another" }) })] })] }) : e.jsx("div", { className: "tool-upload-area", children: e.jsx(_, { onFileSelect: T, accept: { "application/pdf": [".pdf"] }, icon: k, label: "Drag & Drop PDF here" }) }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(N, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About PDF to Excel" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "This turns the text on each page into spreadsheet rows and writes a real .xlsx workbook. It exists to save you retyping a printed table \u2014 a bank statement, an invoice line list, a price schedule \u2014 and it gets you most of the way there, provided you know how it decides what a row is. Parsing and workbook generation both happen in this browser tab; nothing is uploaded." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "A PDF table is not a table" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "When a spreadsheet is printed to PDF, the grid is destroyed. What remains is a set of text fragments at coordinates, plus possibly some lines drawn where the borders were. There is no cell, no row, no column and no relationship between the number and its heading \u2014 those exist only in your eye, which reconstructs them from alignment. Any converter has to do the same reconstruction, and the quality of the result depends entirely on how regular the original was." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "The rule used here is deliberately simple and predictable: fragments whose vertical coordinate rounds to the same integer belong to the same row; rows are emitted from the top of the page downwards; within a row, fragments are ordered by horizontal position and written into consecutive cells. All pages are appended to a single sheet. No lines are read, no column boundaries are inferred, and no cell is ever merged or split." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "What that means in practice" }), e.jsxs("ul", { style: { lineHeight: "1.7", color: "var(--text-secondary)", marginBottom: "1rem", paddingLeft: "1.25rem" }, children: [e.jsxs("li", { children: [e.jsx("strong", { children: "Works well:" }), " machine-generated tables with one line per record and every cell populated \u2014 statements, ledgers, exported reports."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Needs tidying:" }), " tables with blank cells, because a missing fragment shifts every later value one column to the left in that row."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Needs tidying:" }), " rows with superscripts, footnote markers or mixed type sizes, which sit on slightly different baselines and split into two rows."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Struggles:" }), " cells whose text wraps onto a second line, which becomes a separate row underneath."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Not attempted:" }), " repeated header detection, number parsing, merged cells, formatting, formulas."] })] }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "Getting a clean result" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Two habits make the difference. First, narrow the input: if the table occupies four pages of a sixty-page report, pull those pages out with ", e.jsx("strong", { children: "Split PDF" }), " before converting, and you will not have to delete hundreds of prose rows afterwards. Second, verify before you rely on it \u2014 check the first and last rows against the PDF, and cross-foot a column of figures against the printed total. Values arrive as text exactly as printed, symbols and separators included, so a single Text to Columns pass over each numeric column is usually all the cleanup that is needed."] }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "When to use something else" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: ["A scanned document has no text layer at all and will produce an empty workbook; there is no character recognition here, so go via ", e.jsx("strong", { children: "PDF to PNG" }), " and ", e.jsx("strong", { children: "Image to Text" }), " and expect to check the figures carefully. Prose belongs in ", e.jsx("strong", { children: "PDF to Word" }), ", which rebuilds line breaks rather than rows. Raw words for a script belong in ", e.jsx("strong", { children: "PDF to Text" }), ". An encrypted file has to go through ", e.jsx("strong", { children: "Unlock PDF" }), " first, because a parser cannot read a document it has no key for. And if the destination is a data pipeline rather than a spreadsheet, ", e.jsx("strong", { children: "Excel to CSV" }), " converts the finished workbook in one more step."] })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: U.map((o, n) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: o.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: o.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: o.desc })] }, n)) })] })] }) });
};
export {
  oe as default
};
