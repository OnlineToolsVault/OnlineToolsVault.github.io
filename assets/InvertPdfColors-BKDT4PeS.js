import { c as ce, r as n, j as e, L as $ } from "./index-OUpguYFg.js";
import { R as he } from "./RelatedTools-dQ1AUZ0r.js";
import { T as de } from "./ToolLayout-CuKFTkh4.js";
import { u as me } from "./index-CBYUSgtG.js";
import { p as ue, a as ge, _ as pe } from "./pdf.worker.min-C2VdGDxB.js";
import { P as fe } from "./PDFButton-DYmqjJK7.js";
import { F as ye } from "./FileSaver.min-DzHDzKVl.js";
import { y as K, I as we } from "./tools-B3OPepIK.js";
import { A as be } from "./alert-triangle-BqnKTzYa.js";
import { S as xe } from "./shield-BrCBnKXk.js";
import "./UPNG-CjUEgNm-.js";
/**
* @license lucide-react v0.344.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
const ve = ce("Moon", [["path", { d: "M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z", key: "a7tn18" }]]);
ge.workerSrc = ue;
const U = (a) => {
  for (let o = 0; o < a.length; o += 4) a[o] = 255 - a[o], a[o + 1] = 255 - a[o + 1], a[o + 2] = 255 - a[o + 2];
  return a;
}, X = (a) => Math.round(a * 72), W = (a) => {
  if (!Number.isFinite(a) || a <= 0) return "0 B";
  const o = ["B", "KB", "MB", "GB"], r = Math.min(o.length - 1, Math.floor(Math.log(a) / Math.log(1024)));
  return `${(a / Math.pow(1024, r)).toFixed(r === 0 ? 0 : 2)} ${o[r]}`;
}, Pe = 26e6, R = 32767, A = (a) => Number.isFinite(a) && a > 0 ? a : 1, Q = (a, o, r) => {
  const w = A(a), P = A(o), S = Math.sqrt(Pe / (w * P)), b = Math.min(R / w, R / P), F = Math.min(r, S, b);
  return { scale: F, width: Math.min(R, Math.max(1, Math.round(w * F))), height: Math.min(R, Math.max(1, Math.round(P * F))) };
}, Y = 320, je = (a, o) => Q(a, o, Math.min(1, Y / A(a), Y / A(o))), ke = [{ value: 1, label: "Screen \u2014 1x (72 DPI)" }, { value: 2, label: "Standard \u2014 2x (144 DPI)" }, { value: 3, label: "Print \u2014 3x (216 DPI)" }], De = [{ title: "A true channel-by-channel negative", desc: "Every page is rendered to a canvas and each red, green and blue value is replaced by 255 minus itself. Black text on white becomes white on black, and coloured elements become their complement \u2014 a red heading turns cyan, a blue link turns orange.", icon: e.jsx(K, { color: "var(--primary)", size: 24 }) }, { title: "Pages come back as images", desc: "The result is rebuilt from the rendered bitmaps, so the output PDF holds one picture per page at the original page dimensions. Text is no longer selectable or searchable, and the file usually gets bigger \u2014 often several times bigger on a text document.", icon: e.jsx(we, { color: "var(--primary)", size: 24 }) }, { title: "Choose the resolution and the codec", desc: "Render at 72, 144 or 216 DPI, and store the pages as lossless PNG or as JPEG with a quality slider. PNG keeps letterforms crisp and, on inverted text, is usually the smaller of the two as well; JPEG wins on scans and photographs, at the cost of faint ringing around type.", icon: e.jsx(xe, { color: "var(--primary)", size: 24 }) }], Se = [{ question: "What exactly does inverting do to the page?", answer: "Each page is drawn onto a canvas by the same renderer a browser uses to display PDFs, and then every pixel's three colour channels are replaced by 255 minus their value. It is a photographic negative, not a theme: white paper becomes black, black text becomes white, and colours are pushed to their complements, so red becomes cyan, green becomes magenta and blue becomes yellow. Mid-greys stay mid-grey. Photographs come out looking like film negatives, which is usually the ugliest part of the result." }, { question: "Will the text still be selectable in the output?", answer: "No, and this is the main cost. The output pages are pictures. Nothing is selectable, nothing is searchable, links and bookmarks are gone, form fields become flat images of themselves, and a screen reader has nothing to read. If you need the words as well as the dark background, keep the original alongside the inverted copy, or pull the text out first with **PDF to Text**. Most PDF readers also have a built-in night or invert mode that changes only what you see and leaves the file untouched \u2014 worth checking before converting anything." }, { question: "Why did my file get so much bigger?", answer: "Because a page of text stored as vector instructions and a subsetted font is remarkably compact, while the same page stored as a bitmap is not. The useful way to predict the result is per page rather than as a multiple of the original. On the text documents measured here, an A4 page as PNG came out between about a third and about half a megabyte at 144 DPI, and between about a half and about nine tenths of a megabyte at 216 \u2014 the denser the type, the higher in each range you land, whatever the source file weighed. A slim text document can therefore come back tens or even hundreds of times larger, while a PDF that was already mostly photographs may barely change or, as JPEG, shrink. If size matters, dropping the resolution is the reliable lever, because pixels go up with the square of the scale: 1x holds exactly a quarter of the pixels of 2x, and came out at a quarter to a third of the file size on the documents measured here. Switching to JPEG is not a size lever on type \u2014 at the default quality it made every text document measured here larger than PNG, and it only crossed under PNG somewhere around quality 55 to 65, by which point the white letterforms are visibly furred. Either way the result line tells you the real figure." }, { question: "PNG or JPEG?", answer: "PNG is lossless, so it stores exactly what was rendered \u2014 the right choice for text, tables and line art, where JPEG's ringing artefacts show up as a grey halo around every letter. It is also, counter-intuitively, the smaller file for those documents once they are inverted: the flat black ground compresses to almost nothing under PNG, while JPEG pays for every glyph edge. Across three text documents measured here at the default quality, PNG was the smaller file in all nine combinations of document and resolution \u2014 though by twenty to forty per cent rather than by a multiple. JPEG earns its place on scans, photographs and heavy vector art, where it can be many times smaller: on a photographic document it beat PNG by roughly twenty-five times at 72 DPI and still by about seven times at 216, the margin narrowing as the render resolution climbs past the detail actually in the picture. So: choose by what is on the page, not by how many pages there are \u2014 type and line art means PNG, continuous-tone imagery means JPEG at 80 to 90. The margins swing a long way with the document, so if it matters, run it both ways and compare the sizes on the result line." }, { question: "Is the page size preserved?", answer: "Yes. Each output page is created at the source page's displayed dimensions in points, and the image is drawn to fill it exactly, so an A4 page stays A4 no matter which resolution you render at. Pages carrying a /Rotate flag are rendered in the orientation a reader shows and written out upright, so the rotation is baked in rather than carried over as a flag. Mixed page sizes within one document are preserved page by page." }, { question: "Does it work on scanned documents?", answer: "Yes, and scans are the case where inversion loses the least, because a scan is already a picture \u2014 there is no text layer to destroy. The output is a negative of the scan at whatever resolution you pick, which can actually help with faint pencil or carbon-copy originals where light marks on a light background become dark marks on a dark one. Note that rendering at 1x will look worse than the original scan if the original was captured at 300 DPI; use 3x to stay close." }, { question: "What happens to metadata, attachments and annotations?", answer: "The output is a brand new document assembled from images, so nothing structural survives: no title or author metadata, no outline, no attachments, no separate annotation objects, no encryption, and no information dictionary at all \u2014 not even a producer name or a creation timestamp of its own. Annotations that the renderer paints as part of the page \u2014 highlights, stamps, filled form values \u2014 appear inside the image, inverted along with everything else. If stripping that structure is the actual goal, **Remove PDF Metadata** and **Flatten PDF** do it without rasterising." }, { question: "A page came out blank, or the tab froze.", answer: "Rendering is the expensive part: an A4 page at 216 DPI is a canvas of roughly 1786 by 2526 pixels \u2014 four and a half megapixels \u2014 held at four bytes each while it is inverted and encoded. Very large pages \u2014 A0 drawings, poster-size sheets \u2014 can exceed the browser's maximum canvas at high scale, so the scale is automatically reduced for any page that would cross about 26 megapixels or 32,767 pixels on a side, rather than producing an empty image. That means the DPI you asked for is an upper bound: on a poster-size sheet, 3x and 2x can produce the same file. Whenever it happens the result line says how many pages were affected and how low the resolution actually went. If a long document still struggles, cut it up with **Split PDF** and invert the pieces. Everything runs in this tab; nothing is uploaded, and the original file on your disk is never modified." }], qe = () => {
  const [a, o] = n.useState(null), [r, w] = n.useState(null), [P, S] = n.useState(0), [b, F] = n.useState(2), [j, Z] = n.useState("png"), [E, ee] = n.useState(85), [J, G] = n.useState(null), [L, M] = n.useState(false), [l, C] = n.useState(false), [q, _] = n.useState(0), [I, k] = n.useState(""), [g, x] = n.useState(null), y = n.useRef(null), d = n.useRef(0), z = n.useRef(null), B = n.useRef(null);
  n.useEffect(() => {
    B.current = r;
  }, [r]), n.useEffect(() => () => {
    d.current += 1, B.current && B.current.destroy().catch(() => {
    }), B.current = null;
  }, []);
  const te = () => {
    d.current += 1, r && r.destroy().catch(() => {
    }), o(null), w(null), S(0), G(null), M(false), _(0), C(false), k(""), x(null);
  }, ae = async (t) => {
    const c = t == null ? void 0 : t[0];
    if (!c) return;
    d.current += 1;
    const i = d.current;
    r && r.destroy().catch(() => {
    }), k(""), x(null), G(null), M(false), o(c);
    try {
      const m = await c.arrayBuffer(), s = await pe({ data: m }).promise;
      if (d.current !== i) {
        s.destroy().catch(() => {
        });
        return;
      }
      w(s), S(s.numPages);
    } catch (m) {
      if (d.current !== i) return;
      console.error(m), w(null), S(0), k("That PDF could not be opened. Encrypted files need Unlock PDF first; otherwise the file is damaged.");
    }
  }, { getRootProps: re, getInputProps: oe, isDragActive: ne } = me({ onDrop: ae, onDropRejected: (t) => {
    var _a, _b;
    const c = (_b = (_a = t == null ? void 0 : t[0]) == null ? void 0 : _a.file) == null ? void 0 : _b.name;
    k(c ? `${c} is not a PDF. This tool only accepts PDF files.` : "That file is not a PDF. This tool only accepts PDF files.");
  }, accept: { "application/pdf": [".pdf"] }, multiple: false });
  n.useEffect(() => {
    let t = false;
    return (async () => {
      if (r) try {
        y.current && (y.current.cancel(), y.current = null);
        const i = await r.getPage(1);
        if (t) return;
        const m = i.getViewport({ scale: 1 }), s = je(m.width, m.height), D = i.getViewport({ scale: s.scale }), h = document.createElement("canvas");
        h.width = s.width, h.height = s.height;
        const p = h.getContext("2d", { willReadFrequently: true });
        p.fillStyle = "#ffffff", p.fillRect(0, 0, h.width, h.height);
        const f = i.render({ canvasContext: p, viewport: D });
        if (y.current = f, await f.promise, y.current = null, t) return;
        const v = h.toDataURL("image/png"), N = p.getImageData(0, 0, h.width, h.height);
        U(N.data), p.putImageData(N, 0, 0);
        const u = h.toDataURL("image/png");
        t || G({ before: v, after: u });
      } catch (i) {
        if ((i == null ? void 0 : i.name) === "RenderingCancelledException" || t) return;
        console.error(i), M(true);
      }
    })(), () => {
      t = true, y.current && (y.current.cancel(), y.current = null);
    };
  }, [r]);
  const se = (t) => new Promise((c, i) => {
    const m = j === "jpeg" ? "image/jpeg" : "image/png";
    t.toBlob((s) => {
      if (!s) {
        i(new Error("The browser could not encode this page."));
        return;
      }
      s.arrayBuffer().then((D) => c(new Uint8Array(D))).catch(i);
    }, m, j === "jpeg" ? Math.min(100, Math.max(30, Number(E) || 85)) / 100 : void 0);
  }), ie = async () => {
    if (!r || !a) return;
    const t = d.current;
    if (z.current === t) return;
    z.current = t;
    const c = a;
    k(""), x(null), C(true), _(0);
    let i = 0, m = b;
    try {
      const s = await fe.create({ updateMetadata: false });
      for (let h = 1; h <= r.numPages; h += 1) {
        if (d.current !== t) return;
        const p = await r.getPage(h), f = p.getViewport({ scale: 1 }), v = Q(f.width, f.height, b);
        v.scale < b && (i += 1, m = Math.min(m, v.scale));
        const N = p.getViewport({ scale: v.scale }), u = document.createElement("canvas");
        u.width = v.width, u.height = v.height;
        const T = u.getContext("2d", { willReadFrequently: true });
        T.fillStyle = "#ffffff", T.fillRect(0, 0, u.width, u.height), await p.render({ canvasContext: T, viewport: N }).promise;
        const H = T.getImageData(0, 0, u.width, u.height);
        U(H.data), T.putImageData(H, 0, 0);
        const V = await se(u), le = j === "jpeg" ? await s.embedJpg(V) : await s.embedPng(V);
        s.addPage([f.width, f.height]).drawImage(le, { x: 0, y: 0, width: f.width, height: f.height }), u.width = 0, u.height = 0, d.current === t && _(Math.round(h / r.numPages * 100));
      }
      const D = await s.save();
      if (d.current !== t) return;
      ye.saveAs(new Blob([D], { type: "application/pdf" }), `inverted-${c.name}`), x({ inputSize: c.size, outputSize: D.byteLength, pages: r.numPages, downscaled: i, lowestDpi: X(m) });
    } catch (s) {
      if (d.current !== t) return;
      console.error(s), k("Inverting failed part way through, so no file was produced. A long document at a high resolution can run the tab out of memory \u2014 try 1x or 2x, or split the file first.");
    } finally {
      z.current === t && (z.current = null), d.current === t && C(false);
    }
  }, O = !!a && !r && !!I;
  return e.jsxs(de, { title: "Invert PDF Colors", description: "Turn a PDF into its own negative \u2014 white pages become black, black text becomes white.", seoTitle: "Invert PDF Colors Online - Free Dark Mode PDF Tool", seoDescription: "Turn a PDF into its own negative at 72, 144 or 216 DPI as PNG or JPEG. Output pages become images, so text stops being selectable. Nothing is uploaded.", faqs: Se, children: [e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [e.jsx("div", { style: { background: "white", border: "1px solid var(--border)", borderRadius: "1rem", padding: "2rem" }, children: a ? e.jsxs("div", { className: "invert-grid", children: [e.jsxs("div", { children: [e.jsxs("p", { style: { fontWeight: 600, fontSize: "0.9rem", marginBottom: "0.75rem" }, children: [a.name, " \xB7 ", P || "?", " page", P === 1 ? "" : "s", " \xB7 ", W(a.size)] }), e.jsx("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: "0.75rem" }, children: ["before", "after"].map((t) => e.jsxs("div", { children: [e.jsx("p", { style: { fontSize: "0.72rem", textTransform: "uppercase", letterSpacing: "0.05em", color: "#94a3b8", marginBottom: "0.35rem" }, children: t === "before" ? "Original" : "Inverted" }), e.jsx("div", { style: { background: "#f1f5f9", borderRadius: "0.5rem", height: "340px", display: "flex", alignItems: "center", justifyContent: "center", padding: "0.5rem", overflow: "hidden" }, children: O ? e.jsx("p", { style: { fontSize: "0.75rem", color: "#94a3b8", textAlign: "center", margin: 0 }, children: "Nothing to show" }) : J ? e.jsx("img", { src: J[t], alt: `Page 1 ${t} inversion`, style: { maxWidth: "100%", maxHeight: "100%", width: "auto", height: "auto", objectFit: "contain", boxShadow: "0 2px 8px -2px rgba(0,0,0,0.25)" } }) : L ? e.jsx("p", { style: { fontSize: "0.75rem", color: "#94a3b8", textAlign: "center", margin: 0 }, children: "No preview for this page" }) : e.jsx($, { size: 22, style: { color: "var(--primary)", animation: "spin 1s linear infinite" } }) })] }, t)) }), e.jsx("p", { style: { fontSize: "0.75rem", color: "#94a3b8", marginTop: "0.5rem" }, children: O ? "This document could not be opened, so there is nothing to preview and nothing to invert. Choose another file." : L ? "This page could not be drawn as a thumbnail. Inverting the document still works \u2014 the preview is the only thing missing." : "Page 1, shown small. The download is rendered at the resolution selected on the right." })] }), e.jsxs("div", { id: "invert-pdf-colors-settings", children: [e.jsx("label", { htmlFor: "invert-scale", style: { display: "block", fontWeight: 700, marginBottom: "0.4rem" }, children: "Render resolution" }), e.jsx("select", { id: "invert-scale", value: b, disabled: l, onChange: (t) => {
    F(Number(t.target.value)), x(null);
  }, style: { width: "100%", padding: "0.5rem", borderRadius: "0.5rem", border: "1px solid var(--border)", marginBottom: "1rem", opacity: l ? 0.6 : 1, cursor: l ? "not-allowed" : "pointer" }, children: ke.map((t) => e.jsx("option", { value: t.value, children: t.label }, t.value)) }), e.jsx("label", { htmlFor: "invert-format", style: { display: "block", fontWeight: 700, marginBottom: "0.4rem" }, children: "Image format inside the PDF" }), e.jsxs("select", { id: "invert-format", value: j, disabled: l, onChange: (t) => {
    Z(t.target.value), x(null);
  }, style: { width: "100%", padding: "0.5rem", borderRadius: "0.5rem", border: "1px solid var(--border)", marginBottom: j === "jpeg" ? "0.5rem" : "1rem", opacity: l ? 0.6 : 1, cursor: l ? "not-allowed" : "pointer" }, children: [e.jsx("option", { value: "png", children: "PNG \u2014 lossless, sharpest text, smaller on type" }), e.jsx("option", { value: "jpeg", children: "JPEG \u2014 smaller on scans and photographs" })] }), j === "jpeg" && e.jsxs("div", { style: { marginBottom: "1rem" }, children: [e.jsxs("label", { htmlFor: "invert-quality", style: { display: "block", fontSize: "0.78rem", color: "#64748b", marginBottom: "0.25rem" }, children: ["JPEG quality: ", E] }), e.jsx("input", { id: "invert-quality", type: "range", min: "30", max: "100", value: E, disabled: l, onChange: (t) => {
    ee(Number(t.target.value)), x(null);
  }, style: { width: "100%", opacity: l ? 0.6 : 1, cursor: l ? "not-allowed" : "pointer" } })] }), e.jsxs("div", { style: { display: "flex", gap: "0.5rem", alignItems: "flex-start", background: "#fffbeb", border: "1px solid #fde68a", borderRadius: "0.5rem", padding: "0.75rem", marginBottom: "1rem" }, children: [e.jsx(be, { size: 18, style: { color: "#b45309", flexShrink: 0, marginTop: "2px" } }), e.jsxs("p", { style: { fontSize: "0.8rem", color: "#78350f", lineHeight: 1.5, margin: 0 }, children: ["Output pages are images at ", X(b), " DPI, or less on a page so large that the browser canvas limit forces a lower scale \u2014 that is reported with the result. Text stops being selectable or searchable, and the file will usually be larger than the original."] })] }), I && e.jsx("p", { role: "alert", style: { color: "#b91c1c", fontSize: "0.85rem", marginBottom: "0.75rem" }, children: I }), g && e.jsxs("p", { role: "status", style: { color: "#15803d", fontSize: "0.85rem", marginBottom: "0.75rem" }, children: ["Inverted ", g.pages, " page", g.pages === 1 ? "" : "s", ". ", W(g.inputSize), " in, ", W(g.outputSize), " out.", g.downscaled > 0 && e.jsxs("span", { style: { color: "#b45309" }, children: [" ", g.downscaled, " page", g.downscaled === 1 ? " was" : "s were", " too large for the browser canvas and ", g.downscaled === 1 ? "was" : "were", " rendered at a lower resolution, down to about ", g.lowestDpi, " DPI."] })] }), l && e.jsxs("div", { style: { marginBottom: "0.75rem" }, children: [e.jsx("div", { role: "progressbar", "aria-valuenow": q, "aria-valuemin": 0, "aria-valuemax": 100, "aria-label": "Inverting progress", style: { height: "8px", background: "#e2e8f0", borderRadius: "99px", overflow: "hidden" }, children: e.jsx("div", { style: { width: `${q}%`, height: "100%", background: "var(--primary)", transition: "width 0.2s ease" } }) }), e.jsxs("p", { role: "status", style: { fontSize: "0.78rem", color: "#64748b", marginTop: "0.3rem" }, children: ["Rendering and inverting\u2026 ", q, "%"] })] }), e.jsxs("button", { id: "invert-pdf-colors-download-btn", onClick: ie, disabled: l || !r, className: "tool-btn-primary", style: { width: "100%", padding: "0.9rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", cursor: l ? "wait" : "pointer", fontWeight: "bold", gap: "0.5rem", opacity: r ? 1 : 0.6 }, children: [l ? e.jsx($, { size: 20, style: { animation: "spin 1s linear infinite" } }) : e.jsx(ve, { size: 20 }), l ? "Inverting\u2026" : "Invert & Download"] }), e.jsx("div", { style: { textAlign: "center", marginTop: "0.75rem" }, children: e.jsx("button", { id: "invert-pdf-colors-reset-btn", onClick: te, style: { background: "none", border: "none", color: "#64748b", textDecoration: "underline" }, children: "Choose another file" }) })] })] }) : e.jsxs(e.Fragment, { children: [e.jsxs("div", { className: "tool-upload-area", ...re(), style: { border: "2px dashed var(--border)", borderRadius: "0.75rem", padding: "3rem 2rem", textAlign: "center", cursor: "pointer", background: ne ? "var(--secondary)" : "#f8fafc", transition: "all 0.2s ease" }, children: [e.jsx("input", { ...oe(), "aria-label": "Choose a PDF file to invert" }), e.jsx("div", { style: { width: "64px", height: "64px", background: "#e0f2fe", borderRadius: "50%", display: "flex", alignItems: "center", justifyContent: "center", margin: "0 auto 1rem", color: "#0284c7" }, children: e.jsx(K, { size: 32 }) }), e.jsx("h3", { style: { fontSize: "1.25rem", fontWeight: "600", marginBottom: "0.5rem" }, children: "Drag & drop a PDF here" }), e.jsx("p", { style: { color: "#64748b" }, children: "or click to select a file" })] }), I && e.jsx("p", { role: "alert", style: { color: "#b91c1c", fontSize: "0.85rem", marginTop: "0.75rem", textAlign: "center" }, children: I })] }) }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(he, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About inverting the colours of a PDF" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "This tool draws every page of your document with the same renderer a browser uses to display PDFs, flips every pixel to its opposite, and assembles the results into a new file called inverted-yourfile.pdf. A page of black text on white paper comes back as white text on black. Everything happens in this tab \u2014 the pdf.js worker is served from this site, and your document is never sent anywhere." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "What inversion actually is" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Each pixel holds three numbers between 0 and 255 for red, green and blue. Inverting replaces each with 255 minus itself. That single operation produces the familiar photographic negative: pure white (255, 255, 255) becomes pure black, pure black becomes pure white, and every colour lands on its complement \u2014 red becomes cyan, green becomes magenta, blue becomes yellow. Mid-grey stays where it is. It is not a theme, a stylesheet or a smart dark mode; there is no attempt to keep photographs looking natural or to preserve brand colours, so an inverted page containing a portrait will contain a negative of that portrait." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "The trade you are making" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "There is no way to invert a PDF's colours without rasterising it. Text in a PDF is drawn by referencing a font and a fill colour, and while a handful of those colour operators could in principle be rewritten, the same page will also contain images, shading patterns, transparency groups and vector art whose colours live in a dozen other places. Rendering the page and flipping the pixels is the only approach that treats all of it consistently. The consequence is that the output pages are pictures. Text selection, search, copy and paste, links, bookmarks, form fields and screen-reader access all stop working, and the file typically grows \u2014 on a text-heavy document, where the original is compact precisely because it is not a picture, the inverted copy can easily be tens of times larger." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Before converting, it is worth checking whether your reader can do this without touching the file. Acrobat has an accessibility setting that replaces document colours, macOS Preview and several mobile readers offer a night mode, and browser extensions do the same for PDFs opened on the web. Those are all display-time effects: reversible, free, and they leave the text intact. Converting is the right answer when the dark version has to be the artefact \u2014 something you hand to someone else, print, or open in software with no such setting." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "Choosing a resolution" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "A PDF point is one seventy-second of an inch, so the render scale is exactly the DPI divided by 72. The three settings are 1x (72 DPI), 2x (144 DPI) and 3x (216 DPI); on an A4 page that is roughly 595 by 842, 1191 by 1684 and 1786 by 2526 pixels. Two is the sensible default for reading on a screen, including a high-density display. One is for a quick look or when the document is enormous. Three is for printing, or for a scanned original that was captured at high resolution and would visibly soften at anything less. Any page whose canvas would exceed about 26 megapixels, or 32,767 pixels along either edge \u2014 a poster, a large engineering drawing, an unusually long strip \u2014 is rendered at a lower scale, because browsers return an empty canvas rather than an error when either limit is crossed. The setting is therefore a ceiling rather than a promise, and the result line tells you when a page fell short of it and by how much." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "PNG or JPEG inside the file" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "PNG stores the rendered page exactly. On documents made of type and line art that is both the sharper and the more compact choice, because lossless compression is very efficient on the large flat areas a page is mostly made of \u2014 and inversion makes those areas flat black, which costs almost nothing. On three different text documents at the default quality, PNG was the smaller file at 72, 144 and 216 DPI alike, by twenty to forty per cent. JPEG throws away detail the eye is least likely to miss and is dramatically smaller on scans, photographs and dense vector art \u2014 on a photographic document it came out roughly twenty-five times smaller than PNG at 72 DPI, and about seven times smaller at 216 \u2014 at the cost of faint ringing around hard edges, which on inverted text means a soft grey halo around white letters. Choose by what the pages contain rather than by how many there are, and treat every one of those margins as a rough guide: they move a long way from document to document, which is why the result line reports the real size each time. The quality slider runs from 30 to 100 and 85 is a good default. Pushing it to 95 cost about 1.8 times a 70 on the text documents measured here and about five and a half times on the photographic one; and on type a 95 landed at roughly twice the size of the lossless PNG, so if you find yourself reaching that high on a page of text, PNG is the better trade in both directions." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "What is preserved, and what is not" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: ["Page dimensions are kept exactly: each output page is created at the source page's displayed size in points and the image is stretched to fill it, so an A4 stays A4 and a document with mixed page sizes keeps them. Pages flagged with a rotation are rendered the way a reader shows them and written out upright. Everything else is discarded, because the output is a new document built from images \u2014 no metadata, no outline, no attachments, no annotation objects, no encryption. The file is written without an information dictionary at all, so it does not even carry the producer name or creation timestamp a PDF library would normally stamp on it. Annotations the renderer paints onto the page, such as highlights and filled form values, become part of the picture. If your real goal is to strip structure rather than to change colours, ", e.jsx("strong", { children: "Remove PDF Metadata" }), " and ", e.jsx("strong", { children: "Flatten PDF" }), " do that without rasterising; if you want the pages as standalone images instead of a PDF, ", e.jsx("strong", { children: "PDF to PNG" }), " is the direct route."] })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: De.map((t, c) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: t.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: t.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: t.desc })] }, c)) })] })] }), e.jsx("style", { children: `
                @keyframes spin { 100% { transform: rotate(360deg); } }
                .invert-grid {
                    display: grid;
                    grid-template-columns: minmax(0, 1.25fr) minmax(260px, 1fr);
                    gap: 2rem;
                    align-items: start;
                }
                @media (max-width: 820px) {
                    .invert-grid { grid-template-columns: minmax(0, 1fr); }
                }
            ` })] });
};
export {
  qe as default
};
