import { r as d, j as e } from "./index-DsTeKLg-.js";
import { R as k } from "./RelatedTools-Dai5N42q.js";
import { T as C } from "./ToolLayout-DdnzCrcK.js";
import { F as N } from "./FileUploader-Bd7gYSgk.js";
import { u as p } from "./xlsx-C4NO0l3T.js";
import { F as A } from "./FileSaver.min-DaXhTG4A.js";
import { w as g } from "./toolPageSchema-BVedbqe3.js";
import { D as O } from "./download-CwxFsq81.js";
import { C as y } from "./check-CBilouqu.js";
import { C as T } from "./copy-C94LAPHc.js";
import { S as J } from "./shield-CtuUP7ih.js";
import "./index-Bpm0RpmP.js";
const F = [{ title: "Nesting Becomes Dotted Columns", desc: "An address object turns into address.city and address.zip; a tags array turns into tags.0 and tags.1. Every leaf value reaches the table instead of being dropped as an empty cell.", icon: e.jsx(g, { color: "var(--primary)", size: 24 }) }, { title: "Downloads With A UTF-8 Marker", desc: "The saved file starts with a byte-order mark, so Excel reads accented names and non-Latin scripts correctly instead of guessing a legacy codepage and producing mojibake.", icon: e.jsx(y, { color: "var(--primary)", size: 24 }) }, { title: "Parsed In The Page", desc: "JSON.parse and the table builder both run in this tab. An API dump full of customer records is never uploaded, and closing the tab is the whole of the cleanup.", icon: e.jsx(J, { color: "var(--primary)", size: 24 }) }], E = [{ question: "What shape does my JSON need to be?", answer: 'An array of objects converts most naturally, one row per element. A single object becomes a one-row table. The common API envelope, where the array sits under a key such as {"users": [ ... ]}, is unwrapped automatically as long as exactly one key holds a non-empty array \u2014 so you usually do not have to strip the wrapper first. An array of plain values with no objects at all becomes a single column headed value.' }, { question: "How exactly does the flattening work?", answer: 'Every leaf in the structure gets its own column, named by joining the path with dots. Object keys contribute their name and array elements contribute their index, so a row of [{"id":1,"items":[{"sku":"A"},{"sku":"B"}]}] produces columns id, items.0.sku and items.1.sku. Note that a top-level {"items":[ ... ]} is not flattened that way at all: it matches the envelope rule below, so the array supplies the rows and you get one plain sku column instead. Nothing is lost, but the shape can be surprising: a row with fifty line items generates a hundred columns, and because column names include indexes, two records with different array lengths do not line up.' }, { question: "What do I get when records have different fields?", answer: "The header is the union of every key seen across all rows, in the order each first appears, and any record missing a key gets an empty cell. That is usually what you want for a slightly ragged export. It becomes unwieldy when the file mixes genuinely different record types, since the result is a wide, mostly blank sheet \u2014 splitting those into separate conversions gives a far more usable table." }, { question: "How are nulls, booleans and awkward strings written?", answer: "A null becomes an empty cell, indistinguishable from a missing key, so if that distinction matters in your data the CSV will not preserve it. Booleans are written as uppercase TRUE and FALSE. Strings containing a comma, a double quote or a line break are wrapped in quotes with any internal quotes doubled, which is standard CSV escaping \u2014 a value spanning two lines therefore occupies two physical lines in the file while remaining a single cell." }, { question: "My file will not convert. What is it complaining about?", answer: 'Three messages are possible, and in practice you will only ever see the first. "That file is not valid JSON" means the parser rejected it \u2014 a trailing comma, a single-quoted key or an unescaped newline inside a string are the usual causes, and the JSON Formatter will point at the line. "Could not read the file" is a filesystem-level failure. "Could not convert this JSON to a table" is a fallback for the table builder itself failing; it is rare, because almost any valid JSON converts to something \u2014 a bare number, string or null becomes a single row under a column headed value, and an empty array produces an empty CSV with a note above the box rather than an error. Note that JSON Lines, with one document per line, is not valid JSON as a whole; wrap the lines in brackets and separate them with commas first.' }, { question: "Does the copied text differ from the downloaded file?", answer: "Slightly. Download prepends a UTF-8 byte-order mark so that double-clicking the file opens it correctly in Excel; Copy puts the plain text on your clipboard without it, which is what you want when pasting into an editor or a terminal. The delimiter is a comma in both cases, with no option to change it, so a tool expecting semicolons will need the file adjusted afterwards." }], h = (t, o = "", r = {}) => {
  if (t === null || typeof t != "object") return r[o || "value"] = t, r;
  if (Array.isArray(t)) return t.length === 0 ? (r[o] = "", r) : (t.forEach((n, a) => h(n, o ? `${o}.${a}` : String(a), r)), r);
  const i = Object.keys(t);
  return i.length === 0 ? (r[o] = "", r) : (i.forEach((n) => h(t[n], o ? `${o}.${n}` : n, r)), r);
}, V = (t) => {
  if (Array.isArray(t)) return t;
  if (t && typeof t == "object") {
    const o = Object.keys(t).filter((r) => Array.isArray(t[r]));
    if (o.length === 1 && t[o[0]].length > 0) return t[o[0]];
  }
  return [t];
}, _ = () => {
  const [t, o] = d.useState(null), [r, i] = d.useState(null), [n, a] = d.useState(""), [f, m] = d.useState(false), b = (s) => {
    o(s), i(null), a("");
    const l = new FileReader();
    l.onerror = () => a("Could not read the file."), l.onload = (v) => {
      let u;
      try {
        u = JSON.parse(v.target.result);
      } catch {
        a("That file is not valid JSON.");
        return;
      }
      try {
        const c = V(u).map((S) => h(S)), j = p.json_to_sheet(c);
        i(p.sheet_to_csv(j));
      } catch {
        a("Could not convert this JSON to a table. Expected an object or an array of objects.");
      }
    }, l.readAsText(s);
  }, w = () => {
    const s = new Blob(["\uFEFF" + r], { type: "text/csv;charset=utf-8" });
    A.saveAs(s, t ? `${t.name.replace(/\.json$/i, "")}.csv` : "converted.csv");
  }, x = () => {
    navigator.clipboard.writeText(r), m(true), setTimeout(() => m(false), 2e3);
  };
  return e.jsx(C, { title: "JSON to CSV Converter", description: "Convert JSON files to CSV format.", seoTitle: "JSON to CSV Converter - Convert JSON to CSV Online", seoDescription: "Convert JSON data to CSV format online. Upload JSON file and download standard CSV spreadsheet compatible with Excel.", faqs: E, children: e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [r === null ? e.jsxs(e.Fragment, { children: [e.jsx(N, { id: "json-upload", onFileSelect: b, accept: { "application/json": [".json"] }, icon: g, label: "Drop JSON file here" }), n && e.jsx("p", { style: { marginTop: "1rem", textAlign: "center", color: "#dc2626", fontWeight: "500" }, children: n })] }) : e.jsxs("div", { style: { background: "white", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)" }, children: [e.jsxs("div", { style: { display: "flex", justifyContent: "space-between", marginBottom: "1rem" }, children: [e.jsxs("button", { onClick: w, className: "tool-btn-primary", style: { padding: "0.5rem 1rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", cursor: "pointer", fontWeight: "bold", display: "flex", alignItems: "center", gap: "0.5rem" }, children: [e.jsx(O, { size: 16 }), " Download CSV"] }), e.jsxs("button", { onClick: x, className: "tool-btn-secondary", style: { padding: "0.5rem 1rem", borderRadius: "0.5rem", background: "white", border: "1px solid var(--border)", cursor: "pointer", fontWeight: "bold", display: "flex", alignItems: "center", gap: "0.5rem" }, children: [f ? e.jsx(y, { size: 16, color: "green" }) : e.jsx(T, { size: 16 }), " Copy"] })] }), r === "" && e.jsx("p", { style: { marginBottom: "1rem", color: "var(--text-secondary)" }, children: "This JSON contained no rows, so the CSV is empty." }), e.jsx("textarea", { readOnly: true, value: r, style: { width: "100%", height: "400px", padding: "1rem", borderRadius: "0.5rem", border: "1px solid var(--border)", fontFamily: "monospace", fontSize: "0.9rem", background: "#f8fafc", color: "#334155" } }), e.jsx("div", { style: { textAlign: "center", marginTop: "1rem" }, children: e.jsx("button", { onClick: () => {
    o(null), i(null), a("");
  }, style: { background: "none", border: "none", color: "#64748b", textDecoration: "underline", cursor: "pointer" }, children: "Convert Another" }) })] }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(k, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About JSON to CSV Converter" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["JSON is a tree; a spreadsheet is a rectangle. Converting between them means deciding what becomes a row and what becomes a column. Drop a ", e.jsx("code", { children: ".json" }), " file above and this page makes those decisions for you, then shows the resulting CSV so you can check it before downloading."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "How rows and columns are chosen" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Rows come first. A top-level array gives one row per element. A top-level object whose only array is under a single key \u2014 the ", e.jsxs("code", { children: ["{", '"data": [ \u2026 ]', "}"] }), " envelope most APIs return \u2014 is unwrapped so that array supplies the rows. Anything else is treated as one record. Columns then come from flattening each row: every nested key and array index is folded into a dotted path, so no leaf value is silently dropped the way a naive conversion would drop an object into a blank cell."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Where the tree does not fit the rectangle" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Arrays of sub-records are the hard case. Because each element gets its own indexed columns, an order containing three line items produces ", e.jsx("code", { children: "items.0.sku" }), " through ", e.jsx("code", { children: "items.2.qty" }), ", and an order with ten produces thirty. Nothing lines up between rows of different lengths, and the sheet grows sideways fast. When the nested array is the interesting part, you will get a far more useful result by extracting it and converting that instead, giving one row per line item with the order id repeated."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Encoding, quoting and Excel" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Values needing protection are quoted and internal quotes doubled, per the usual CSV convention, so text with commas or newlines survives intact. The downloaded file is prefixed with a UTF-8 byte-order mark, which is the difference between a name like Zo\xEB opening correctly in Excel and arriving as ", e.jsx("code", { children: "Zo\xC3\xAB" }), ". Google Sheets and LibreOffice detect UTF-8 without the marker and handle either version."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Before and after this step" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: ["If the file will not parse, the ", e.jsx("strong", { children: "JSON Formatter" }), " reports where the syntax breaks down. To go back the other way, ", e.jsx("strong", { children: "CSV to JSON" }), " rebuilds an array of objects from a table, though it will not reconstruct the nesting that flattening removed \u2014 dotted headings come back as literal key names. And if the real destination is a workbook rather than a text file, converting here and then running the result through ", e.jsx("strong", { children: "CSV to Excel" }), " produces an ", e.jsx("code", { children: ".xlsx" }), "."] })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: F.map((s, l) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: s.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: s.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: s.desc })] }, l)) })] })] }) });
};
export {
  _ as default
};
