import { r as g, j as e, L as Q, a as F } from "./index-OUpguYFg.js";
import { R as xe } from "./RelatedTools-dQ1AUZ0r.js";
import { T as ve } from "./ToolLayout-CuKFTkh4.js";
import { F as ke } from "./FileUploader-Dhw1e8vw.js";
import { r as ee, u as M } from "./xlsx-C4NO0l3T.js";
import { E as je } from "./jspdf.es.min-8gPLB5Ns.js";
import { a as Se } from "./jspdf.plugin.autotable-DdtGmf65.js";
import { F as Te } from "./FileSaver.min-DzHDzKVl.js";
import { c as U, T as Ce } from "./tools-B3OPepIK.js";
import { A as te } from "./alert-triangle-BqnKTzYa.js";
import { D as Fe } from "./download-DqlBxbZM.js";
import { S as Ae } from "./shield-check-DCjAjSWE.js";
import "./index-CBYUSgtG.js";
import "./shield-BrCBnKXk.js";
const Pe = /* @__PURE__ */ new Set(["\u20AC", "\u201A", "\u0192", "\u201E", "\u2026", "\u2020", "\u2021", "\u02C6", "\u2030", "\u0160", "\u2039", "\u0152", "\u017D", "\u2018", "\u2019", "\u201C", "\u201D", "\u2022", "\u2013", "\u2014", "\u02DC", "\u2122", "\u0161", "\u203A", "\u0153", "\u017E", "\u0178"]), N = 500, ze = { 0: "#NULL!", 7: "#DIV/0!", 15: "#VALUE!", 23: "#REF!", 29: "#NAME?", 36: "#NUM!", 42: "#N/A", 43: "#GETTING_DATA" }, De = (t) => t.length >= 4 && t[0] === 80 && t[1] === 75 && t[2] < 9 ? "zip" : t.length >= 8 && t[0] === 208 && t[1] === 207 && t[2] === 17 && t[3] === 224 ? "cfb" : "text", Re = (t) => {
  if (t.length >= 2 && t[0] === 255 && t[1] === 254) return "utf-16le";
  if (t.length >= 2 && t[0] === 254 && t[1] === 255) return "utf-16be";
  const i = Math.min(t.length, 2048);
  let a = 0, l = 0;
  for (let n = 0; n < i; n += 1) t[n] === 0 && (n % 2 === 0 ? a += 1 : l += 1);
  return l > i / 4 && a === 0 ? "utf-16le" : a > i / 4 && l === 0 ? "utf-16be" : null;
}, Ee = (t) => {
  const i = Re(t);
  if (i) return new TextDecoder(i).decode(t);
  try {
    return new TextDecoder("utf-8", { fatal: true }).decode(t);
  } catch {
    return new TextDecoder("windows-1252").decode(t);
  }
}, Ie = (t) => {
  if (!t || !t.trim()) return false;
  const i = t.slice(0, 8192);
  let a = 0;
  for (let l = 0; l < i.length; l += 1) {
    const n = i.charCodeAt(l);
    n === 9 || n === 10 || n === 13 || (n < 32 || n === 127 || n === 65533) && (a += 1);
  }
  return a <= i.length * 0.01;
}, Ne = (t, i) => {
  let a = 1 / 0, l = 1 / 0, n = -1, s = -1;
  for (const h of Object.keys(t)) {
    if (h.charCodeAt(0) === 33) continue;
    const d = i(h);
    if (!d) continue;
    const { r: f, c: b } = d;
    !Number.isInteger(f) || !Number.isInteger(b) || f < 0 || b < 0 || (f < a && (a = f), f > n && (n = f), b < l && (l = b), b > s && (s = b));
  }
  return n < 0 || s < 0 ? null : { s: { r: a, c: l }, e: { r: n, c: s } };
}, Le = (t, i, a, l) => {
  for (const n of Object.keys(t)) {
    if (n.charCodeAt(0) === 33) continue;
    const s = t[n];
    if (!s || s.t !== "e") continue;
    const h = l(n);
    if (!h) continue;
    const d = h.r - i.s.r, f = h.c - i.s.c;
    if (d < 0 || f < 0 || d >= a.length) continue;
    const b = s.w || ze[s.v] || (typeof s.v == "string" ? s.v : "#ERROR");
    a[d] || (a[d] = []), a[d][f] = b;
  }
  return a;
}, We = 2, ae = (t) => t == null || String(t).trim() === "", Be = (t) => !t || t.every(ae), oe = (t, i) => {
  let a = 0, l = t - 1;
  for (; a <= l && i(a); ) a += 1;
  for (; l >= a && i(l); ) l -= 1;
  const n = [];
  let s = 0;
  for (let h = a; h <= l; h += 1) {
    if (i(h)) {
      if (s += 1, s > We) continue;
    } else s = 0;
    n.push(h);
  }
  return n;
}, Oe = (t) => {
  const i = oe(t.length, (s) => Be(t[s]));
  if (i.length === 0) return [];
  const a = i.reduce((s, h) => {
    const d = t[h];
    return Math.max(s, d ? d.length : 0);
  }, 0), l = new Array(a).fill(false);
  for (const s of i) {
    const h = t[s];
    if (h) for (let d = 0; d < a; d += 1) l[d] || ae(h[d]) || (l[d] = true);
  }
  const n = oe(a, (s) => !l[s]);
  return n.length === 0 ? [] : i.map((s) => {
    const h = t[s];
    return n.map((d) => {
      const f = h ? h[d] : "";
      return f == null ? "" : String(f);
    });
  });
}, $e = (t) => !Number.isFinite(t) || t <= 0 ? "0 KB" : t < 1024 ? `${t} B` : t < 1024 * 1024 ? `${(t / 1024).toFixed(t < 10 * 1024 ? 1 : 0)} KB` : `${(t / 1024 / 1024).toFixed(1)} MB`, re = (t) => M.decode_cell(t), qe = [{ title: "Every sheet, or just the ones you pick", desc: "The workbook is opened and each tab listed with its row and column count. Tick the sheets you want; each selected sheet starts on a fresh page as its own table. Tabs the workbook has hidden are marked and left unticked, so a lookup sheet does not walk into a document you are about to send.", icon: e.jsx(Ce, { color: "var(--primary)", size: 24 }) }, { title: "Real table rulings, not a screenshot", desc: "Rows are drawn with jsPDF-AutoTable as vector text and lines, so the PDF stays selectable, searchable and sharp at any zoom. Long cells wrap inside their column and tall tables continue onto the next page with the header repeated.", icon: e.jsx(U, { color: "var(--primary)", size: 24 }) }, { title: "The workbook never leaves the tab", desc: "Parsing and PDF generation both run in this browser with the File API. Budgets, payroll and pricing sheets are exactly the files that should not be posted to a converter, and here there is no upload step at all.", icon: e.jsx(Ae, { color: "var(--primary)", size: 24 }) }], He = [{ question: "Do my formulas come across?", answer: 'Not as formulas \u2014 as the value each one last calculated to. A workbook stores both the formula and its cached result, and it is the cached result that is read and printed. That is almost always what you want in a PDF, but it has one sharp edge: the number comes from the file rather than from a fresh recalculation, so a workbook saved with automatic calculation switched off, or one whose external links were never refreshed, will print stale figures. Open it, force a recalculation, save, then convert. A formula whose result is an error prints that error \u2014 #N/A, #REF!, #DIV/0!, #VALUE!, #NAME?, #NUM!, #NULL! and the newer #SPILL! and #CALC! all appear as themselves, because a blank in their place would read as "nothing here" rather than "this figure is broken". A formula that was never calculated at all has no cached result to print and does come through blank.' }, { question: "Why does the PDF not look like my spreadsheet?", answer: "Because nothing about the presentation layer is read. Fills, font colours, borders, conditional formatting, column widths, frozen panes, charts, images, shapes, comments and data validation are all ignored. Number formats are read only for the text they produce \u2014 \xA31,234.50 keeps its symbol and separators \u2014 never for the colour or alignment they carry. What is read is the grid of values, and what is drawn is a clean table with a coloured header row and alternating row shading. If the visual design of the sheet is the point \u2014 a formatted invoice, a dashboard, a certificate \u2014 do not use this; print to PDF from Excel, LibreOffice Calc or Google Sheets instead, because only those know the layout." }, { question: "What happens to merged cells?", answer: "They flatten. A merged region stores its value in the top-left cell only, and the covered cells are genuinely empty in the file, so the value lands in the first column of the merge and the rest of the row comes through blank. A merged title spanning A1:F1 therefore prints as one value followed by five empty cells \u2014 assuming the table below it uses those columns, which is the usual case. If nothing else in the sheet reaches column F, the empty columns are trimmed away with the rest of the oversized used range and the title simply sits alone. It is worth unmerging header rows before converting, otherwise your column headings can end up one row lower than the table expects." }, { question: "How are dates, currency and percentages printed?", answer: "As the text the spreadsheet displays, not as the underlying serial number \u2014 a date shown as 15/03/2024 prints as 15/03/2024, and a cell showing \xA31,234.50 prints with the symbol and separators. The advantage is that the PDF reads exactly like the sheet. The consequence is that the output inherits whatever locale the workbook was authored in, including day-first or month-first ordering, so a PDF meant for an international audience is easier to read if you switch the date columns to an unambiguous format first." }, { question: "Some characters came out as question marks.", answer: 'The PDF is built with the standard Helvetica font, which covers the Latin-1 / Windows-1252 character set: English, Western European accents, curly quotes, dashes, the euro sign, and the symbols that live in Latin-1 such as \xB0 \xB1 \xB5 \xD7 \xF7 \xBC \xBD \xBE. Anything outside that \u2014 Cyrillic, Greek, Hebrew, Arabic, Chinese, Japanese, Korean, Devanagari, Thai, emoji, tick marks, and mathematical symbols like \u0394 \u03A9 \u03C0 \u2211 \u2014 has no glyph available, so each such character is replaced with a question mark and the total is reported after conversion. Invisible control characters left behind by some exports are replaced too, and counted in the same total. If a cell was also cut at 500 characters, only the replacements that survived the cut are counted. Note that ticking "Print the sheet name above each table" puts the sheet names through the same conversion, so the total can rise slightly. No font file is embedded, which is what keeps the output small and the conversion instant. For a workbook in a non-Latin script, print to PDF from the spreadsheet application, which can embed the right font.' }, { question: "My table is too wide for the page.", answer: "Switch the orientation to landscape, which is picked automatically when any selected sheet has more than six columns, and drop the text size to Compact. The column count that triggers that is counted from real data: columns that are empty from top to bottom are trimmed off the edges of the grid first, so a sheet whose file claims a hundred columns but only fills three is treated as three. Beyond roughly twenty genuine columns even landscape A4 runs out of room and the columns become unreadably narrow \u2014 AutoTable will keep shrinking them rather than clipping the data, so nothing is lost, but the result stops being useful. Split a very wide sheet into two ranges, or convert to CSV and print from there." }, { question: "What counts as the header row?", answer: 'The first row of each sheet that contains anything. Blank rows at the very top of a sheet are dropped before the table is laid out, so a spacer above your column names does not push the header out of position \u2014 but a title line does, because it is real content. If your sheet starts with a title rather than column names, untick "Treat the first row of each sheet as a header" and every row is printed as a body row instead. There is no support for multi-row headers; a two-line heading will have its first line styled as the header and its second printed as ordinary data.' }, { question: "Are hidden sheets included?", answer: `They are listed, marked "hidden", and left unticked. A hidden tab is usually a lookup table, a working column or last quarter's figures, and quietly printing it into a document you are about to email to somebody is the wrong default. Tick it and it converts exactly like any other sheet. This reads the workbook's own hidden flag, so it covers both ordinary hiding and the "very hidden" state set from the VBA editor. It says nothing about hidden rows and columns inside a visible sheet \u2014 those are part of the value grid and are printed.` }, { question: "Which file types can I drop in?", answer: "The picker accepts .xlsx, .xlsm, .xlsb, the older binary .xls, OpenDocument .ods, and plain .csv \u2014 anything else it simply will not take, so if clicking a file appears to do nothing, that is why. What the file actually contains matters more than what it is called: the first bytes decide whether it is treated as a ZIP-based workbook, an old binary workbook, or delimited text, so a CSV that someone saved as .xlsx is still read correctly and still decoded as UTF-8 or UTF-16 rather than mangled. Macros in an .xlsm or .xlsb are ignored \u2014 only the stored values are read, which is the only sensible behaviour for a PDF. A password-protected workbook cannot be opened at all and will report an error; remove the password in Excel first." }, { question: "Are there limits on size?", answer: "The whole workbook is read into memory, converted to an array of rows and then laid out, so peak memory runs to several times the file size and everything happens on the main thread. A few thousand rows convert in a second or two. Tens of thousands of rows will produce a very large PDF and can make the tab unresponsive while it draws, so convert in slices if you are dealing with an export that size. Individual cells longer than 500 characters are truncated with an ellipsis, because a single cell taller than the page has nowhere to go." }, { question: "Is anything uploaded?", answer: "Your workbook is not. It is read from your disk by the browser, parsed by a JavaScript library in this tab, and the finished PDF is handed straight to your downloads folder \u2014 no request carries any part of it, nothing is queued on a server and there is no copy to delete afterwards. To be exact rather than merely reassuring: the page itself loads analytics and advertising scripts like most of the web, and those make requests of their own. Nothing from your file is in them, and you can confirm all of this by opening the network panel or simply going offline \u2014 the converter keeps working with no connection at all." }], A = { color: "var(--primary)", fontWeight: 600 }, q = { a4: { label: "A4", format: "a4" }, letter: { label: "Letter", format: "letter" }, legal: { label: "Legal", format: "legal" } }, H = { compact: { label: "Compact", body: 7.5, padding: 2.5 }, normal: { label: "Normal", body: 9, padding: 4 }, large: { label: "Large", body: 11, padding: 5 } }, rt = () => {
  const [t, i] = g.useState(null), [a, l] = g.useState([]), [n, s] = g.useState({}), [h, d] = g.useState(null), [f, b] = g.useState(false), [I, L] = g.useState(false), [u, P] = g.useState(null), [V, ne] = g.useState("a4"), [W, se] = g.useState("auto"), [_, ie] = g.useState("normal"), [X, le] = g.useState(true), [G, de] = g.useState(true), z = g.useRef(0), B = g.useRef(0), ce = () => {
    z.current += 1, i(null), l([]), s({}), d(null), P(null), b(false), L(false);
  }, K = () => {
    P(null), B.current = 0;
  }, D = (o) => (c) => {
    K(), o(c);
  }, he = (o) => {
    const c = Ne(o, re);
    c && (o["!ref"] = M.encode_range(c));
    const y = M.sheet_to_json(o, { header: 1, raw: false, defval: "", blankrows: true });
    return c && Le(o, c, y, re), Oe(y);
  }, ue = async (o) => {
    i(o), d(null), P(null), b(true);
    try {
      const c = await o.arrayBuffer(), y = new Uint8Array(c);
      let k;
      if (De(y) === "text") {
        const r = Ee(y);
        if (!Ie(r)) throw new Error("This file is not a spreadsheet or a delimited text file.");
        k = ee(r, { type: "string" });
      } else k = ee(y, { type: "array" });
      const R = /* @__PURE__ */ new Set();
      (k.Workbook && k.Workbook.Sheets || []).forEach((r) => {
        r && r.name && Number(r.Hidden) > 0 && R.add(r.name);
      });
      const m = k.SheetNames.map((r) => {
        const v = he(k.Sheets[r]);
        return { name: r, rows: v, hidden: R.has(r), rowCount: v.length, columnCount: v.reduce((w, E) => Math.max(w, E.length), 0) };
      });
      l(m);
      const j = {};
      m.forEach((r) => {
        j[r.name] = r.rowCount > 0 && !r.hidden;
      }), s(j), m.every((r) => r.rowCount === 0) && d("Every sheet in this workbook is empty, so there is nothing to lay out.");
    } catch (c) {
      console.error(c), d("This workbook could not be opened. Password-protected files cannot be read; a damaged, empty or partially downloaded file, and a file that is not a spreadsheet at all, fail the same way. Try re-saving it as .xlsx and dropping it in again."), l([]);
    } finally {
      b(false);
    }
  }, me = async () => {
    const o = a.filter((x) => n[x.name] && x.rowCount > 0);
    if (o.length === 0 || Date.now() - B.current < 800) return;
    const c = z.current;
    L(true), d(null), P(null), await new Promise((x) => setTimeout(x, 20));
    let y = 0, k = 0;
    const R = (x) => {
      let m = "", j = 0, r = 0;
      for (const v of String(x ?? "")) {
        const w = v.codePointAt(0);
        w === 9 || w === 10 || w === 13 ? m += " " : w >= 32 && w <= 126 ? m += v : w === 160 ? m += " " : w > 160 && w <= 255 || Pe.has(v) ? m += v : (m += "?", j += 1, m.length <= N && (r += 1));
      }
      return m.length > N ? (k += 1, y += r, `${m.slice(0, N)}\u2026`) : (y += j, m);
    };
    try {
      const x = o.reduce((p, S) => Math.max(p, S.columnCount), 0), m = W === "auto" ? x > 6 ? "landscape" : "portrait" : W, j = H[_] || H.normal, r = new je({ orientation: m, unit: "pt", format: (q[V] || q.a4).format }), v = r.internal.pageSize.getWidth() - 56;
      let w = true;
      o.forEach((p) => {
        w || r.addPage(), w = false;
        const S = p.rows.map((ye) => ye.map(R));
        let Y = 44;
        G && (r.setFont("helvetica", "bold"), r.setFontSize(13), r.setTextColor(15, 23, 42), r.text(R(p.name), 32, 44), r.setFont("helvetica", "normal"), r.setFontSize(8), r.setTextColor(100, 116, 139), r.text(`${p.rowCount} row${p.rowCount === 1 ? "" : "s"} \xB7 ${p.columnCount} column${p.columnCount === 1 ? "" : "s"}`, 32, 58), Y = 70);
        const Z = X && S.length > 1, be = Math.min(46, v / Math.max(1, p.columnCount));
        Se(r, { head: Z ? [S[0]] : void 0, body: Z ? S.slice(1) : S, startY: Y, margin: { top: 36, right: 28, bottom: 40, left: 28 }, styles: { font: "helvetica", fontSize: j.body, cellPadding: j.padding, overflow: "linebreak", minCellWidth: be, lineColor: [226, 232, 240], lineWidth: 0.5, textColor: [30, 41, 59], valign: "top" }, headStyles: { fillColor: [37, 99, 235], textColor: [255, 255, 255], fontStyle: "bold" }, alternateRowStyles: { fillColor: [246, 249, 253] }, theme: "grid" });
      });
      const E = r.getNumberOfPages(), pe = r.internal.pageSize.getWidth(), fe = r.internal.pageSize.getHeight();
      for (let p = 1; p <= E; p += 1) r.setPage(p), r.setFont("helvetica", "normal"), r.setFontSize(8), r.setTextColor(148, 163, 184), r.text(`${p} / ${E}`, pe / 2, fe - 18, { align: "center" });
      if (z.current !== c) return;
      const ge = t.name.replace(/\.(xlsx|xlsm|xlsb|xls|csv|ods)$/i, "") || "workbook", we = r.output("blob");
      Te.saveAs(we, `${ge}.pdf`), B.current = Date.now(), P({ sheets: o.length, pages: E, rows: o.reduce((p, S) => p + S.rowCount, 0), orientation: m, replaced: y, truncated: k });
    } catch (x) {
      console.error(x), z.current === c && d("The PDF could not be generated. A sheet with an extreme number of columns is the usual cause \u2014 try Compact text with landscape orientation, or convert fewer sheets at once.");
    } finally {
      z.current === c && L(false);
    }
  }, T = a.filter((o) => n[o.name] && o.rowCount > 0).length, C = a.filter((o) => o.hidden && o.rowCount > 0).length, J = a.filter((o) => n[o.name]).reduce((o, c) => Math.max(o, c.columnCount), 0), O = { display: "block", fontWeight: 600, fontSize: "0.85rem", marginBottom: "0.4rem", color: "#334155" }, $ = { width: "100%", padding: "0.6rem 0.7rem", border: "1px solid var(--border)", borderRadius: "0.5rem", background: "white", fontSize: "0.9rem" };
  return e.jsx(ve, { title: "Excel to PDF", description: "Turn the sheets of a workbook into a clean, selectable PDF table \u2014 entirely in your browser.", seoTitle: "Excel to PDF Converter - Free Online Tool", seoDescription: "Convert XLSX, XLS, ODS or CSV sheets into a PDF of real selectable tables. Pick sheets, page size and orientation. Values only, and nothing is uploaded.", faqs: He, children: e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [t ? e.jsxs("div", { style: { background: "white", border: "1px solid var(--border)", borderRadius: "1rem", padding: "2rem" }, children: [e.jsxs("div", { style: { display: "flex", alignItems: "center", gap: "0.75rem", marginBottom: "1.5rem" }, children: [e.jsx(U, { size: 28, color: "var(--primary)" }), e.jsxs("div", { style: { flex: 1, minWidth: 0 }, children: [e.jsx("p", { style: { fontWeight: 600, margin: 0, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }, children: t.name }), e.jsxs("p", { style: { margin: 0, color: "#64748b", fontSize: "0.85rem" }, children: [$e(t.size), a.length > 0 ? ` \xB7 ${a.length} sheet${a.length === 1 ? "" : "s"}` : ""] })] }), e.jsx("button", { id: "excel-to-pdf-reset-btn", onClick: ce, style: { background: "none", border: "none", color: "#64748b", textDecoration: "underline", cursor: "pointer" }, children: "Start over" })] }), f && e.jsxs("p", { style: { color: "#64748b" }, children: [e.jsx(Q, { size: 18, style: { verticalAlign: "middle", marginRight: "0.5rem", animation: "spin 1s linear infinite" } }), "Reading the workbook\u2026"] }), h && e.jsxs("div", { style: { background: "#fef2f2", border: "1px solid #fecaca", color: "#b91c1c", borderRadius: "0.75rem", padding: "1rem", marginBottom: "1.5rem" }, children: [e.jsx(te, { size: 18, style: { verticalAlign: "middle", marginRight: "0.5rem" } }), h] }), a.length > 0 && e.jsxs(e.Fragment, { children: [e.jsxs("div", { id: "excel-to-pdf-settings", style: { display: "grid", gap: "1.5rem" }, children: [e.jsxs("div", { children: [e.jsx("p", { style: { fontWeight: 700, marginBottom: "0.75rem" }, children: "Sheets to include" }), C > 0 && e.jsxs("p", { style: { margin: "-0.35rem 0 0.75rem", color: "#64748b", fontSize: "0.85rem" }, children: [C === 1 ? "One sheet is" : `${C} sheets are`, " hidden in the workbook, so ", C === 1 ? "it is" : "they are", " listed but left unticked. Tick ", C === 1 ? "it" : "them", " to include ", C === 1 ? "it" : "them", " anyway."] }), e.jsx("div", { style: { display: "grid", gap: "0.5rem" }, children: a.map((o) => e.jsxs("label", { style: { display: "flex", alignItems: "center", gap: "0.75rem", padding: "0.7rem 0.9rem", border: "1px solid var(--border)", borderRadius: "0.6rem", background: o.rowCount === 0 ? "#f8fafc" : "white", cursor: o.rowCount === 0 ? "not-allowed" : "pointer", opacity: o.rowCount === 0 ? 0.6 : 1 }, children: [e.jsx("input", { type: "checkbox", checked: !!n[o.name], disabled: o.rowCount === 0, onChange: (c) => {
    K(), s((y) => ({ ...y, [o.name]: c.target.checked }));
  } }), e.jsx("span", { style: { fontWeight: 600, flex: 1, overflow: "hidden", textOverflow: "ellipsis" }, children: o.name }), o.hidden && e.jsx("span", { style: { background: "#f1f5f9", border: "1px solid var(--border)", color: "#475569", borderRadius: "999px", padding: "0.1rem 0.55rem", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", textTransform: "uppercase" }, children: "hidden" }), e.jsx("span", { style: { color: "#64748b", fontSize: "0.85rem" }, children: o.rowCount === 0 ? "empty" : `${o.rowCount} \xD7 ${o.columnCount}` })] }, o.name)) })] }), e.jsxs("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(160px, 1fr))", gap: "1rem" }, children: [e.jsxs("div", { children: [e.jsx("label", { style: O, htmlFor: "excel-to-pdf-page-size", children: "Page size" }), e.jsx("select", { id: "excel-to-pdf-page-size", value: V, onChange: (o) => D(ne)(o.target.value), style: $, children: Object.entries(q).map(([o, c]) => e.jsx("option", { value: o, children: c.label }, o)) })] }), e.jsxs("div", { children: [e.jsx("label", { style: O, htmlFor: "excel-to-pdf-orientation", children: "Orientation" }), e.jsxs("select", { id: "excel-to-pdf-orientation", value: W, onChange: (o) => D(se)(o.target.value), style: $, children: [e.jsx("option", { value: "auto", children: "Auto (landscape above 6 columns)" }), e.jsx("option", { value: "portrait", children: "Portrait" }), e.jsx("option", { value: "landscape", children: "Landscape" })] })] }), e.jsxs("div", { children: [e.jsx("label", { style: O, htmlFor: "excel-to-pdf-text-size", children: "Table text" }), e.jsx("select", { id: "excel-to-pdf-text-size", value: _, onChange: (o) => D(ie)(o.target.value), style: $, children: Object.entries(H).map(([o, c]) => e.jsxs("option", { value: o, children: [c.label, " (", c.body, "pt)"] }, o)) })] })] }), e.jsxs("div", { style: { display: "grid", gap: "0.6rem" }, children: [e.jsxs("label", { style: { display: "flex", alignItems: "center", gap: "0.6rem", cursor: "pointer" }, children: [e.jsx("input", { type: "checkbox", checked: X, onChange: (o) => D(le)(o.target.checked) }), e.jsx("span", { children: "Treat the first row of each sheet as a header (repeated on every page)" })] }), e.jsxs("label", { style: { display: "flex", alignItems: "center", gap: "0.6rem", cursor: "pointer" }, children: [e.jsx("input", { type: "checkbox", checked: G, onChange: (o) => D(de)(o.target.checked) }), e.jsx("span", { children: "Print the sheet name above each table" })] })] }), J > 20 && e.jsxs("div", { style: { background: "#fffbeb", border: "1px solid #fde68a", color: "#92400e", borderRadius: "0.75rem", padding: "0.9rem 1rem", fontSize: "0.9rem" }, children: [e.jsx(te, { size: 16, style: { verticalAlign: "middle", marginRight: "0.5rem" } }), "One of the selected sheets is ", J, " columns wide. Even landscape with Compact text will make those columns very narrow \u2014 nothing is clipped, but the table may be hard to read."] })] }), e.jsxs("div", { style: { marginTop: "1.5rem" }, children: [e.jsxs("button", { id: "excel-to-pdf-download-btn", onClick: me, disabled: I || T === 0, className: "tool-btn-primary", style: { width: "100%", padding: "1rem", borderRadius: "0.5rem", background: T === 0 ? "#cbd5e1" : "var(--primary)", color: "white", border: "none", cursor: I ? "wait" : T === 0 ? "not-allowed" : "pointer", fontWeight: "bold", display: "flex", justifyContent: "center", alignItems: "center", gap: "0.5rem" }, children: [I ? e.jsx(Q, { size: 20, style: { animation: "spin 1s linear infinite" } }) : e.jsx(Fe, { size: 20 }), I ? "Building PDF\u2026" : T === 0 ? "Select at least one sheet" : `Convert ${T} sheet${T === 1 ? "" : "s"} to PDF`] }), e.jsx("style", { children: "@keyframes spin { 100% { transform: rotate(360deg); } }" })] }), u && e.jsxs("div", { style: { marginTop: "1.25rem", background: "#f0fdf4", border: "1px solid #bbf7d0", borderRadius: "0.75rem", padding: "1rem", color: "#166534" }, children: [e.jsxs("p", { style: { margin: 0, fontWeight: 600 }, children: ["Saved ", u.pages, " page", u.pages === 1 ? "" : "s", " from ", u.sheets, " sheet", u.sheets === 1 ? "" : "s", " (", u.rows, " row", u.rows === 1 ? "" : "s", ", ", u.orientation, ")."] }), (u.replaced > 0 || u.truncated > 0) && e.jsxs("p", { style: { margin: "0.5rem 0 0", color: "#92400e" }, children: [u.replaced > 0 && `${u.replaced} character${u.replaced === 1 ? " was" : "s were"} replaced with "?" because the standard PDF fonts have no glyph for ${u.replaced === 1 ? "it" : "them"} \u2014 anything outside Latin-1 / Windows-1252, plus the invisible control characters some exports leave behind. `, u.truncated > 0 && `${u.truncated} cell${u.truncated === 1 ? " was" : "s were"} longer than ${N} characters and got an ellipsis.`] })] })] })] }) : e.jsxs("div", { className: "tool-upload-area", children: [e.jsx(ke, { onFileSelect: ue, accept: { "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet": [".xlsx"], "application/vnd.ms-excel.sheet.macroEnabled.12": [".xlsm"], "application/vnd.ms-excel.sheet.binary.macroEnabled.12": [".xlsb"], "application/vnd.ms-excel": [".xls"], "application/vnd.oasis.opendocument.spreadsheet": [".ods"], "text/csv": [".csv"] }, icon: U, label: "Drag & drop a spreadsheet here", subLabel: "or click to select \u2014 .xlsx, .xlsm, .xlsb, .xls, .ods, .csv" }), e.jsx("p", { style: { textAlign: "center", color: "#64748b", fontSize: "0.82rem", marginTop: "0.75rem" }, children: "Other file types are refused by the picker itself, so nothing happens when you choose one." })] }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(xe, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About Excel to PDF" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Drop in a workbook, choose which sheets matter, and download a PDF in which each of those sheets is drawn as a proper table. The output is vector text, so it stays selectable and searchable, prints crisply and is a fraction of the size of a screenshot. Everything \u2014 reading the file, laying out the tables, writing the PDF \u2014 happens inside this browser tab." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "Values, not appearance" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "This is the single thing to understand before you use it. A spreadsheet file holds two very different kinds of information: the data, and the way that data is presented. Only the data is read here. Each sheet is turned into a plain grid of displayed values and then redrawn in a consistent house style \u2014 a blue header band, hairline rules, alternating row shading. Cell fills, font choices, borders, conditional formatting, column widths, frozen panes, charts, pictures, shapes, comments and data validation are not carried over, because none of them are part of the value grid." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Formulas are read as the result they last calculated to, which is stored alongside the formula in the file. That is the correct behaviour for a printed document, but it makes the output only as fresh as the last recalculation \u2014 a workbook saved with calculation set to manual can print numbers that no longer match its own inputs. Where that stored result is an error, the error is what prints: #N/A, #REF!, #DIV/0! and the rest appear in the table as themselves, because a broken figure that arrives as an empty cell is worse than one that arrives loudly. Merged cells flatten in the same way they do in every value-level export: the value sits in the top-left cell of the merge and the covered cells arrive empty." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Which cells count as the sheet is worked out from the cells the file actually contains rather than from the used range recorded in it, because that record is unreliable in both directions \u2014 some exporters write a placeholder that would have a full sheet read as a single cell, and a sheet that has had rows or columns deleted keeps a range far larger than anything left in it. Tabs the workbook has hidden are listed and marked but not ticked; hidden rows and columns inside a visible sheet are part of its value grid and are printed." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "How the page is laid out" }), e.jsxs("ul", { style: { lineHeight: "1.7", color: "var(--text-secondary)", marginBottom: "1rem", paddingLeft: "1.25rem" }, children: [e.jsxs("li", { children: [e.jsx("strong", { children: "One sheet per section." }), " Each selected sheet starts on a new page. Unless you untick it, the sheet name is printed above the table as a heading, with its row and column count underneath."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Header rows repeat." }), " When the first row is treated as a header it is redrawn at the top of every page a long table continues onto, so a fifty-page export stays readable."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Cells wrap, they do not clip." }), " A long value breaks onto extra lines inside its own column and the row grows taller. Nothing is silently cut off, except cells over 500 characters, which get an ellipsis."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "The empty edges go." }), " Blank rows above and below the data, and columns that are blank from top to bottom on either side of it, are dropped before layout \u2014 a used range is often far larger than the data in it. Blanks that sit ", e.jsx("em", { children: "between" }), " rows or columns of data are kept and printed, so the row count you see matches the table; a run of more than two consecutive blanks is shortened to two, because one stray value ten thousand rows below the table would otherwise become hundreds of pages of empty rulings, and one far to the right would squeeze the real columns into hairlines."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Orientation can pick itself." }), " On Auto, any selection containing a sheet wider than six columns switches the whole document to landscape."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Page numbers." }), " A centred ", e.jsx("em", { children: "n / total" }), " footer is added to every page after layout finishes."] })] }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "The character-set limit, stated plainly" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "No font file is embedded in the output. Text is drawn with Helvetica, one of the fourteen fonts every PDF reader already has, and those fonts are encoded with Windows-1252. Latin script and its accents, curly quotes, en and em dashes, the euro and pound signs and the trademark symbol all work. Cyrillic, Greek, Hebrew, Arabic, CJK, Indic scripts, emoji and most mathematical or dingbat symbols do not, and each such character is swapped for a question mark with the total reported after conversion rather than being allowed to corrupt the surrounding text. If your data is in one of those scripts, print to PDF from Excel or LibreOffice, which can embed the font it needs." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "When another tool is the right one" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: ["Use the spreadsheet application's own print-to-PDF when the design of the sheet is the deliverable \u2014 an invoice, a formatted report, anything with charts. Use ", e.jsx(F, { to: "/excel-to-csv", style: A, children: "Excel to CSV" }), " when the destination is another system rather than a reader. Use ", e.jsx(F, { to: "/csv-to-excel", style: A, children: "CSV to Excel" }), " to go the other way. To pull a table back out of a PDF, ", e.jsx(F, { to: "/pdf-to-excel", style: A, children: "PDF to Excel" }), " reconstructs rows from text coordinates. Once you have the PDF, ", e.jsx(F, { to: "/merge-pdf", style: A, children: "Merge PDF" }), " will bind it to a cover letter, ", e.jsx(F, { to: "/add-page-numbers-pdf", style: A, children: "Add Page Numbers to PDF" }), " will renumber a combined document, and ", e.jsx(F, { to: "/protect-pdf", style: A, children: "Protect PDF" }), " will put a password on it before it goes out."] })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: qe.map((o, c) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: o.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: o.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: o.desc })] }, c)) })] })] }) });
};
export {
  rt as default
};
