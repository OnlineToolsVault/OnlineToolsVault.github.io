import { r as h, j as e, X as j, L as k } from "./index-OUpguYFg.js";
import { R as F } from "./RelatedTools-dQ1AUZ0r.js";
import { T as P } from "./ToolLayout-CuKFTkh4.js";
import { u as D } from "./index-CBYUSgtG.js";
import { P as m } from "./PDFButton-DYmqjJK7.js";
import { U as z } from "./upload-Dhp0AOOy.js";
import { a as B, L as T } from "./tools-B3OPepIK.js";
import { A as I, a as C } from "./arrow-up-Dwd1c4Q0.js";
import { D as M } from "./download-DqlBxbZM.js";
import { Z as R } from "./zap-C2842ZXr.js";
import { S } from "./shield-check-DCjAjSWE.js";
import "./UPNG-CjUEgNm-.js";
import "./shield-BrCBnKXk.js";
const A = [{ title: "Pages copied, not re-rendered", desc: "Each page object is copied into the new document with its content stream intact, so fonts stay embedded, vectors stay vector, and images keep their original resolution. Nothing is rasterised or re-compressed along the way.", icon: e.jsx(T, { color: "var(--primary)", size: 24 }) }, { title: "Merge order you control", desc: "Files are appended top to bottom exactly as the list shows them. Use the up and down arrows on any row to move a file, or the remove button to drop one, before you press Merge.", icon: e.jsx(R, { color: "var(--primary)", size: 24 }) }, { title: "No upload step at all", desc: "Assembly is done by the pdf-lib library running in this tab. Your PDFs are read with the File API and never sent over the network \u2014 after the page has loaded you can switch off Wi-Fi and it still works.", icon: e.jsx(S, { color: "var(--primary)", size: 24 }) }], W = [{ question: "What order are the files combined in?", answer: "Top to bottom, in the order the list shows them. Dropping several files at once adds them in the order your operating system hands them over, which is not always alphabetical \u2014 check the numbers on the left and use the arrow buttons to fix the sequence before merging." }, { question: "Do bookmarks and fillable form fields survive the merge?", answer: "Page content does; document-level structures do not. Bookmarks (the PDF outline) live on the document catalogue rather than on any page, so they are dropped. Form fields are a mixed case: the widget stays visible on the page because it is a page annotation, but it is no longer registered in the merged file's form dictionary, so it stops being fillable. Fill the form in first, or flatten it with **Flatten PDF**, then merge." }, { question: "How many files can I merge, and how large can they be?", answer: "There is no limit written into the tool. The real ceiling is browser memory: every source file is decoded into an in-memory object graph and the finished document is held there too, so peak usage runs to a few times the combined input size. A dozen ordinary reports is unremarkable; several 200 MB scans at once may make the tab stall or crash." }, { question: "Why does a password-protected PDF fail?", answer: "The merge engine refuses to parse encrypted documents, so the whole operation stops with an error. Run the file through **Unlock PDF** first (you need the password), then merge the unlocked copy." }, { question: "Can I merge images, Word files or Excel sheets into the PDF?", answer: "Not directly \u2014 the file picker only accepts application/pdf. Convert first, then merge: **JPG to PDF** or **Image to PDF** for pictures, and **Word to PDF** for .docx files. Each converter hands you a normal PDF that this tool will accept." }, { question: "Can I take only some pages from each file?", answer: "No. Merging always takes whole documents. Extract the ranges you want with **Split PDF** first and merge the resulting pieces, or merge everything and then delete the surplus pages in **Organize PDF**." }, { question: "Will the merged file be smaller than the originals added together?", answer: "Usually slightly larger. Identical resources are not de-duplicated across documents, so if the same logo font appears in five source files it is embedded five times. If size matters, run the result through **Compress PDF**, which strips metadata and repacks the object structure." }, { question: "What is the merged file called and where does it go?", answer: "It is generated as a Blob in the page and handed to the browser as merged-document.pdf, so it lands in whatever folder your browser uses for downloads. Rename it afterwards if you need something more descriptive." }], Q = () => {
  const [s, n] = h.useState([]), [i, l] = h.useState(false), p = (t) => {
    const r = t.filter((o) => o.type === "application/pdf");
    n((o) => [...o, ...r]);
  }, { getRootProps: g, getInputProps: f, isDragActive: u } = D({ onDrop: p, accept: { "application/pdf": [".pdf"] } }), y = (t) => {
    n((r) => r.filter((o, a) => a !== t));
  }, d = (t, r) => {
    const o = [...s];
    r === "up" && t > 0 ? [o[t], o[t - 1]] = [o[t - 1], o[t]] : r === "down" && t < o.length - 1 && ([o[t], o[t + 1]] = [o[t + 1], o[t]]), n(o);
  }, b = async () => {
    if (!(s.length < 2)) {
      l(true);
      try {
        const t = await m.create();
        for (const w of s) {
          const x = await w.arrayBuffer(), c = await m.load(x);
          (await t.copyPages(c, c.getPageIndices())).forEach((v) => t.addPage(v));
        }
        const r = await t.save(), o = new Blob([r], { type: "application/pdf" }), a = document.createElement("a");
        a.href = URL.createObjectURL(o), a.download = "merged-document.pdf", document.body.appendChild(a), a.click(), document.body.removeChild(a);
      } catch (t) {
        console.error(t), alert("Error merging PDFs");
      } finally {
        l(false);
      }
    }
  };
  return e.jsxs(P, { title: "Merge PDF Files", description: "Combine multiple PDF files into one single document.", seoTitle: "Merge PDF - Combine PDF Files Online for Free", seoDescription: "Merge multiple PDF files into one single document. Fast, free, and secure client-side PDF merger.", faqs: W, children: [e.jsx("div", { className: "tool-workspace", style: { maxWidth: "800px", margin: "0 auto" }, children: e.jsxs("div", { style: { background: "white", border: "1px solid var(--border)", borderRadius: "1rem", padding: "2rem" }, children: [e.jsxs("div", { className: "tool-upload-area", ...g(), style: { border: "2px dashed var(--border)", borderRadius: "0.75rem", padding: "2rem", textAlign: "center", cursor: "pointer", background: u ? "var(--secondary)" : "#f8fafc", marginBottom: "2rem" }, children: [e.jsx("input", { ...f(), "aria-label": "Choose a file for Merge PDF Files" }), e.jsxs("div", { style: { display: "flex", alignItems: "center", justifyContent: "center", gap: "1rem", color: "#64748b" }, children: [e.jsx(z, { size: 24 }), e.jsx("span", { style: { fontWeight: "500" }, children: "Drop PDFs here or click to upload" })] })] }), s.length > 0 && e.jsxs("div", { style: { marginBottom: "2rem" }, children: [e.jsxs("h3", { style: { fontSize: "1.125rem", fontWeight: "600", marginBottom: "1rem" }, children: ["Files to Merge (", s.length, ")"] }), e.jsx("div", { style: { display: "grid", gap: "0.75rem" }, children: s.map((t, r) => e.jsxs("div", { style: { display: "flex", alignItems: "center", gap: "1rem", padding: "0.75rem", border: "1px solid var(--border)", borderRadius: "0.5rem", background: "white" }, children: [e.jsx("div", { style: { width: "24px", textAlign: "center", color: "#94a3b8" }, children: r + 1 }), e.jsx("div", { style: { padding: "0.5rem", background: "#fee2e2", borderRadius: "0.25rem", color: "#dc2626" }, children: e.jsx(B, { size: 20 }) }), e.jsx("div", { style: { flex: 1, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap", fontWeight: "500" }, children: t.name }), e.jsxs("div", { style: { fontSize: "0.875rem", color: "#64748b", marginRight: "1rem" }, children: [(t.size / 1024 / 1024).toFixed(2), " MB"] }), e.jsxs("div", { style: { display: "flex", gap: "0.5rem" }, children: [e.jsx("button", { onClick: () => d(r, "up"), disabled: r === 0, style: { padding: "0.25rem", background: "transparent", border: "none", cursor: r === 0 ? "default" : "pointer", opacity: r === 0 ? 0.3 : 1 }, children: e.jsx(I, { size: 18 }) }), e.jsx("button", { onClick: () => d(r, "down"), disabled: r === s.length - 1, style: { padding: "0.25rem", background: "transparent", border: "none", cursor: r === s.length - 1 ? "default" : "pointer", opacity: r === s.length - 1 ? 0.3 : 1 }, children: e.jsx(C, { size: 18 }) }), e.jsx("button", { onClick: () => y(r), style: { padding: "0.25rem", background: "transparent", border: "none", color: "#ef4444" }, children: e.jsx(j, { size: 18 }) })] })] }, r)) })] }), e.jsx("button", { onClick: b, disabled: s.length < 2 || i, className: "tool-btn-primary", style: { width: "100%", padding: "1rem", background: "var(--primary)", color: "white", border: "none", borderRadius: "0.5rem", fontWeight: "600", display: "flex", alignItems: "center", justifyContent: "center", gap: "0.5rem", opacity: s.length < 2 || i ? 0.5 : 1 }, children: i ? e.jsxs(e.Fragment, { children: [e.jsx(k, { className: "spin", size: 20 }), " Processing...", e.jsx("style", { children: "@keyframes spin { 100% { transform: rotate(360deg); } }" })] }) : e.jsxs(e.Fragment, { children: [e.jsx(M, { size: 20 }), " Merge PDF"] }) })] }) }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(F, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About Merge PDF" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "This tool concatenates two or more PDF documents into one. Drop the files in, put them in the order you want with the arrow buttons, and press Merge; the browser builds a new document and downloads it as merged-document.pdf. The Merge button stays disabled until at least two files are in the list, because merging one file would just hand you back a copy." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "How the merge actually works" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "A PDF is not a stream of pages \u2014 it is a graph of numbered objects, and a page is one object that points at fonts, images, and a content stream. Merging here creates an empty document and then deep-copies each source page object, following every reference it depends on and renumbering it in the new file. Because the content stream is copied rather than replayed, text stays selectable, vector artwork stays sharp at any zoom, and a 600 DPI scan is still 600 DPI afterwards. This is the difference between merging and printing to PDF: printing flattens everything to a fresh rendering, merging moves the originals." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "What travels with the pages, and what does not" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Anything attached to a page comes along: embedded fonts, images, link and comment annotations, and the page size and rotation of each original \u2014 so merging an A4 report with a US Letter appendix produces one file whose pages are genuinely different sizes, which is normal and prints correctly." }), e.jsxs("ul", { style: { lineHeight: "1.7", color: "var(--text-secondary)", marginBottom: "1rem", paddingLeft: "1.25rem" }, children: [e.jsxs("li", { children: [e.jsx("strong", { children: "Dropped:" }), " bookmarks and the outline panel, because they hang off the document catalogue, not off pages."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Dropped:" }), " registration of interactive form fields. The field still draws on the page but no longer accepts input, so fill or flatten forms before merging."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Dropped:" }), " document-level JavaScript, attachments, and the original metadata \u2014 the merged file starts with a clean Info dictionary."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Kept:" }), " page content, page dimensions, page rotation, embedded fonts, image resolution, and per-page annotations."] })] }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "When something goes wrong" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Two failures account for nearly everything. The first is encryption: a password-protected file cannot be parsed and the merge aborts, so unlock it first. The second is memory \u2014 the whole job happens inside one browser tab, and merging a stack of large scans can exhaust it. If a big merge stalls, combine the files in batches of three or four and then merge the batches, which keeps peak memory to a fraction of doing it in one pass. A file that other readers already struggle with (a truncated download, a partially-written export) will also fail here, and re-exporting it from its source application is the only real fix." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "Why it runs in your browser" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: "Merging is usually the last step before a document goes to a client, a court, or a lender, which makes it a poor moment to upload signed contracts and payroll statements to somebody else's server. Everything here happens in the tab: the files are read through the browser File API, assembled by JavaScript, and written back out as a Blob that the download bar picks up. No request carries your document, there is nothing to delete afterwards, and the tool keeps working with the network switched off once the page has loaded." })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: A.map((t, r) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: t.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: t.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: t.desc })] }, r)) })] })] });
};
export {
  Q as default
};
