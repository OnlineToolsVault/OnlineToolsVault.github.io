import { r as p, j as e, d as B, X as N } from "./index-BtmU1OS0.js";
import { R } from "./RelatedTools-GVPazTWJ.js";
import { T } from "./ToolLayout-CgcEif7J.js";
import { u as S } from "./index-BhP_zCBa.js";
import { i as W } from "./browser-image-compression-BVJ4p9S0.js";
import { J as E } from "./jszip.min-C_BKxssD.js";
import { F as G } from "./FileSaver.min-2_N9Q3K6.js";
import { S as x } from "./settings-B3PyRGCV.js";
import { Z as g } from "./zap-DZ5cj3rv.js";
import { I as A, A as u } from "./tools-DOXC7sEs.js";
import "./_commonjs-dynamic-modules-TDtrdbi3.js";
import "./shield-C_IpXjfc.js";
const D = () => new URL("/vendor/browser-image-compression.js", window.location.href).href, F = [{ title: "Resolution is left alone", desc: "Every image keeps its original pixel dimensions. Only the encoder quality is lowered, which is what you want for product shots, print sources and anything that will be cropped later.", icon: e.jsx(x, { color: "var(--primary)", size: 24 }) }, { title: "One shared quality setting", desc: "Pick a single quality target and it is applied to the whole batch, so a set of gallery images ends up visually consistent rather than each one tuned by hand.", icon: e.jsx(g, { color: "var(--primary)", size: 24 }) }, { title: "Per-file before and after", desc: "Each row shows the original size next to the compressed size, so a file that barely shrank is obvious and you can drop it from the batch instead of shipping it.", icon: e.jsx(g, { color: "var(--primary)", size: 24 }) }, { title: "ZIP with collision-safe names", desc: "Everything downloads as one archive. Two photos called IMG_0001.jpg from different folders become separate entries rather than one silently overwriting the other.", icon: e.jsx(u, { color: "var(--primary)", size: 24 }) }, { title: "Nothing about the batch is uploaded", desc: "Every file is decoded and re-encoded by your own CPU in a Web Worker. No image, filename or size is ever sent anywhere, which matters when the batch is client work. Even the worker\u2019s own script is served from this site, not a third-party CDN.", icon: e.jsx(u, { color: "var(--primary)", size: 24 }) }], M = [{ question: "How is this different from the single Image Compressor?", answer: "Two things. This tool takes a whole selection and returns a ZIP, and \u2014 more importantly \u2014 it keeps every image at its **original pixel dimensions**. The single-file compressor deliberately caps the longest side at 1920 px, which is right for the web but wrong if you are compressing masters, print sources or images that still have to be cropped." }, { question: "How many images can I add?", answer: "There is no built-in cap. The real limit is your device memory, because every file is held in memory along with a preview and its compressed copy. A few hundred phone photos is comfortable on a laptop; on an older phone, work in batches of twenty or thirty." }, { question: "Are the images processed in parallel?", answer: "No \u2014 they are compressed one after another, so the browser tab stays responsive and memory use stays flat instead of spiking. The encoding itself runs in a Web Worker off the main thread. Expect a large batch to take a while, and leave the tab open while it works." }, { question: "I pressed Compress twice. Why did nothing happen the second time?", answer: "Files that are already done at the current quality setting are skipped, so pressing the button again is harmless and instant. Change the quality slider and press it again and the whole batch is redone at the new setting. Files you added mid-run are picked up on the next press." }, { question: "One image failed. Did the batch stop?", answer: "No. A file that cannot be decoded is marked with an error on its own row and the run continues through the rest. You do not have to clear it before downloading: the ZIP is built only from rows that finished, so a failed file is simply left out of the archive." }, { question: "Which formats can I mix in one batch?", answer: "JPEG, PNG and WebP, in any combination. Each file keeps its own format through the process, so a batch of mixed screenshots and photos comes back as the same mixture. HEIC photos from an iPhone cannot be decoded by the browser and should go through the HEIC to JPG converter first." }, { question: "How does the quality slider affect PNGs in the batch?", answer: "Differently from JPEG. PNG has no quality parameter, so the encoder converts the slider into a **colour count** and quantises each PNG to that many colours \u2014 roughly 4,000 at 100% and around 400 at 10%. That makes PNG output here lossy. Screenshots, logos and flat graphics take it well and can shrink a lot; a photograph saved as PNG will start to band at low settings, and is better converted to JPEG or WebP with the Image Converter first." }, { question: "Do the filenames change?", answer: "Each entry in the ZIP is your original filename with a compressed- prefix. If two files in the batch share a name, the second gets a numbered suffix so nothing is lost. Extensions are unchanged because the format is unchanged." }, { question: "Is anything uploaded, even filenames?", answer: "No. The files are read locally, compressed locally and zipped locally by JSZip in the same tab. No image, filename or byte count is transmitted. The one request you will see in the Network tab is the compression library loading its Web Worker script on the first run \u2014 that is code, not your batch, and it comes from this site rather than the public CDN it used to. Go offline and it falls back to compressing on the main thread, so the whole job still completes." }], V = () => {
  const [a, d] = p.useState([]), [h, f] = p.useState(false), [l, v] = p.useState(0.8), j = (t) => {
    (t == null ? void 0 : t.length) > 0 && I(t);
  }, { getRootProps: k, getInputProps: z, isDragActive: y } = S({ onDrop: j, accept: { "image/*": [".png", ".jpg", ".jpeg", ".webp"] }, multiple: true }), I = (t) => {
    const s = t.map((r) => ({ file: r, status: "pending", id: Math.random().toString(36).substr(2, 9), preview: URL.createObjectURL(r) }));
    d((r) => [...r, ...s]);
  }, P = (t) => {
    d((s) => s.filter((r) => r.id !== t));
  }, C = async () => {
    if (a.length === 0) return;
    f(true);
    const t = a.filter((s) => !(s.status === "done" && s.quality === l));
    for (const s of t) try {
      const r = { maxSizeMB: 2, useWebWorker: true, libURL: D(), initialQuality: l, alwaysKeepResolution: true }, i = await W(s.file, r);
      d((o) => o.map((n) => n.id === s.id ? { ...n, compressed: i, sizeBefore: n.file.size, sizeAfter: i.size, status: "done", quality: l } : n));
    } catch (r) {
      console.error(r), d((i) => i.map((o) => o.id === s.id ? { ...o, status: "error" } : o));
    }
    f(false);
  }, q = async () => {
    const t = new E(), s = /* @__PURE__ */ new Set();
    a.forEach((i) => {
      if (i.status === "done" && i.compressed) {
        let o = `compressed-${i.file.name}`;
        if (s.has(o)) {
          const n = o.lastIndexOf("."), b = n > 0 ? o.slice(0, n) : o, w = n > 0 ? o.slice(n) : "";
          let m = 1;
          for (; s.has(`${b} (${m})${w}`); ) m += 1;
          o = `${b} (${m})${w}`;
        }
        s.add(o), t.file(o, i.compressed);
      }
    });
    const r = await t.generateAsync({ type: "blob" });
    G.saveAs(r, "compressed-images.zip");
  }, c = (t) => {
    if (!t) return "0 B";
    const s = 1024, r = ["B", "KB", "MB", "GB"], i = Math.floor(Math.log(t) / Math.log(s));
    return parseFloat((t / Math.pow(s, i)).toFixed(2)) + " " + r[i];
  };
  return e.jsx(T, { title: "Bulk Image Compressor", description: "Compress multiple images (JPG, PNG, WebP) at once.", seoTitle: "Bulk Image Compressor - Optimize Multiple Photos", seoDescription: "Batch compress images online. Reduce file size of multiple PNG, JPG, and WebP files simultaneously with minimal quality loss.", faqs: M, children: e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [e.jsxs("div", { style: { marginBottom: "2rem", padding: "1.5rem", background: "white", borderRadius: "1rem", border: "1px solid var(--border)" }, children: [e.jsxs("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "1.5rem", flexWrap: "wrap", gap: "1rem" }, children: [e.jsxs("div", { children: [e.jsxs("label", { style: { display: "flex", alignItems: "center", gap: "0.5rem", marginBottom: "0.5rem", fontWeight: "bold" }, children: [e.jsx(x, { size: 18 }), " Compression Level: ", Math.round(l * 100), "%"] }), e.jsx("input", { id: "compression-level-slider", type: "range", "aria-label": "Compression level", min: "0.1", max: "1", step: "0.1", value: l, onChange: (t) => v(parseFloat(t.target.value)), style: { width: "200px" } })] }), e.jsx("button", { id: "compress-all-btn", onClick: C, disabled: h || a.length === 0, className: "tool-btn-primary", style: { padding: "0.75rem 1.5rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", cursor: h || a.length === 0 ? "not-allowed" : "pointer", fontWeight: "bold", display: "flex", alignItems: "center", gap: "0.5rem" }, children: h ? e.jsxs(e.Fragment, { children: [e.jsx(B, { className: "animate-spin", size: 20 }), " Compressing..."] }) : e.jsxs(e.Fragment, { children: [e.jsx(g, { size: 20 }), " Compress All"] }) })] }), e.jsxs("div", { id: "bulk-compress-dropzone", ...k(), className: "tool-upload-area", style: { border: "2px dashed var(--border)", borderRadius: "1rem", padding: "3rem 2rem", textAlign: "center", cursor: "pointer", backgroundColor: y ? "var(--bg-secondary)" : "var(--bg-card)", transition: "all 0.2s ease" }, children: [e.jsx("input", { ...z(), "aria-label": "Choose a file for Bulk Image Compressor" }), e.jsx("div", { style: { width: "64px", height: "64px", background: "var(--primary-light)", borderRadius: "50%", display: "flex", alignItems: "center", justifyContent: "center", margin: "0 auto 1rem", color: "var(--primary)" }, children: e.jsx(A, { size: 32 }) }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem", color: "var(--text-primary)" }, children: y ? "Drop images here..." : "Drag & Drop Images" }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: "or click to browse files" })] })] }), a.length > 0 && e.jsxs("div", { style: { display: "grid", gap: "1rem" }, children: [e.jsxs("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center" }, children: [e.jsxs("h3", { children: ["Queued Images (", a.length, ")"] }), a.some((t) => t.status === "done") && e.jsxs("button", { id: "download-all-zip-btn", onClick: q, style: { color: "var(--primary)", fontWeight: "bold", background: "none", border: "none", cursor: "pointer", display: "flex", alignItems: "center", gap: "0.5rem" }, children: [e.jsx(u, { size: 18 }), " Download All (ZIP)"] })] }), a.map((t) => e.jsxs("div", { style: { display: "grid", gridTemplateColumns: "60px 1fr auto auto", alignItems: "center", gap: "1rem", padding: "0.75rem", background: "white", border: "1px solid var(--border)", borderRadius: "0.5rem" }, children: [e.jsx("img", { src: t.preview, alt: "thumb", style: { width: "50px", height: "50px", objectFit: "cover", borderRadius: "0.25rem" } }), e.jsxs("div", { children: [e.jsx("div", { style: { fontWeight: "500" }, children: t.file.name }), e.jsx("div", { style: { fontSize: "0.8rem", color: "#64748b" }, children: t.status === "done" ? e.jsxs("span", { style: { color: "green" }, children: [c(t.sizeBefore), " \u2192 ", c(t.sizeAfter), " (-", Math.round((1 - t.sizeAfter / t.sizeBefore) * 100), "%)"] }) : e.jsx("span", { children: c(t.file.size) }) })] }), e.jsxs("div", { children: [t.status === "pending" && e.jsx("span", { style: { padding: "0.25rem 0.5rem", background: "#f1f5f9", borderRadius: "1rem", fontSize: "0.75rem" }, children: "Pending" }), t.status === "done" && e.jsx("span", { style: { padding: "0.25rem 0.5rem", background: "#dcfce7", color: "#166534", borderRadius: "1rem", fontSize: "0.75rem" }, children: "Done" }), t.status === "error" && e.jsx("span", { style: { padding: "0.25rem 0.5rem", background: "#fee2e2", color: "#b91c1c", borderRadius: "1rem", fontSize: "0.75rem" }, children: "Error" })] }), e.jsx("button", { id: `remove-file-${t.id}`, onClick: () => P(t.id), style: { color: "#94a3b8", background: "none", border: "none", cursor: "pointer" }, children: e.jsx(N, { size: 18 }) })] }, t.id))] }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(R, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About Bulk Image Compressor" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Drop in a whole selection of JPEG, PNG and WebP files, choose one quality setting, and get every image back re-encoded at that setting in a single ZIP. The batch is worked through one file at a time, and each row updates with its own before and after size as it finishes, so you can watch which images are actually paying for themselves." }), e.jsx("h3", { style: { fontSize: "1.15rem", margin: "1.5rem 0 0.75rem" }, children: "Resolution is deliberately preserved" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "This is the one behaviour that separates this tool from the single-file Image Compressor. That tool caps the longest side at 1920 pixels, which is the right default for something headed straight to a web page. This one changes nothing about the pixel grid \u2014 a 6000 x 4000 raw export comes back at 6000 x 4000, just lighter. That matters when the images are masters you will crop later, product photography that needs to survive a zoom control, or anything destined for print. If you want a batch resized as well, run it through the Bulk Image Resizer first and compress the output." }), e.jsx("h3", { style: { fontSize: "1.15rem", margin: "1.5rem 0 0.75rem" }, children: "Working through a large batch" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Files are queued and compressed sequentially rather than all at once. That is a deliberate trade: a parallel run would spike memory and could stall the tab on a laptop, while a serial run keeps the interface usable and the progress honest. You can keep adding files while a run is in progress, and you can delete a row you no longer want \u2014 neither will disturb the images already finished. Pressing the compress button again only picks up what is new or what the quality change invalidated." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "A file the browser cannot decode is marked as an error on its own row and skipped; the rest of the batch carries on. Those rows are simply left out of the ZIP, so a single bad file never costs you the run. Inside the archive each entry is the original filename with a compressed- prefix, and duplicate names are given a numbered suffix instead of overwriting one another, which is easy to hit when several camera folders all contain an IMG_0001.jpg." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: "Everything runs on your own hardware. Decoding, encoding and zipping all happen inside this tab, using a Web Worker for the heavy encoding so the page does not freeze. No file, filename, or byte count is sent anywhere. The compression library does load its worker script on the first run \u2014 a single request for JavaScript, with none of your data in it \u2014 but that script is served from this site, so a batch run makes no cross-origin request whatsoever; and if it cannot be loaded the encoding falls back to the main thread, so the batch still completes offline. For a folder of client photographs or internal screenshots, that is a materially different privacy position from uploading it to someone else\u2019s server." })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: F.map((t, s) => e.jsxs("div", { className: "tool-feature-block", style: { background: "var(--bg-card)", padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: t.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: t.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: t.desc })] }, s)) })] })] }) });
};
export {
  V as default
};
