import { r as d, j as e, L as Me } from "./index-OUpguYFg.js";
import { R as _e } from "./RelatedTools-dQ1AUZ0r.js";
import { T as He } from "./ToolLayout-CuKFTkh4.js";
import { u as Le } from "./index-CBYUSgtG.js";
import { _ as pe, p as $e, a as qe } from "./pdf.worker.min-C2VdGDxB.js";
import { P as Ge, S as Ue, b as Je, s as Ye, c as Xe, T as Ve, e as Ke } from "./PDFButton-DYmqjJK7.js";
import { s as ue } from "./index-BY5VtkO3.js";
import { F as Qe } from "./FileSaver.min-DzHDzKVl.js";
import { h as V, a as Ze, g as et } from "./tools-B3OPepIK.js";
import { S as tt } from "./square-cZfAfc2g.js";
import { D as rt } from "./download-DqlBxbZM.js";
import { C as at } from "./check-CzYxOQpM.js";
import { C as ot } from "./copy-BogRq2Ao.js";
import { A as be } from "./alert-triangle-BqnKTzYa.js";
import { S as nt } from "./shield-BrCBnKXk.js";
import "./UPNG-CjUEgNm-.js";
import "./_commonjs-dynamic-modules-TDtrdbi3.js";
qe.workerSrc = $e;
const st = { "\u2018": "'", "\u2019": "'", "\u201A": ",", "\u201B": "'", "\u201C": '"', "\u201D": '"', "\u201E": '"', "\u2010": "-", "\u2011": "-", "\u2012": "-", "\u2013": "-", "\u2014": "-", "\u2015": "-", "\u2026": "...", "\u2022": "-", "\u2032": "'", "\u2033": '"', "\xA0": " ", "\u2002": " ", "\u2003": " ", "\u2007": " ", "\u2009": " ", "\u200A": " ", "\u202F": " ", "\u200B": "", "\uFEFF": "" }, it = new Set("\u20AC\u0192\u2020\u2021\u02C6\u2030\u0160\u2039\u0152\u017D\u02DC\u2122\u0161\u203A\u0153\u017E\u0178");
function lt(t) {
  let r = "";
  for (const o of String(t || "")) {
    const l = st[o];
    if (l !== void 0) {
      r += l;
      continue;
    }
    if (it.has(o)) {
      r += o;
      continue;
    }
    const n = o.codePointAt(0);
    (n >= 32 && n <= 126 || n >= 161 && n <= 255) && (r += o);
  }
  return r;
}
function dt(t, r) {
  const o = r, l = [];
  for (const n of t || []) for (const g of (n == null ? void 0 : n.paragraphs) || []) for (const R of (g == null ? void 0 : g.lines) || []) for (const h of (R == null ? void 0 : R.words) || []) {
    if (!h || !h.bbox) continue;
    const u = typeof h.confidence == "number" ? h.confidence : 0;
    if (u < o) continue;
    const f = lt(h.text).trim();
    f && l.push({ text: f, bbox: h.bbox, confidence: u });
  }
  return l;
}
const re = { top: 0.72, bottom: 0 }, ht = [["\xC0\xC1\xC2\xC3\xC4\xC5\xC8\xC9\xCA\xCB\xCC\xCD\xCE\xCF\xD1\xD2\xD3\xD4\xD5\xD6\xD9\xDA\xDB\xDC\xDD\u0160\u017D\u0178", 0.91, -0.01], ["\xE5\xF0", 0.79, -0.02], ["$", 0.78, -0.12], ["\xC7", 0.74, -0.23], ["\u0192", 0.74, -0.21], ["Q", 0.74, -0.06], ["@CGOS\xD8\u20AC", 0.74, -0.02], ["\xAA\xBA", 0.74, 0.4], ["\xFD", 0.73, -0.21], ["(){}", 0.73, -0.2], ["[]", 0.72, -0.2], ["j\xA7", 0.72, -0.21], ["\xB6", 0.72, -0.18], ["\u2020\u2021", 0.72, -0.16], ["\xE0\xE1\xE2\xE3\xE4\xE8\xE9\xEA\xEB\xEC\xED\xEE\xEF\xF2\xF3\xF4\xF5\xF6\xF9\xFA\xFB\xFC\xDF\u0152\u0161&JUbd\xA3", 0.72, -0.02], ["^", 0.72, 0.3], ["\u2122", 0.72, 0.36], [`"'*`, 0.72, 0.43], ["`\xB4\u02C6\u02DC\xA8\xAF", 0.72, 0.6], ["\xFE\xFF", 0.71, -0.21], ["0123456789%\xBC\xBD\xBE\xA5", 0.7, -0.01], ["\xB9\xB2\xB3", 0.7, 0.27], ["\xB0", 0.7, 0.4], ["t", 0.67, -0.01], ["\xA2", 0.62, -0.12], ["\xA4", 0.58, 0.11], ["\xE7", 0.54, -0.23], ["g", 0.54, -0.22], ["py", 0.54, -0.21], ["q", 0.53, -0.21], ["aceos\xE6\xF8\u0153umnrz", 0.54, -0.01], ["\xBF", 0.53, -0.2], ["\xB5", 0.52, -0.21], ["\xA1", 0.52, -0.19], [";", 0.52, -0.15], [":vwx<>", 0.52, 0], ["+\xB1", 0.51, 0], ["\xD7\xF7", 0.5, 0.01], ["\xAB\xBB\u2039\u203A", 0.44, 0.1], ["=\xAC", 0.4, 0.11], ["\xB7", 0.4, 0.28], ["~", 0.35, 0.16], ["-", 0.32, 0.23], [",", 0.11, -0.15], [".", 0.11, 0], ["\xB8", 0, -0.23], ["_", -0.08, -0.12]], xe = /* @__PURE__ */ new Map();
for (const [t, r, o] of ht) for (const l of t) xe.set(l, { top: r, bottom: o });
function ct(t) {
  let r = null, o = null;
  for (const l of String(t || "")) {
    if (l === " ") continue;
    const n = xe.get(l) || re;
    r = r === null ? n.top : Math.max(r, n.top), o = o === null ? n.bottom : Math.min(o, n.bottom);
  }
  return r === null && (r = re.top, o = re.bottom), { top: r, bottom: o, height: Math.max(r - o, 0.08) };
}
function gt(t, r, o, l, n) {
  const g = t.x0 / o, R = t.x1 / o, h = t.y0 / o, u = t.y1 / o, f = Math.max(R - g, 0.01), a = Math.max(u - h, 0.01), S = ct(n), w = Math.min(400, Math.max(0.5, a / S.height)), D = r > 0 ? r * w : 0, I = D > 0 ? Math.min(400, Math.max(25, f / D * 100)) : 100;
  return { x: g, y: l - u - S.bottom * w, size: w, squeeze: I };
}
const me = 1e8, fe = 16384;
function pt(t) {
  const r = atob(t.slice(t.indexOf(",") + 1)), o = new Uint8Array(r.length);
  for (let l = 0; l < r.length; l += 1) o[l] = r.charCodeAt(l);
  return o;
}
function ut(t, r, o) {
  const l = Math.max(Number(t) || 1, 1), n = Math.max(Number(r) || 1, 1);
  let g = Math.min(o, fe / l, fe / n);
  return l * n * g * g > me && (g = Math.sqrt(me / (l * n))), Math.min(o, g);
}
function mt(t) {
  if (t == null ? void 0 : t.friendly) return t.message;
  const r = typeof t == "string" ? t : (t == null ? void 0 : t.message) || "";
  return (typeof t == "object" && t && t.name || "") === "PasswordException" || /password/i.test(r) ? "This PDF is password-protected, so its pages cannot be read. Remove the password with Unlock PDF first, then bring the file back here." : /empty/i.test(r) ? "That file is zero bytes \u2014 there is nothing in it to read. Re-export or re-download the PDF and try again." : /invalid pdf|structure|corrupt|xref|unexpected end of file|missing pdf header/i.test(r) ? "This file could not be read as a PDF. It looks truncated or damaged \u2014 try re-downloading it, or open it in a reader first to confirm it still works." : /memory|allocation|array buffer|json input|too large|call stack/i.test(r) ? "The browser ran out of memory part way through. Switch to Standard 2x, close other heavy tabs, or cut the file into parts with Split PDF and OCR them separately." : r ? `Could not finish this PDF: ${r}` : "Could not process this PDF. It may be damaged, or too large for this browser to render.";
}
const _ = { standard: { scale: 2, label: "Standard \u2014 2x (144 DPI)" }, fine: { scale: 3, label: "Fine \u2014 3x (216 DPI, slower)" } }, ye = 30, ft = 0.82, we = "OcrHiddenText", yt = 75, wt = 5, bt = 3, xt = 50, vt = "Could not load the OCR engine. It is several megabytes of WebAssembly and English training data served from this site \u2014 check your connection, or any extension or proxy blocking the download, and try again.", ae = (t) => Object.assign(new Error(t), { friendly: true }), A = ({ id: t, children: r }) => e.jsxs("p", { id: t, style: { marginTop: "1rem", padding: "0.85rem 1rem", background: "#fffbeb", color: "#92400e", border: "1px solid #fde68a", borderRadius: "0.5rem", fontSize: "0.9rem", display: "flex", gap: "0.5rem", alignItems: "flex-start" }, children: [e.jsx(be, { size: 18, style: { flexShrink: 0, marginTop: "2px" } }), e.jsx("span", { children: r })] }), jt = [{ title: "A real invisible text layer", desc: "Every recognised word is drawn in invisible text at the left edge and baseline of the box Tesseract reported, sized from the box height and horizontally scaled so it measures the same width as that box. Search, select and copy land on the whole word; nothing changes visually.", icon: e.jsx(V, { color: "var(--primary)", size: 24 }) }, { title: "Page image plus text, not text alone", desc: "Each page is rendered at 144 or 216 DPI, embedded as a JPEG and given its text layer, at the original page size in points. Unusually large pages are rendered lower so the browser can still rasterise them, and the result panel says when that happened.", icon: e.jsx(Ze, { color: "var(--primary)", size: 24 }) }, { title: "Per-page progress, English LSTM engine", desc: "A bar tracks recognition page by page so a fifty-page scan is never a blank screen, and Stop ends a run you did not mean to start. Recognition runs on the neural line recogniser with the English model served from this site, not a CDN.", icon: e.jsx(et, { color: "var(--primary)", size: 24 }) }, { title: "It tells you when it is the wrong tool", desc: "A file that already has real text is spotted when you drop it in, before you spend a minute rasterising a document that did not need it. A page the engine could not actually read \u2014 nearly always one lying on its side \u2014 is called out by its confidence score instead of quietly filling the layer with nonsense. And a run that found nothing, or almost nothing, says so rather than showing you a green tick over an unsearchable file.", icon: e.jsx(be, { color: "var(--primary)", size: 24 }) }, { title: "Nothing leaves the tab", desc: "Rendering, recognition and PDF assembly all happen in your browser. A scanned contract, medical letter or bank statement is never uploaded, queued or stored anywhere.", icon: e.jsx(nt, { color: "var(--primary)", size: 24 }) }], kt = [{ question: "What does the output PDF actually contain?", answer: "Two layers per page. The visible layer is a JPEG of the page rendered at your chosen resolution. Behind it sits every recognised word, drawn as real text in the position the engine reported but painted with nothing \u2014 PDF text rendering mode 3, plus a zero-alpha graphics state. That is the standard construction for a searchable scan: it looks identical to the original, and search, text selection and copy-paste all work." }, { question: "Will the file get bigger?", answer: "Usually yes, sometimes dramatically. The original pages are replaced by JPEG images at 144 or 216 DPI, so a small vector PDF can grow several times over, while a scan that was already a bundle of images may stay similar or even shrink. If size matters, use the Standard 2x setting and run the result through **Compress PDF** afterwards." }, { question: "Which languages does it recognise?", answer: "**English only.** The English trained data is the single language model bundled with this site so that nothing has to be fetched from a third-party CDN. Pointing it at another script does not raise an error, but it does not produce a usable layer either: the invisible text can only hold characters in the PDF WinAnsi set (ASCII, Latin-1 and the usual typographic extras such as \u20AC and \u2122), so Chinese, Japanese, Greek, Cyrillic, Arabic or Devanagari text ends up as a few stray Latin characters or nothing at all. Whatever the engine thought it saw is still shown in full in the recognised-text panel. Accented Latin text in French, German or Spanish often comes out mostly right, but the model is not trained for it." }, { question: "How accurate is it?", answer: "On a clean, flat, well-lit 300 DPI scan of ordinary printed type, very good. On a phone photo of a page shot at an angle in poor light, poor. OCR accuracy is dominated by the input, not by the engine \u2014 sharpness, contrast, straightness and text size in the frame matter far more than any setting. Words the engine scores below 30 out of 100 are left out of the text layer rather than added as noise, and the result panel reports the average score of the words that did go in. Because that average only covers the words that were placed, it stays high even when the engine read almost nothing, so the word count is checked as well: a run that recognised nothing, or fewer than a handful of words per page, is reported as such instead of as a success. Small print is the one case where the resolution setting really moves the needle: 5-point type that reads at 70% confidence at 2x can read at 94% at 3x." }, { question: "My scan is sideways or upside down.", answer: "Turn it the right way up first, with **Rotate PDF**. This tool reads the page exactly as it is drawn and does **not** detect or correct orientation \u2014 there is no page-orientation model in the English-only build. A page lying on its side does not fail: the engine reads the marks column by column and returns confident-looking rubbish, which is worse than a failure because it goes into the text layer. The one signal you get is the confidence figure, and it is a reliable one: an upright page of printed type comes back in the nineties, and even a badly blurred one still scores in the low eighties, while a sideways page lands in the sixties. Below 75% the page says so in an orange warning and tells you to rotate and retry." }, { question: "Does it read handwriting, or reproduce tables?", answer: "No to both. Tesseract is trained on printed type; cursive comes back as confident-looking nonsense. Table structure is not reconstructed either \u2014 the words inside a table land in roughly the right places on the page, but there is no table object, so pulling a grid out of the result is not something this produces. For tabular data from a PDF that already has real text, **PDF to Excel** is the right tool." }, { question: "My PDF already has selectable text. Should I run it through this?", answer: "No, and you do not have to check by hand: the moment you drop a file in, the first three pages are scanned for existing text, and if there is any real amount of it an orange warning appears above the button before you run anything. OCR on such a file would only replace crisp vector text with a picture of it plus a guessed layer \u2014 strictly worse on every axis, and several times the size. Use **PDF to Text** to pull the words out instead. The button is not disabled, because there is one case where re-running is right: a file whose existing text layer is itself a bad OCR job you want to redo. This tool exists for the other case, where a document is nothing but scanned images." }, { question: "Why is it so slow?", answer: "Recognition is a neural model running in WebAssembly on your own CPU, roughly a few seconds per page depending on how dense the page is and which resolution you picked. Nothing is being uploaded to a server farm, which is the trade: privacy and no upload limit in exchange for your laptop doing the work. A long document is best left running in a foreground tab \u2014 background tabs get throttled by the browser. If you started the wrong file, **Stop** ends the run and gives you the controls back." }, { question: "Can I change the resolution after a run?", answer: "Yes. Change the setting and press **Run OCR again** \u2014 the page tells you when the result on screen was produced at a different resolution from the one currently selected, and the download keeps giving you the last result actually produced until you re-run. Nothing is re-uploaded or re-read from disk; your file is still loaded." }, { question: "Can I get the plain text as well?", answer: "Yes. Everything the engine read is shown in the panel under the progress bar with a copy button, so you can paste the raw text straight into a document without opening the PDF at all. If the browser blocks clipboard access the button says so instead of pretending it worked \u2014 select the text and press Ctrl+C or Cmd+C. If the engine read nothing at all, the panel says so and the copy button is greyed out rather than silently copying an empty clipboard." }, { question: "It failed part way through.", answer: "The message on screen says which of these it was. A PDF that needs a password to open cannot be read at all \u2014 run **Unlock PDF** first; a file that merely restricts printing or editing opens here normally. A blocked engine or language download is reported as an engine problem, not as a fault in your PDF. Very large pages are rendered below your chosen resolution automatically so the browser's canvas limit is never hit, and the result panel tells you when that happened; if the tab still runs out of memory on a long document, drop to 2x or cut the file into parts with **Split PDF**. Nothing is ever stuck: **Stop** ends a run in progress and hands the controls straight back." }, { question: "Is any of this uploaded?", answer: "No. The pdf.js renderer, the Tesseract WebAssembly core, the English trained data and the pdf-lib writer are all served from this site and run inside this browser tab. Your document is read with the File API, held in memory, and written straight back to your downloads folder as searchable-yourfile.pdf." }], Ht = () => {
  var _a, _b, _c;
  const [t, r] = d.useState(null), [o, l] = d.useState("standard"), [n, g] = d.useState(false), [R, h] = d.useState(""), [u, f] = d.useState({ done: 0, total: 0, within: 0 }), [a, S] = d.useState(null), [w, D] = d.useState(""), [I, W] = d.useState(""), [oe, H] = d.useState(""), [O, K] = d.useState("idle"), [N, Q] = d.useState(null), C = d.useRef(0), F = d.useRef(0), L = d.useRef(null), $ = d.useRef(null), q = u.total ? Math.min(100, Math.round((u.done + u.within) / u.total * 100)) : 0, B = d.useCallback(() => {
    const s = L.current, i = $.current;
    L.current = null, $.current = null, s && Promise.resolve(s.terminate()).catch(() => {
    }), i && Promise.resolve(i.destroy()).catch(() => {
    });
  }, []);
  d.useEffect(() => () => {
    C.current += 1, F.current += 1, B();
  }, [B]);
  const ve = d.useCallback(async (s, i) => {
    let b = null;
    try {
      const k = await s.arrayBuffer();
      b = await pe({ data: new Uint8Array(k) }).promise;
      const m = Math.min(b.numPages, bt);
      let T = 0;
      for (let x = 1; x <= m; x += 1) {
        if (F.current !== i) return;
        const P = await b.getPage(x), G = await P.getTextContent();
        for (const U of G.items) T += String((U == null ? void 0 : U.str) || "").replace(/\s+/g, "").length;
        P.cleanup();
      }
      if (F.current !== i) return;
      Q(T >= m * xt ? { characters: T, sampled: m, pages: b.numPages } : null);
    } catch {
    } finally {
      if (b) try {
        await b.destroy();
      } catch {
      }
    }
  }, []), je = () => {
    C.current += 1, B(), g(false), h(""), f({ done: 0, total: 0, within: 0 }), H("Recognition stopped. Nothing was written \u2014 your file is still loaded, so you can change the resolution and run it again.");
  }, ke = () => {
    C.current += 1, F.current += 1, B(), g(false), r(null), S(null), D(""), W(""), H(""), h(""), Q(null), f({ done: 0, total: 0, within: 0 });
  }, Pe = (s, i) => {
    var _a2, _b2;
    if ((s == null ? void 0 : s.length) > 0) {
      C.current += 1, F.current += 1, B(), g(false), r(s[0]), S(null), D(""), W(""), H(""), Q(null), ve(s[0], F.current);
      return;
    }
    if ((i == null ? void 0 : i.length) > 0) {
      const b = i.length > 1 || i.some((k) => {
        var _a3;
        return (_a3 = k.errors) == null ? void 0 : _a3.some((m) => m.code === "too-many-files");
      });
      W(b ? "One file at a time \u2014 drop a single scanned PDF." : `"${((_b2 = (_a2 = i[0]) == null ? void 0 : _a2.file) == null ? void 0 : _b2.name) || "That file"}" is not a PDF, so it was not loaded. This tool needs a scanned PDF; for a single photo or screenshot use Image to Text, and for a pile of images use Image to PDF first.`);
    }
  }, { getRootProps: Te, getInputProps: Se, isDragActive: De } = Le({ onDrop: Pe, accept: { "application/pdf": [".pdf"] }, multiple: false }), Ce = async () => {
    var _a2;
    if (!t || n) return;
    const s = C.current + 1;
    C.current = s;
    const i = () => C.current === s;
    g(true), W(""), H(""), S(null), D(""), h("Loading the OCR engine\u2026"), f({ done: 0, total: 0, within: 0 });
    const b = ((_a2 = _[o]) == null ? void 0 : _a2.scale) || 2;
    let k = null, m = null;
    try {
      try {
        let p;
        const v = new Promise((c, y) => {
          p = y;
        });
        v.catch(() => {
        }), k = await Promise.race([ue.createWorker("eng", ue.OEM.LSTM_ONLY, { workerPath: "/tesseract/worker.min.js", corePath: "/tesseract", langPath: "/tesseract/lang", logger: (c) => {
          i() && c.status === "recognizing text" && f((y) => ({ ...y, within: c.progress }));
        }, errorHandler: (c) => p(c) }), v]);
      } catch (p) {
        throw console.error(p), ae(vt);
      }
      if (!i()) return;
      L.current = k;
      const T = await t.arrayBuffer();
      if (m = await pe({ data: new Uint8Array(T) }).promise, !i()) {
        await m.destroy().catch(() => {
        });
        return;
      }
      $.current = m;
      const x = m.numPages;
      f({ done: 0, total: x, within: 0 });
      const P = await Ge.create(), G = await P.embedFont(Ue.Helvetica), U = /* @__PURE__ */ new Map(), Ae = (p) => {
        let v = 0;
        for (const c of p) {
          let y = U.get(c);
          y === void 0 && (y = G.widthOfTextAtSize(c, 1), U.set(c, y)), v += y;
        }
        return v;
      }, ne = [];
      let J = 0, se = 0, ie = 0;
      for (let p = 1; p <= x; p += 1) {
        if (!i()) return;
        h(`Reading page ${p} of ${x}\u2026`), f({ done: p - 1, total: x, within: 0 });
        const v = await m.getPage(p), c = v.getViewport({ scale: 1 }), y = ut(c.width, c.height, b);
        y < b && (ie += 1);
        const ee = v.getViewport({ scale: y }), j = document.createElement("canvas");
        j.width = Math.max(1, Math.floor(ee.width)), j.height = Math.max(1, Math.floor(ee.height));
        const Y = j.getContext("2d");
        if (!Y) throw ae(`The browser would not give this page a drawing surface (page ${p} is ${Math.round(c.width)} x ${Math.round(c.height)} points). Close other tabs and try again at Standard 2x.`);
        if (Y.fillStyle = "#ffffff", Y.fillRect(0, 0, j.width, j.height), await v.render({ canvasContext: Y, viewport: ee }).promise, !i()) return;
        const de = j.toDataURL("image/jpeg", ft);
        if (!de.startsWith("data:image/jpeg")) throw ae(`Page ${p} is too large for this browser to turn into an image (${j.width} x ${j.height} pixels). Try Standard 2x, or pull the page out with Split PDF and OCR it on its own.`);
        const he = pt(de), { data: ce } = await k.recognize(he, {}, { text: true, blocks: true });
        if (!i()) return;
        const ge = c.width, te = c.height, z = P.addPage([ge, te]), We = await P.embedJpg(he);
        z.drawImage(We, { x: 0, y: 0, width: ge, height: te }), z.setFont(G);
        const Oe = P.context.register(P.context.obj({ Type: "ExtGState", ca: 0, CA: 0 }));
        z.node.setExtGState(Je.of(we), Oe), z.pushOperators(Ye(we), Xe(Ve.Invisible));
        const Ne = dt(ce == null ? void 0 : ce.blocks, ye);
        for (const M of Ne) {
          const Be = Ae(M.text), X = gt(M.bbox, Be, y, te, M.text);
          z.pushOperators(Ke(Math.round(X.squeeze * 100) / 100)), z.drawText(M.text, { x: X.x, y: X.y, size: X.size }), J += 1, se += M.confidence;
        }
        ne.push(((ce == null ? void 0 : ce.text) || "").trim()), j.width = 0, j.height = 0, v.cleanup(), f({ done: p, total: x, within: 0 });
      }
      if (!i()) return;
      h("Writing the searchable PDF\u2026");
      const Ie = await P.save(), le = new Blob([Ie], { type: "application/pdf" });
      if (!i()) return;
      S({ blob: le, pages: x, words: J, averageConfidence: J ? Math.round(se / J) : 0, size: le.size, sourceSize: t.size, preset: o, downscaledPages: ie }), D(ne.join(`

`)), h("");
    } catch (T) {
      if (!i()) return;
      console.error(T), W(mt(T)), h("");
    } finally {
      if (k) try {
        await k.terminate();
      } catch {
      }
      if (m) try {
        await m.destroy();
      } catch {
      }
      i() && (L.current = null, $.current = null, g(false));
    }
  }, Re = () => {
    var _a2;
    if (!a) return;
    const s = ((_a2 = t == null ? void 0 : t.name) == null ? void 0 : _a2.replace(/\.pdf$/i, "")) || "document";
    Qe.saveAs(a.blob, `searchable-${s}.pdf`);
  }, Fe = async () => {
    var _a2;
    if (w) {
      try {
        if (!((_a2 = navigator.clipboard) == null ? void 0 : _a2.writeText)) throw new Error("Clipboard API unavailable");
        await navigator.clipboard.writeText(w), K("copied");
      } catch {
        K("failed");
      }
      setTimeout(() => K("idle"), 2500);
    }
  }, Z = (s) => s ? s < 1024 * 1024 ? `${(s / 1024).toFixed(0)} KB` : `${(s / 1024 / 1024).toFixed(2)} MB` : "0 KB", Ee = !!a && a.preset !== o, E = !!a && a.words === 0, ze = !!a && a.words > 0 && a.words < a.pages * wt;
  return e.jsx(He, { title: "OCR PDF", description: "Turn a scanned PDF into one you can search, select and copy from.", seoTitle: "OCR PDF Online - Make a Scanned PDF Searchable", seoDescription: "Add an invisible text layer to a scanned PDF in your browser. English OCR with Tesseract, page images preserved, nothing uploaded.", faqs: kt, children: e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [e.jsxs("div", { style: { background: "white", border: "1px solid var(--border)", borderRadius: "1rem", padding: "2rem" }, children: [t ? e.jsxs("div", { children: [e.jsxs("div", { style: { display: "flex", alignItems: "center", gap: "1rem", flexWrap: "wrap", marginBottom: "1.5rem" }, children: [e.jsx("div", { style: { padding: "0.75rem", background: "#e0f2fe", borderRadius: "0.5rem", color: "#0284c7" }, children: e.jsx(V, { size: 24 }) }), e.jsxs("div", { style: { flex: 1, minWidth: "180px" }, children: [e.jsx("h3", { style: { fontSize: "1.05rem", fontWeight: "600", wordBreak: "break-all" }, children: t.name }), e.jsx("p", { style: { fontSize: "0.875rem", color: "#64748b" }, children: Z(t.size) })] }), n && e.jsxs("button", { id: "ocr-pdf-stop-btn", onClick: je, style: { display: "flex", alignItems: "center", gap: "0.4rem", padding: "0.5rem 1rem", borderRadius: "0.5rem", background: "white", border: "1px solid #fecaca", color: "#b91c1c", fontWeight: "600", cursor: "pointer" }, children: [e.jsx(tt, { size: 14 }), " Stop"] }), e.jsx("button", { id: "ocr-pdf-reset-btn", onClick: ke, style: { padding: "0.5rem 1rem", borderRadius: "0.5rem", background: "white", border: "1px solid var(--border)", fontWeight: "600", cursor: "pointer" }, children: "Choose another" })] }), e.jsxs("div", { id: "ocr-pdf-settings", style: { display: "flex", gap: "1rem", alignItems: "center", flexWrap: "wrap", padding: "1rem", background: "#f8fafc", borderRadius: "0.75rem", border: "1px solid var(--border)", marginBottom: "1.5rem" }, children: [e.jsx("label", { htmlFor: "ocr-pdf-resolution", style: { fontWeight: "600", fontSize: "0.9rem" }, children: "Render resolution" }), e.jsx("select", { id: "ocr-pdf-resolution", value: o, onChange: (s) => l(s.target.value), disabled: n, style: { padding: "0.5rem", borderRadius: "0.5rem", border: "1px solid var(--border)", cursor: "pointer" }, children: Object.entries(_).map(([s, i]) => e.jsx("option", { value: s, children: i.label }, s)) }), e.jsx("span", { style: { fontSize: "0.85rem", color: "#64748b" }, children: "Higher resolution reads small print better and produces a larger file." })] }), N && !n && e.jsxs(A, { id: "ocr-pdf-existing-text-warning", children: ["This PDF ", e.jsx("strong", { children: "already contains selectable text" }), " \u2014 ", N.characters.toLocaleString(), " characters on ", N.sampled === 1 ? "its first page" : `its first ${N.sampled} pages`, ". OCR is for documents that are nothing but scanned images: running it here would replace text that is already exact with a picture of the page plus a guessed layer, which is worse on every axis and much larger. Pull the words out with ", e.jsx("strong", { children: "PDF to Text" }), " instead. Carry on only if the text it already has is itself a bad OCR layer you want to redo."] }), e.jsxs("button", { id: "ocr-pdf-run-btn", onClick: Ce, disabled: n, className: "tool-btn-primary", style: { width: "100%", padding: "1rem", borderRadius: "0.5rem", marginTop: N && !n ? "1rem" : 0, background: "var(--primary)", color: "white", border: "none", cursor: n ? "wait" : "pointer", fontWeight: "bold", display: "flex", justifyContent: "center", alignItems: "center", gap: "0.5rem" }, children: [n ? e.jsx(Me, { size: 20, style: { animation: "spin 1s linear infinite" } }) : e.jsx(V, { size: 20 }), n ? "Recognising\u2026" : a ? "Run OCR again" : "Make this PDF searchable"] }), n && e.jsxs("div", { style: { marginTop: "1.5rem" }, children: [e.jsxs("div", { style: { display: "flex", justifyContent: "space-between", fontSize: "0.9rem", color: "#475569", marginBottom: "0.5rem" }, children: [e.jsx("span", { "aria-live": "polite", children: R || "Working\u2026" }), e.jsxs("span", { style: { fontWeight: "600" }, children: [q, "%"] })] }), e.jsx("div", { role: "progressbar", "aria-label": "OCR progress", "aria-valuemin": 0, "aria-valuemax": 100, "aria-valuenow": q, "aria-valuetext": `${q}% \u2014 ${R || "working"}`, style: { height: "8px", background: "#e2e8f0", borderRadius: "4px", overflow: "hidden" }, children: e.jsx("div", { style: { width: `${q}%`, height: "100%", background: "var(--primary)", transition: "width 0.3s" } }) }), u.total > 0 && e.jsxs("p", { style: { marginTop: "0.5rem", fontSize: "0.85rem", color: "#64748b" }, children: ["Page ", Math.min(u.done + 1, u.total), " of ", u.total] })] }), Ee && !n && e.jsxs(A, { id: "ocr-pdf-stale-warning", children: ["The result below was produced at ", e.jsx("strong", { children: (_a = _[a.preset]) == null ? void 0 : _a.label }), ", not the ", e.jsx("strong", { children: (_b = _[o]) == null ? void 0 : _b.label }), " now selected. Press ", e.jsx("strong", { children: "Run OCR again" }), " to rebuild it; until then the download gives you the earlier version."] }), a && a.downscaledPages > 0 && e.jsxs(A, { id: "ocr-pdf-downscale-warning", children: [a.downscaledPages === 1 ? "One page was" : `${a.downscaledPages} pages were`, " rendered below the chosen resolution because the page is far larger than a normal sheet and the browser cannot rasterise a canvas that big. The text layer is still positioned correctly; recognition of small print on ", a.downscaledPages === 1 ? "that page" : "those pages", " may be weaker."] }), E && !n && e.jsxs(A, { id: "ocr-pdf-empty-warning", children: [e.jsxs("strong", { children: ["No text was recognised on ", a.pages === 1 ? "this page" : "any of these pages"] }), ", so the file you can download is a picture of your document with nothing searchable in it \u2014 and normally a larger file than the one you started with. Nothing was lost: your original is untouched. The usual causes are a page that really is only a photograph, drawing or blank sheet; a scan too dark, blurred or too low-resolution to read; handwriting rather than printed type; or a script this English-only build cannot read. If the page is upright printed English and simply small, try ", e.jsx("strong", { children: "Fine \u2014 3x" }), ". If it is sideways, turn it with ", e.jsx("strong", { children: "Rotate PDF" }), " first. Otherwise rescan it straighter, brighter and larger."] }), ze && !n && e.jsxs(A, { id: "ocr-pdf-thin-warning", children: ["Only ", e.jsx("strong", { children: a.words === 1 ? "one word was" : `${a.words.toLocaleString()} words were` }), " recognised across ", a.pages === 1 ? "this page" : `these ${a.pages} pages`, " \u2014 far less than a page of printed type normally yields, so most of this document did not come out searchable. The confidence figure below is the average of just ", a.words === 1 ? "that one word" : "those few words", ", so a high score there does not mean the pages were read. Read the recognised text below before relying on it: if it is nearly empty, the pages are probably photographs, drawings or handwriting, or the scan is too poor or too small to read \u2014 and rescanning matters far more than any setting here."] }), a && a.words > 0 && a.averageConfidence < yt && e.jsxs(A, { id: "ocr-pdf-low-confidence-warning", children: ["The engine averaged only ", e.jsxs("strong", { children: [a.averageConfidence, "%"] }), " confidence across the ", a.words.toLocaleString(), " words it placed, which usually means it could not really read ", a.pages === 1 ? "the page" : "these pages", ". The commonest cause by far is a page lying on its side or upside down: ", e.jsx("strong", { children: "this tool does not detect or correct orientation" }), ", so turn the pages the right way up with ", e.jsx("strong", { children: "Rotate PDF" }), " and run it again. A very blurred, skewed or low-resolution scan does the same. Read the recognised text below before relying on the layer \u2014 at this score it is likely to be nonsense that makes the document findable under words that are not in it."] })] }) : e.jsxs("div", { id: "ocr-pdf-dropzone", className: "tool-upload-area", ...Te(), style: { border: "2px dashed var(--border)", borderRadius: "0.75rem", padding: "3rem 2rem", textAlign: "center", cursor: "pointer", background: De ? "var(--secondary)" : "#f8fafc", transition: "all 0.2s ease" }, children: [e.jsx("input", { ...Se(), "aria-label": "Choose a file for OCR PDF" }), e.jsx("div", { style: { width: "64px", height: "64px", background: "#e0f2fe", borderRadius: "50%", display: "flex", alignItems: "center", justifyContent: "center", margin: "0 auto 1rem", color: "#0284c7" }, children: e.jsx(V, { size: 32 }) }), e.jsx("h3", { style: { fontSize: "1.25rem", fontWeight: "600", marginBottom: "0.5rem" }, children: "Drag & drop a scanned PDF here" }), e.jsx("p", { style: { color: "#64748b" }, children: "or click to select a file \u2014 English printed text, processed in this tab" })] }), I && e.jsx("p", { role: "alert", style: { marginTop: "1.5rem", padding: "1rem", background: "#fef2f2", color: "#b91c1c", border: "1px solid #fecaca", borderRadius: "0.5rem" }, children: I }), oe && !I && e.jsx("p", { role: "status", style: { marginTop: "1.5rem", padding: "1rem", background: "#f8fafc", color: "#475569", border: "1px solid var(--border)", borderRadius: "0.5rem" }, children: oe }), t && a && e.jsxs("div", { style: { marginTop: "1.5rem" }, children: [e.jsxs("div", { style: { padding: "1.25rem", borderRadius: "0.75rem", marginBottom: "1.5rem", background: E ? "#fffbeb" : "#f0fdf4", border: `1px solid ${E ? "#fde68a" : "#bbf7d0"}` }, children: [e.jsx("h3", { style: { fontSize: "1.05rem", fontWeight: "700", color: E ? "#92400e" : "#166534", marginBottom: "0.75rem" }, children: E ? "Pages rebuilt, but no text was recognised" : "Text layer added" }), e.jsxs("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(140px, 1fr))", gap: "1rem" }, children: [e.jsxs("div", { children: [e.jsx("div", { style: { fontSize: "1.4rem", fontWeight: "700" }, children: a.pages }), e.jsx("div", { style: { fontSize: "0.8rem", color: "#475569" }, children: "pages processed" })] }), e.jsxs("div", { children: [e.jsx("div", { style: { fontSize: "1.4rem", fontWeight: "700" }, children: a.words.toLocaleString() }), e.jsx("div", { style: { fontSize: "0.8rem", color: "#475569" }, children: "words placed" })] }), e.jsxs("div", { children: [e.jsxs("div", { style: { fontSize: "1.4rem", fontWeight: "700" }, children: [a.averageConfidence, "%"] }), e.jsx("div", { style: { fontSize: "0.8rem", color: "#475569" }, children: "mean confidence" })] }), e.jsxs("div", { children: [e.jsx("div", { style: { fontSize: "1.4rem", fontWeight: "700" }, children: Z(a.size) }), e.jsxs("div", { style: { fontSize: "0.8rem", color: "#475569" }, children: ["from ", Z(a.sourceSize)] })] })] }), e.jsxs("p", { style: { marginTop: "0.85rem", fontSize: "0.8rem", color: "#475569" }, children: ["Rendered at ", (_c = _[a.preset]) == null ? void 0 : _c.label, ". ", w ? `Words the engine scored under ${ye} out of 100 are shown in the panel below but left out of the text layer.` : "Every page was rebuilt as an image at that resolution, but the engine returned no readable text at all, so there was nothing to put in the layer."] })] }), e.jsxs("button", { id: "ocr-pdf-download-btn", onClick: Re, className: "tool-btn-primary", style: { width: "100%", padding: "1rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", cursor: "pointer", fontWeight: "bold", display: "flex", justifyContent: "center", alignItems: "center", gap: "0.5rem" }, children: [e.jsx(rt, { size: 20 }), " ", E ? "Download the rebuilt PDF anyway" : "Download searchable PDF"] }), e.jsxs("div", { style: { marginTop: "1.5rem" }, children: [e.jsxs("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "0.5rem", gap: "1rem", flexWrap: "wrap" }, children: [e.jsx("h4", { style: { fontWeight: "600" }, children: e.jsx("label", { htmlFor: "ocr-pdf-text", children: "Recognised text" }) }), e.jsxs("button", { onClick: Fe, disabled: !w, "aria-label": "Copy the recognised text to the clipboard", style: { display: "flex", alignItems: "center", gap: "0.4rem", padding: "0.4rem 0.9rem", borderRadius: "0.5rem", border: "1px solid var(--border)", background: "white", cursor: w ? "pointer" : "not-allowed", opacity: w ? 1 : 0.5, color: O === "failed" ? "#b91c1c" : "inherit" }, children: [O === "copied" ? e.jsx(at, { size: 16 }) : e.jsx(ot, { size: 16 }), O === "copied" ? "Copied" : O === "failed" ? "Copy blocked" : "Copy"] })] }), O === "failed" && e.jsx("p", { role: "alert", style: { fontSize: "0.85rem", color: "#b91c1c", marginBottom: "0.5rem" }, children: "Your browser blocked clipboard access. Select the text below and press Ctrl+C or Cmd+C instead." }), e.jsx("textarea", { id: "ocr-pdf-text", readOnly: true, value: w || "No text was recognised on any page.", style: { width: "100%", minHeight: "220px", padding: "1rem", borderRadius: "0.5rem", border: "1px solid var(--border)", background: "#f8fafc", fontSize: "0.9rem", lineHeight: "1.6", resize: "vertical" } })] })] }), e.jsx("style", { children: "@keyframes spin { 100% { transform: rotate(360deg); } }" })] }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(_e, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About OCR PDF" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "A scanned PDF is a stack of photographs. It looks like a document, prints like a document and is completely opaque to search, because there are no characters in it \u2014 only pixels arranged in the shape of characters. This tool reads those pixels and writes the words back in as real text, so the same-looking document starts answering Ctrl+F. The result downloads as searchable-yourfile.pdf and the original on your disk is untouched." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "How the searchable layer is built" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Each page is rendered to a canvas at two or three times its point size \u2014 144 or 216 DPI \u2014 and handed to Tesseract, which returns not just the text but a bounding box and a confidence score for every individual word. A new PDF page is created at the original page dimensions, the rendered image is embedded as a JPEG covering it, and then each word is drawn on top as Helvetica in text rendering mode 3, the PDF way of saying \u201Clay this text out but paint nothing\u201D, under a zero-alpha graphics state as well." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Placing a word takes a little care, because the box Tesseract reports is the box of the ink, not of the em square. The font size is derived from the box height and the word\u2019s own letters using Helvetica\u2019s metrics \u2014 a word of x-height letters like \u201Cmoon\u201D implies a much larger font than its box is tall, while \u201CMl\u201D reaches the full ascender \u2014 and the drawn word is then squeezed or stretched horizontally with the PDF Tz operator so that it measures exactly as wide as the box, which is the same mechanism Tesseract\u2019s own PDF writer uses. The baseline sits on the bottom edge of the box, and drops by a descender\u2019s depth only for words whose ink actually goes below it \u2014 anything containing a g, j, p, q, y, a comma or a bracket. The result is that a drag-selection covers the whole word and a search highlight lands on the ink rather than two thirds of it." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Words the engine scored below 30 out of 100 are dropped rather than placed. A low-confidence guess is not neutral: it makes the document findable under a word that is not there, which is more damaging in an archive than a small gap. The panel below the progress bar shows the full raw recognition for every page, including the low-confidence words, so nothing is hidden from you \u2014 only from the search index. One more filter applies at the last step: the invisible layer is written in the standard PDF WinAnsi encoding, so a character outside it \u2014 a CJK glyph, a Cyrillic letter \u2014 is dropped from the layer even though it still appears in the panel." }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["A per-word threshold cannot catch the case where the engine is confidently wrong about the whole page, which is what happens when a page is fed in sideways or upside down: every line is read as a column of unrelated marks, and enough of those marks score above 30 to fill the layer with nonsense. So the average score of the words that were placed is reported next to the page count, and if it comes out below 75 the result panel says plainly that the page probably was not readable and points at ", e.jsx("strong", { children: "Rotate PDF" }), ". The separation is clean in practice \u2014 an upright page of printed type scores in the nineties, a badly blurred one still manages the low eighties, and a sideways page lands in the sixties. Orientation is not detected or corrected here; there is no page-orientation model in the English-only build, so that is a warning rather than a fix."] }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "That average has a blind spot of its own, and it is worth knowing about, because it is the one number a hurried reader trusts. It is taken over the words that were placed, so a page of photographs that yielded a single legible caption scores in the nineties on the strength of that one word. A mean confidence therefore says how sure the engine was about what it found, never how much of the page it found. The word count is checked alongside it: a run that placed nothing at all is reported as exactly that rather than as a text layer added, with the download offered but plainly labelled, and a run that placed fewer than five words per page is called out as too thin to have made the document searchable. Both notes say what usually causes it \u2014 a page that really is only a picture, a scan too dark or too small to read, handwriting, or a script this build does not know." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Very large pages get one extra safeguard. Browsers refuse to allocate a canvas beyond a fixed pixel area \u2014 268 megapixels on the desktop, far less on some phones \u2014 and they also cap each single edge at 16,384 pixels, and in both cases they hand back a blank canvas rather than an error, so an E-size plot or a long banner page at 3x would otherwise produce an empty image and recognise nothing. Pages that would cross either limit are rendered at whatever scale fits a 100-megapixel budget and a 16,384-pixel edge instead, and the result panel tells you how many pages that applied to. Ordinary paper sizes, including A0 at 3x, are never affected. If a browser refuses even the reduced canvas, you get a message naming the page rather than a run that never ends." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "English, printed, and honest about it" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["One language model ships with this site \u2014 ", e.jsx("strong", { children: "English" }), " \u2014 so that the entire pipeline can be served from our own origin and works on a locked-down network or with no connection at all once cached. There is no language selector, and pointing this at a document in Greek, Arabic, Hindi or Chinese does not raise an error: the engine returns plausible-looking rubbish, most of which the WinAnsi filter then drops, so what you get is an almost empty text layer rather than a useful one. Printed type is what the model knows. Handwriting, especially cursive, is not recognised in any useful sense."] }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Accuracy is set almost entirely by the scan. A flat, straight, evenly lit page of 10-point type at 300 DPI reads close to perfectly. A photograph taken at an angle, with a shadow across the gutter and the text filling a third of the frame, reads badly, and no option on this page fixes that \u2014 rescanning does. Faint carbon copies, heavy background tints, stamps overlapping text and skewed pages are the usual causes of a disappointing result. Where the resolution setting does earn its keep is small print: change it and press Run OCR again to compare, without re-picking the file." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "What changes about the file" }), e.jsxs("ul", { style: { lineHeight: "1.7", color: "var(--text-secondary)", marginBottom: "1rem", paddingLeft: "1.25rem" }, children: [e.jsxs("li", { children: [e.jsx("strong", { children: "Pages become images." }), " Every page is rebuilt from the render, so anything vector in the original becomes a JPEG at your chosen resolution. For a scan that costs nothing, because it was already pixels; for a born-digital PDF it is a real loss."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Size usually grows." }), " JPEG page images at 144 DPI are bigger than a compressed scan of the same page in many documents. Run ", e.jsx("strong", { children: "Compress PDF" }), " on the output if that matters."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Annotations, forms, bookmarks and links are not carried over." }), " The output is a freshly built document containing page images and the text layer, and nothing else."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "Metadata is not copied either" }), ", which is usually welcome \u2014 but if you needed the original Title and Author, set them again afterwards with ", e.jsx("strong", { children: "PDF Metadata" }), "."] }), e.jsxs("li", { children: [e.jsx("strong", { children: "A password-protected file is not opened at all." }), " If the PDF asks for a password to view, run ", e.jsx("strong", { children: "Unlock PDF" }), " first; one that only restricts printing or editing is processed normally."] })] }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "When to use something else" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: ["If you can already select text in the document, it does not need OCR and running it here would make it worse. You will be told: the first three pages of whatever you drop in are checked for existing text, and a warning appears above the button before you spend a minute finding out. ", e.jsx("strong", { children: "PDF to Text" }), " is the tool for that file, and it extracts the real layer with no error rate at all. If you only want the words and not a PDF, ", e.jsx("strong", { children: "PDF to Text" }), " again for text-layer files, or ", e.jsx("strong", { children: "Image to Text" }), " for a single photograph. If the source is a pile of photos rather than a PDF, ", e.jsx("strong", { children: "Image to PDF" }), " will bundle them into one document that you can then bring back here. And if the document is confidential, note that everything above happens inside this browser tab: the renderer, the recognition engine and the PDF writer are all local, so the file is never uploaded."] })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: jt.map((s, i) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: s.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: s.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: s.desc })] }, i)) })] })] }) });
};
export {
  Ht as default
};
