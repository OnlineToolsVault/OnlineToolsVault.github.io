import { c as m, r as a, j as e } from "./index-OUpguYFg.js";
import { R as w } from "./RelatedTools-dQ1AUZ0r.js";
import { T as v } from "./ToolLayout-CuKFTkh4.js";
import { F as x } from "./FileUploader-Dhw1e8vw.js";
import { J as b } from "./jszip.min-BycgvNgQ.js";
import { A as k, a as j } from "./tools-B3OPepIK.js";
import { E as I } from "./eye-BOsdHd5p.js";
import { S as P } from "./shield-BrCBnKXk.js";
import "./index-CBYUSgtG.js";
import "./_commonjs-dynamic-modules-TDtrdbi3.js";
/**
* @license lucide-react v0.344.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
const z = m("FileArchive", [["path", { d: "M16 22h2a2 2 0 0 0 2-2V7l-5-5H6a2 2 0 0 0-2 2v18", key: "1oywqq" }], ["path", { d: "M14 2v4a2 2 0 0 0 2 2h4", key: "tnqrlb" }], ["circle", { cx: "10", cy: "20", r: "2", key: "1xzdoj" }], ["path", { d: "M10 7V6", key: "dljcrl" }], ["path", { d: "M10 12v-1", key: "v7bkov" }], ["path", { d: "M10 18v-2", key: "1cjy8d" }]]);
/**
* @license lucide-react v0.344.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
const Z = m("FolderOpen", [["path", { d: "m6 14 1.5-2.9A2 2 0 0 1 9.24 10H20a2 2 0 0 1 1.94 2.5l-1.54 6a2 2 0 0 1-1.95 1.5H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h3.9a2 2 0 0 1 1.69.9l.81 1.2a2 2 0 0 0 1.67.9H18a2 2 0 0 1 2 2v2", key: "usdka0" }]]), A = [{ question: "Does the archive get uploaded?", answer: "No. The file is handed to a JavaScript ZIP library inside this tab, which reads the archive's central directory \u2014 the index at the end of the file that lists every entry \u2014 and renders it. No request is made and nothing is stored, so you can inspect an archive containing confidential material without it leaving the machine." }, { question: "What exactly does the listing show me?", answer: "One row per entry, in the archive's own order with directories floated to the top. Each row gives the full path as stored, a folder or file icon, and the modification date, and the header above the list gives the total number of entries, counting folders. Sizes and compression ratios are not shown, and neither is the content of any file \u2014 this is an index of what the archive holds, not a preview of it." }, { question: "Can I extract or preview a file from inside?", answer: "No \u2014 this lists the contents and stops there. That restraint is the point: nothing inside the archive is decompressed, opened, rendered or executed, so a malicious archive has no opportunity to do anything. Once you know what is inside and you trust it, extract with your operating system's own tools." }, { question: "It accepted my .rar file and then said it was invalid.", answer: "The file picker lists .rar and .7z alongside .zip, but the reader only understands the ZIP format. RAR and 7z are entirely different container formats with different compression, so they fail as soon as the reader looks for a ZIP central directory. Use the archiver that created them, or repack as ZIP." }, { question: "Can I inspect a .docx, .jar or .epub?", answer: "Yes, but only after renaming a copy to end in .zip. Those formats are ZIP archives holding XML, class files or assets, so the reader handles them perfectly well \u2014 the obstacle is the file picker, which accepts .zip, .rar and .7z and silently ignores everything else. Dropping a .docx straight in produces no listing and no error message, which looks like a broken page rather than a rejected file. Duplicate the document, change the extension, and it lists normally." }, { question: "My password-protected ZIP will not open.", answer: "Encrypted archives cannot be listed here. The reader has no decryption support at all, so it fails on the archive as a whole rather than showing names and hiding contents. You will get the same message as for a corrupt file, which is worth remembering when diagnosing: the error cannot distinguish between encrypted and damaged." }, { question: "How large an archive can it handle?", answer: "There is no coded limit, but the whole file is read into memory before parsing, so the practical ceiling is your available RAM and the browser's own allocation cap \u2014 usually somewhere in the low gigabytes on desktop and much less on a phone. A very large archive will make the tab unresponsive while it loads, or fail outright with an out-of-memory error." }, { question: "Why is only a date shown, and why is it sometimes a day out?", answer: "Each row carries the calendar date alone, formatted for your locale \u2014 there is no clock time in the listing. The underlying field is ZIP's original MS-DOS timestamp, which has two-second resolution and stores no timezone at all, so an archive created in another region records a wall-clock time with no record of which wall it came off. That is why an entry can land a day either side of what you expect. Treat the dates as approximate ordering information, never as evidence." }, { question: "Why is inspecting first safer than just extracting?", answer: "Because extraction is where archive attacks land. A listing reveals a path traversal attempt \u2014 entries whose names climb out of the target directory \u2014 an executable disguised with a double extension, or an implausible number of entries suggesting a decompression bomb. Reading the names costs nothing and rules all of that out before you commit." }], p = () => {
  const [g, u] = a.useState(null), [o, d] = a.useState([]), [f, n] = a.useState(false), [l, c] = a.useState(""), y = async (t) => {
    u(t), d([]), c(""), n(true);
    try {
      const i = await b.loadAsync(t), h = [];
      i.forEach((s, r) => {
        h.push({ name: r.name, dir: r.dir, date: r.date, comment: r.comment });
      }), d(h.sort((s, r) => s.dir === r.dir ? 0 : s.dir ? -1 : 1)), n(false);
    } catch (i) {
      console.error(i), c(`Could not read "${t.name}". It is not a valid ZIP archive, or it is encrypted.`), n(false);
    }
  };
  return e.jsxs(v, { title: "ZIP File Viewer", description: "View contents of a ZIP file online without extracting.", seoTitle: "ZIP Viewer Online - List ZIP Contents", seoDescription: "View files inside a ZIP archive online. Browser-based ZIP inspection without downloading or extracting.", faqs: A, children: [e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [e.jsx("div", { style: { marginBottom: "2rem" }, children: e.jsx(x, { onFileSelect: y, accept: { "application/zip": [".zip", ".rar", ".7z"] }, icon: k, label: "Drop ZIP file to view contents" }) }), f && e.jsx("div", { style: { textAlign: "center", padding: "2rem" }, children: "Analyzing ZIP..." }), l && e.jsx("div", { role: "alert", style: { padding: "1rem", background: "#fef2f2", border: "1px solid #fee2e2", borderRadius: "0.5rem", color: "#b91c1c", marginBottom: "2rem" }, children: l }), o.length > 0 && e.jsxs("div", { style: { background: "white", borderRadius: "1rem", border: "1px solid var(--border)", overflow: "hidden" }, children: [e.jsxs("div", { style: { padding: "1rem", background: "#f8fafc", borderBottom: "1px solid var(--border)", fontWeight: "bold" }, children: ["Contents of ", g == null ? void 0 : g.name, " (", o.length, " items)"] }), e.jsx("div", { style: { maxHeight: "600px", overflow: "auto" }, children: o.map((t, i) => e.jsxs("div", { style: { display: "flex", alignItems: "center", gap: "0.75rem", padding: "0.75rem 1rem", borderBottom: "1px solid #f1f5f9" }, children: [t.dir ? e.jsx(Z, { size: 18, color: "#eab308" }) : e.jsx(j, { size: 18, color: "#64748b" }), e.jsx("span", { style: { fontFamily: "monospace", fontSize: "0.9rem" }, children: t.name }), e.jsx("span", { style: { marginLeft: "auto", color: "#94a3b8", fontSize: "0.8rem" }, children: t.date ? t.date.toLocaleDateString() : "" })] }, i)) })] })] }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(w, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About ZIP Viewer Online" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Drop an archive and you get its table of contents: every entry, with its full stored path, a folder or file marker, and the modification date the archive recorded. Directories are listed first. Nothing is decompressed, nothing is extracted to disk, and nothing is uploaded." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Reading the index, not the contents" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["A ZIP file keeps an index called the ", e.jsx("strong", { children: "central directory" }), " at the very end, listing every entry and where its data begins. That design is why an archiver can show you the contents of a large archive instantly: it reads the index and never touches the compressed payloads. This viewer does the same thing. The consequence worth understanding is that the listing describes what the archive ", e.jsx("em", { children: "claims" }), " to contain \u2014 the entry names and dates are metadata, and no byte of any compressed file is decoded, checked against its stored checksum, or run."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Why look before extracting" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Almost every archive-related security problem happens during extraction rather than during reading. A listing lets you spot the classic warning signs first: an entry whose path climbs out of the destination folder with parent-directory segments, an installer or script hiding behind a double extension, a single entry that expands to an implausible size, or thousands of entries where you expected a handful. Checking takes a second and costs nothing, because the names alone cannot do anything to your machine." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "ZIP is more common than the extension suggests" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "A surprising amount of everyday software ships inside ZIP containers under other names. Word, Excel and PowerPoint documents are ZIPs full of XML; Java JARs and Android APKs are ZIPs of class files and resources; EPUB books, browser extensions and many plugin bundles are the same. The reader handles all of them, but the file picker does not: it accepts only .zip, .rar and .7z, and anything else is dropped without a listing and without an error. Duplicate the file, rename the copy to .zip, and it opens. That is a fast way to confirm a document is structurally intact rather than corrupt, to see which fonts or images a deck is dragging around, or to check what a downloaded bundle actually contains before installing it." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Limits worth knowing before you start" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "The picker offers .rar and .7z but the reader understands only ZIP, so those formats fail with the same message as a corrupt file. Encrypted archives cannot be listed at all \u2014 there is no decryption support, so a password-protected ZIP is rejected outright rather than showing names with contents withheld. Sizes are not displayed. And because the entire file is loaded into memory before the index is parsed, the ceiling is your available RAM rather than a configured limit; a multi-gigabyte archive may simply exhaust the tab." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "About those timestamps" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "The dates come from ZIP's original MS-DOS timestamp field, which resolves to two seconds and carries no timezone. An archive built in another country therefore records a wall-clock time with no indication of which clock, so a date can appear a day off either side of what you expect. Use them to order entries and to spot an obviously stale file; do not use them as evidence of when something was created." })] })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: p.features.map((t, i) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: i === 0 ? e.jsx(I, { color: "var(--primary)", size: 24 }) : i === 1 ? e.jsx(P, { color: "var(--primary)", size: 24 }) : e.jsx(z, { color: "var(--primary)", size: 24 }) }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: t.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: t.desc })] }, i)) })] })] });
};
p.features = [{ title: "Index Only, Nothing Executed", desc: "The archive index is parsed and rendered while every compressed payload is left untouched \u2014 not decoded, not written to disk, not run. Inspecting an untrusted download is therefore risk-free in a way extracting it is not." }, { title: "Never Leaves Your Machine", desc: "The ZIP reader is a JavaScript library running in this tab, so an archive full of client documents or internal source can be inspected without an upload, an account or a temporary copy on someone else\u2019s server." }, { title: "Disguised ZIPs, After A Rename", desc: "Office documents, JARs, APKs and EPUBs are ZIP containers wearing another extension. The picker only takes .zip, .rar and .7z, so copy one and change the extension to .zip and its internal structure lists like any other archive." }];
export {
  p as default
};
