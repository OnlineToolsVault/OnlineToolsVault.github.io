import { r as s, j as e } from "./index-OUpguYFg.js";
import { R as u } from "./RelatedTools-dQ1AUZ0r.js";
import { T as g } from "./ToolLayout-CuKFTkh4.js";
import { A as y } from "./arrow-right-BvMEROXY.js";
import { C as f } from "./check-CzYxOQpM.js";
import { C as o } from "./copy-BogRq2Ao.js";
import { k as b } from "./tools-B3OPepIK.js";
import { S as w } from "./shield-BrCBnKXk.js";
const x = [{ question: "Should I paste a whole URL in here?", answer: "Usually not. This encodes a component \u2014 one value that will sit inside a URL \u2014 so it escapes the delimiters too. Paste a full address and the colon and slashes after the scheme come back as %3A and %2F, giving you a string that is no longer a working link. That is correct when the URL is itself a parameter value, as with a redirect_uri or a share link; it is wrong if you were hoping to tidy up an address. For that, only the illegal characters need escaping, and your browser will do it when you paste the URL into the address bar." }, { question: "Exactly which characters survive untouched?", answer: "Letters, digits, and exactly nine punctuation marks: hyphen, underscore, full stop, exclamation mark, tilde, asterisk, apostrophe and the two round brackets. Everything else becomes a percent sign followed by two hex digits: a space is %20, an ampersand %26, an equals sign %3D, a slash %2F, a question mark %3F, a hash %23, a plus %2B and a colon %3A. Note that square brackets are not on the safe list \u2014 they become %5B and %5D." }, { question: "Why is my space %20 and not a plus sign?", answer: "Because they belong to different specifications. Percent-encoding, which governs URLs, represents a space as %20. The plus sign comes from HTML form submission, where a browser encodes a space that way when posting application/x-www-form-urlencoded data. Both appear in the wild and the receiving code has to know which it is reading. If your endpoint parses form data and treats a literal plus as a space, encode any real plus in your value as %2B \u2014 which this tool does automatically." }, { question: "What happens to accented characters and emoji?", answer: "They are converted to UTF-8 bytes first and each byte is then percent-encoded, which is why one character can produce several escapes. An e-acute becomes %C3%A9, a CJK character becomes three escapes, and an emoji becomes four. This matches what every modern server, language runtime and HTTP client expects, so the output round-trips cleanly." }, { question: "It said encoding failed. What did I paste?", answer: "Almost certainly a lone surrogate \u2014 half of a character that is normally stored as a pair, which arrives when text is truncated at the wrong byte or copied out of a mangled log. There is no valid UTF-8 representation for it, so the encoder refuses rather than emitting nonsense. Find the truncation point in your source data and fix it there." }, { question: "Are the apostrophe and brackets really safe to leave alone?", answer: "For ordinary web use, yes \u2014 browsers and servers handle them without complaint. There are two places where it matters that this tool leaves them unencoded. OAuth 1.0 signature base strings require the apostrophe, asterisk, exclamation mark and brackets to be escaped, so a signature computed on this output will not match. Some older systems also treat an apostrophe specially. In both cases, escape those characters by hand afterwards." }, { question: "Is there a length limit?", answer: "Nothing is capped here \u2014 the box will happily encode many kilobytes. The limit lives downstream. Browsers and servers impose their own caps on how long a URL may be, commonly around 2000 characters for broad compatibility and around 8000 before typical server configurations reject the request outright. If your encoded value is heading past that, send it in a request body instead of a query string." }, { question: "Is anything I paste transmitted?", answer: "No. Encoding is a single built-in browser function call with no network involvement, so the value never leaves this tab. Nothing is stored either. That makes it safe for tokens and internal URLs, subject to the usual caution that the value is visible on your screen and sits in your clipboard afterwards." }, { question: "How do I reverse it?", answer: "Use the URL Decoder, which applies the exact inverse and will return your original text. Note that decoding is not symmetric with form encoding: a plus sign in the input comes back as a plus sign, not as a space, because the decoder follows percent-encoding rules rather than form rules." }], i = () => {
  const [r, c] = s.useState(""), [a, d] = s.useState(""), [l, n] = s.useState(false), h = () => {
    try {
      d(encodeURIComponent(r));
    } catch {
      alert("Encoding failed");
    }
  }, p = () => {
    navigator.clipboard.writeText(a), n(true), setTimeout(() => n(false), 2e3);
  };
  return e.jsxs(g, { title: "URL Encoder", description: "Encode text for use in URLs (Percent encoding).", seoTitle: "URL Encoder - Online Percent Encoding", seoDescription: "Convert text to URL-safe format. Free online URL encoder tool.", faqs: x, children: [e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [e.jsxs("div", { style: { marginBottom: "2rem" }, children: [e.jsx("label", { style: { display: "block", marginBottom: "0.5rem", fontWeight: "bold" }, children: "Text to Encode" }), e.jsx("textarea", { value: r, onChange: (t) => c(t.target.value), placeholder: "Type text here...", style: { width: "100%", minHeight: "120px", padding: "1rem", borderRadius: "0.5rem", border: "1px solid var(--border)", fontSize: "1rem" } })] }), e.jsx("div", { style: { textAlign: "center", marginBottom: "2rem" }, children: e.jsxs("button", { onClick: h, className: "tool-btn-primary", style: { padding: "1rem 3rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", cursor: "pointer", fontWeight: "bold", display: "inline-flex", alignItems: "center", gap: "0.5rem" }, children: ["Encode ", e.jsx(y, { size: 20 })] }) }), a && e.jsxs("div", { children: [e.jsxs("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "0.5rem" }, children: [e.jsx("label", { style: { fontWeight: "bold" }, children: "URL Encoded Output" }), e.jsxs("button", { onClick: p, style: { border: "none", background: "none", color: "var(--primary)", cursor: "pointer", fontWeight: "bold", display: "flex", alignItems: "center", gap: "0.25rem" }, children: [l ? e.jsx(f, { size: 16 }) : e.jsx(o, { size: 16 }), " Copy"] })] }), e.jsx("textarea", { readOnly: true, value: a, style: { width: "100%", minHeight: "120px", padding: "1rem", borderRadius: "0.5rem", border: "1px solid var(--border)", fontSize: "1rem", fontFamily: "monospace", background: "#f8fafc", color: "#475569" } })] })] }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(u, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About URL Encoder" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["This page performs ", e.jsx("strong", { children: "component" }), " encoding: it escapes everything that is not an unreserved character, including the delimiters that give a URL its structure. That is the right transformation for a single value you are about to drop into a query string, a path segment or a form field \u2014 and the wrong one for an entire address, which would come back with its own colons and slashes escaped."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "What percent-encoding is for" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["A URL is a structured string, and a handful of characters carry that structure: a question mark starts the query, an ampersand separates parameters, an equals sign splits a name from its value, a slash divides path segments and a hash begins the fragment. The moment one of those appears inside a value rather than between them, the parser on the other end reads it as punctuation. A search for ", e.jsx("code", { children: "fish & chips" }), " dropped raw into ", e.jsx("code", { children: "?q=" }), " arrives as a parameter ", e.jsx("code", { children: "q" }), " holding ", e.jsx("code", { children: "fish " }), " plus a second, empty parameter called", e.jsx("code", { children: " chips" }), ". Percent-encoding removes the ambiguity by replacing each such byte with a percent sign and its two hex digits, so the receiver knows it is data."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Component encoding versus whole-URL encoding" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["The distinction catches people out constantly, so it is worth being concrete. Encoding the address", e.jsx("code", { children: " https://a.com/p q?x=1" }), " as a component produces", e.jsx("code", { children: " https%3A%2F%2Fa.com%2Fp%20q%3Fx%3D1" }), " \u2014 every structural character escaped, which is exactly what you want when that URL is going to be the value of a ", e.jsx("code", { children: "redirect_uri" }), " or a", e.jsx("code", { children: " url" }), " parameter. Whole-URL encoding would instead leave the scheme, slashes and query marker intact and escape only the space. Deciding which you need comes down to one question: is this string a URL, or is it a value that happens to look like one?"] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Non-ASCII text becomes UTF-8 bytes" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Anything outside plain ASCII is converted to UTF-8 first and each resulting byte is escaped separately, so a single character can expand to several escapes \u2014 two for most accented Latin letters, three for CJK characters, four for an emoji. This is the behaviour every modern server and HTTP client expects, and it is why the output is safe to hand to a backend written in any language. The one input it will refuse is a lone surrogate, the orphaned half of a character pair that turns up when text has been cut at the wrong byte offset; there is no legal encoding for it, so the tool reports a failure instead of guessing." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Two traps: the plus sign and double encoding" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["A space becomes ", e.jsx("code", { children: "%20" }), " here, never a plus. The plus convention belongs to HTML form submission, and mixing the two is a classic source of corrupted values \u2014 which is also why a literal plus in your input is escaped to ", e.jsx("code", { children: "%2B" }), ", so it cannot be read back as a space. The second trap is running text through an encoder twice: a percent sign is itself escaped, so", e.jsx("code", { children: " %20" }), " becomes ", e.jsx("code", { children: "%2520" }), " and the recipient sees the literal text", e.jsx("code", { children: " %20" }), " instead of a space. If a value arrives looking over-escaped, suspect an encode step happening in both your code and your HTTP library."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Scope, and where it runs" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: "Encoding happens in your browser through a built-in function, with no request made and nothing stored, so internal URLs and tokens stay local. Bear in mind what percent-encoding is not: it offers no confidentiality and no integrity, since anyone can decode it in one step. It is also not a security control on its own \u2014 escaping a value for a URL does nothing to make it safe inside HTML, SQL or a shell command, each of which needs its own escaping. Note finally that the apostrophe, asterisk, exclamation mark and brackets are deliberately left as-is, which is fine for the web but will break an OAuth 1.0 signature base string unless you escape them yourself." })] })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: i.features.map((t, m) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: t.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: t.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: t.desc })] }, m)) })] });
};
i.features = [{ title: "Escapes Delimiters Too", desc: "Ampersands, equals signs, slashes, question marks and hashes are all escaped, not just spaces \u2014 so a value containing them cannot be misread as extra parameters by whatever parses the URL at the other end.", icon: e.jsx(b, { color: "var(--primary)", size: 24 }) }, { title: "UTF-8 Byte Sequences", desc: "Accented letters, CJK text and emoji are converted to UTF-8 and escaped byte by byte, producing exactly what a modern server, framework or HTTP client expects to receive and decode.", icon: e.jsx(o, { color: "var(--primary)", size: 24 }) }, { title: "A Built-In Browser Call", desc: "Encoding uses the platform\u2019s own function with no network step at any point, so pasting an internal URL or a token here does not put it on anyone else\u2019s server.", icon: e.jsx(w, { color: "var(--primary)", size: 24 }) }];
export {
  i as default
};
