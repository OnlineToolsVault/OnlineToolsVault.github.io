import { r as n, j as e } from "./index-DsTeKLg-.js";
import { R as p } from "./RelatedTools-Dai5N42q.js";
import { T as u } from "./ToolLayout-DdnzCrcK.js";
import { A as y } from "./arrow-right-BB5peS8Z.js";
import { C as b } from "./check-CBilouqu.js";
import { C as f } from "./copy-C94LAPHc.js";
import { Z as x } from "./zap-DAyflzDH.js";
import { y as w } from "./toolPageSchema-BVedbqe3.js";
import { S as v } from "./shield-CtuUP7ih.js";
const j = [{ question: "Why does my string fail with hyphens and underscores in it?", answer: "Because that is base64url, a different alphabet. It swaps plus for hyphen and slash for underscore so the value is safe inside URLs and filenames, and it usually drops the trailing equals signs too. This box expects the standard alphabet and rejects the substitutes outright. Replace every hyphen with a plus and every underscore with a slash and it will decode. If the value came from a JSON Web Token, the JWT Decoder does that translation for you and parses the claims as well." }, { question: "Do line breaks or missing padding matter?", answer: "Neither breaks anything here. Base64 copied out of an email header, a PEM certificate or a wrapped log line arrives split across many 64- or 76-column rows, and the browser's decoder ignores all whitespace before it starts. Absent equals signs are tolerated as well. The one length that genuinely cannot be decoded is a string whose character count leaves a remainder of one when divided by four \u2014 that is a truncated value, not a padding problem, and the missing characters cannot be reconstructed." }, { question: "Why is the output full of question marks and boxes?", answer: "The bytes decoded correctly but they were never text. The tool first attempts a strict UTF-8 decode; when that fails it falls back to showing the raw bytes one character at a time, which is what produces the garbage. A result starting with PNG, PK or %PDF is a real file that happens to have been Base64-wrapped, and reading it in a text box will not help \u2014 it needs to be written back out as a binary file." }, { question: "Does decoding tell me whether the data was tampered with?", answer: "No. Base64 carries no checksum, no signature and no key, so a modified string simply decodes to different bytes without complaint. The only inputs that raise an error are ones containing characters outside the alphabet or a length that cannot be valid. If integrity matters, compare a digest of the decoded content using the Hash Generator against a digest you trust." }, { question: "Is it safe to paste a token or credential in here?", answer: "Nothing you paste leaves this tab \u2014 decoding is a single browser API call with no network step, which you can confirm in the Network panel of DevTools. The real risks are local rather than remote: the value sits in your clipboard, in the page until you navigate away, and in a screen recording or shoulder-surfer's view. For a live production secret, decode it on your own machine instead." }, { question: "Can I decode more than one string at a time?", answer: "No. Whitespace is stripped before decoding, so several strings on separate lines are treated as one long value. If they still carry their trailing equals signs, the padding ends up in the middle of that value and you get the red Invalid Base64 string error; if the padding was already absent, the strings silently merge and decode to garbage. Either way, decode them one at a time." }], B = [{ title: "Whitespace And Padding Tolerant", desc: "Base64 wrapped across many lines, or missing its trailing equals signs, decodes without any cleanup \u2014 paste straight from a PEM block, an email header or a log file." }, { title: "Strict UTF-8, With A Fallback", desc: "Text is decoded as UTF-8 so accents and emoji survive intact. When the bytes are not valid UTF-8 the tool shows them raw instead of failing, which is how you spot that a value was really a file." }, { title: "One Browser API Call", desc: "Decoding uses the platform\u2019s own atob, so there is no upload, no queue and no history. Closing the tab is all the cleanup there is." }], T = (r) => {
  const s = atob(r.trim()), t = Uint8Array.from(s, (o) => o.charCodeAt(0));
  try {
    return new TextDecoder("utf-8", { fatal: true }).decode(t);
  } catch {
    return s;
  }
}, z = () => {
  const [r, s] = n.useState(""), [t, o] = n.useState(""), [h, d] = n.useState(false), [c, l] = n.useState(false), m = () => {
    try {
      o(T(r)), l(false);
    } catch {
      l(true), o("");
    }
  }, g = () => {
    navigator.clipboard.writeText(t), d(true), setTimeout(() => d(false), 2e3);
  };
  return e.jsx(u, { title: "Base64 Decoder", description: "Decode Base64 strings back to plain text.", seoTitle: "Base64 Decoder - Online Base64 to Text", seoDescription: "Convert Base64 strings back to text online. Free Base64 decoder tool.", faqs: j, children: e.jsxs("div", { style: { maxWidth: "1000px", margin: "0 auto" }, children: [e.jsxs("div", { style: { marginBottom: "2rem" }, children: [e.jsx("label", { style: { display: "block", marginBottom: "0.5rem", fontWeight: "bold" }, children: "Base64 String" }), e.jsx("textarea", { value: r, onChange: (a) => s(a.target.value), placeholder: "Paste Base64 string here...", style: { width: "100%", minHeight: "120px", padding: "1rem", borderRadius: "0.5rem", border: "1px solid var(--border)", fontSize: "1rem", fontFamily: "monospace" } })] }), e.jsx("div", { style: { textAlign: "center", marginBottom: "2rem" }, children: e.jsxs("button", { onClick: m, className: "btn-primary", style: { padding: "1rem 3rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", cursor: "pointer", fontWeight: "bold", display: "inline-flex", alignItems: "center", gap: "0.5rem" }, children: ["Decode ", e.jsx(y, { size: 20 })] }) }), c && e.jsx("div", { style: { padding: "1rem", background: "#fee2e2", color: "#b91c1c", borderRadius: "0.5rem", textAlign: "center", marginBottom: "2rem" }, children: "Invalid Base64 string." }), t && e.jsxs("div", { children: [e.jsxs("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "0.5rem" }, children: [e.jsx("label", { style: { fontWeight: "bold" }, children: "Decoded Text" }), e.jsxs("button", { onClick: g, style: { border: "none", background: "none", color: "var(--primary)", cursor: "pointer", fontWeight: "bold", display: "flex", alignItems: "center", gap: "0.25rem" }, children: [h ? e.jsx(b, { size: 16 }) : e.jsx(f, { size: 16 }), " Copy"] })] }), e.jsx("textarea", { readOnly: true, value: t, style: { width: "100%", minHeight: "120px", padding: "1rem", borderRadius: "0.5rem", border: "1px solid var(--border)", fontSize: "1rem", background: "#f8fafc", color: "#475569" } })] }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(p, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About Base64 Decoder" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Base64 shows up wherever bytes have to travel through something that only handles text: an ", e.jsx("code", { children: "Authorization: Basic" }), " header, a Kubernetes Secret, a certificate pasted into a config file, a ", e.jsx("code", { children: "data:" }), " URI in a stylesheet. Paste the string above and this page reverses the transformation, four characters at a time, back into the original three bytes."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Two decoding steps, and why the second one can fail" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Decoding happens in two stages. First the characters are mapped back to bytes, which either succeeds or reports ", e.jsx("em", { children: "Invalid Base64 string" }), ". Then those bytes are interpreted as UTF-8 text so that accented letters, CJK characters and emoji come out whole. The second stage is the one that can be wrong even when nothing errored: if the payload was a PNG, a ZIP or a protobuf message, the bytes are perfectly valid but they are not text, and what you see is the tool rendering them literally."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Reading the failure message" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Only two things trigger the red error box. One is a character outside the standard alphabet \u2014 most often a hyphen or underscore from the URL-safe variant, occasionally a stray quote copied along with the value from JSON. The other is a length that no valid Base64 string can have, which means the value was cut off in transit. Whitespace, newlines and missing padding are all handled silently, so they are never the cause." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "What decoding does and does not prove" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["A string that decodes cleanly has told you nothing about who produced it. Base64 is an encoding, not a signature and not encryption, and a Kubernetes Secret is exactly as readable as its ", e.jsx("code", { children: "data" }), " field suggests. Treat any credential recovered here as public the moment it appeared on screen."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "When another tool fits better" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["For a JSON Web Token, use the ", e.jsx("strong", { children: "JWT Decoder" }), ": it handles the URL-safe alphabet, splits the three segments for you and pretty-prints the claims. For percent-encoded text such as ", e.jsx("code", { children: "%20" }), " and ", e.jsx("code", { children: "%3D" }), ", that is URL encoding rather than Base64 \u2014 the ", e.jsx("strong", { children: "URL Decoder" }), " is the right tool. And when the decoded output turns out to be JSON, the ", e.jsx("strong", { children: "JSON Formatter" }), " will indent it into something readable."] })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: B.map((a, i) => e.jsxs("div", { className: "feature-card", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: i === 0 ? e.jsx(x, { color: "var(--primary)", size: 24 }) : i === 1 ? e.jsx(w, { color: "var(--primary)", size: 24 }) : e.jsx(v, { color: "var(--primary)", size: 24 }) }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: a.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: a.desc })] }, i)) })] })] }) });
};
export {
  z as default
};
