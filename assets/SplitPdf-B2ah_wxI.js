import { r as h, j as e, L as R } from "./index-DsTeKLg-.js";
import { R as A } from "./RelatedTools-Dai5N42q.js";
import { T as N } from "./ToolLayout-DdnzCrcK.js";
import { F as C } from "./FileUploader-Bd7gYSgk.js";
import { P as y } from "./PDFButton-CYwtNMhL.js";
import { F as k } from "./FileSaver.min-DaXhTG4A.js";
import { J as $ } from "./jszip.min-qxfOdwpf.js";
import { S, a as W, b as q } from "./toolPageSchema-BVedbqe3.js";
import { F as L } from "./files-BqJfgYeJ.js";
import { L as O } from "./list-ordered-Cm-lC8r2.js";
import "./index-Bpm0RpmP.js";
import "./UPNG-BTH4c9OI.js";
import "./_commonjs-dynamic-modules-TDtrdbi3.js";
import "./shield-CtuUP7ih.js";
const H = [{ title: "Burst into single pages", desc: "Extract every page mode reads the page count off the file and writes one PDF per page, named page-1.pdf, page-2.pdf and so on, then bundles them into a DEFLATE-compressed ZIP.", icon: e.jsx(L, { color: "var(--primary)", size: 24 }) }, { title: "Ranges that mean what they say", desc: "Type 1-3, 5, 8-10 and you get three files: a three-page one, a one-page one, and another three-page one. Each comma-separated entry becomes its own document rather than being flattened together.", icon: e.jsx(O, { color: "var(--primary)", size: 24 }) }, { title: "One file or an archive, automatically", desc: "Ask for a single range and you get a plain PDF. Ask for more than one and the pieces are zipped so the browser only has to trigger one download instead of fighting a pop-up blocker.", icon: e.jsx(q, { color: "var(--primary)", size: 24 }) }], Z = [{ question: "How exactly do I write the page ranges?", answer: "Comma-separated, one-based, spaces ignored: 1-3, 5, 8-10 is valid. A hyphen means an inclusive span, so 8-10 gives pages 8, 9 and 10. Each entry produces its own file \u2014 1-3, 5 yields a three-page PDF and a one-page PDF, not one four-page document. Backwards spans such as 9-4 are rejected rather than silently reversed." }, { question: "When do I get a single PDF and when do I get a ZIP?", answer: "One group means one download. If your range list resolves to exactly one entry, the result is a plain PDF called something like report-pages-2-7.pdf. Anything that produces two or more pieces \u2014 including every page mode \u2014 comes back as report-split.zip, with the individual PDFs inside." }, { question: "It refused my range and told me it cannot use an entry. Why?", answer: "An entry is rejected if it is not a number, if it runs backwards, or if it lands entirely outside the document \u2014 a bare 50 or a span of 50-60 on a 42-page file. The whole job stops rather than the bad entry being quietly dropped, so you never download a set that is missing pages you asked for. One case is treated more leniently: a span that starts inside the document and runs past the end, such as 1-50 on 42 pages, is clamped to the last page and succeeds. You get all 42 pages, in a file still named after the numbers you typed." }, { question: "How do I pull several scattered pages into one document?", answer: "This tool always writes one file per entry, so it cannot do that in a single step. Load the PDF into **Organize PDF** and delete the pages you do not want \u2014 that leaves one document containing exactly the pages you kept. Alternatively split first, then recombine the pieces with **Merge PDF**." }, { question: "Do bookmarks and fillable fields survive the split?", answer: "Page content, embedded fonts, images and per-page annotations are copied intact. Bookmarks are not: the outline lives on the document catalogue rather than on any page, so each piece comes out without one. Interactive form fields keep their appearance but lose their registration in the form dictionary, so they stop being fillable \u2014 flatten the form first if the values matter." }, { question: "Why are the pieces bigger than a proportional share of the original?", answer: "Every output document must carry the resources its pages reference. If one embedded font is used throughout a 100-page report, splitting into 100 single-page PDFs embeds that font 100 times. The total across the ZIP can therefore exceed the original file, even though no page has been re-encoded. Running the pieces through **Compress PDF** trims the metadata but will not undo the duplication." }, { question: "Can I split a password-protected PDF?", answer: "No. The parser refuses encrypted documents, and because that happens the moment the file is read, you get an Invalid PDF file alert and the tool returns to the drop zone rather than showing you a page count. Remove the password with **Unlock PDF** (you need to know it) and split the unlocked copy." }, { question: "Is there a page limit, and does it work on a phone?", answer: "No fixed limit \u2014 the constraint is memory. Bursting a 1,000-page file means building 1,000 documents and holding a ZIP of all of them in RAM at once, which a desktop handles and a budget phone may not. On mobile, prefer targeted ranges over every page mode; the interface itself works in any modern mobile browser." }], ne = () => {
  const [u, b] = h.useState(null), [j, P] = h.useState(false), [x, F] = h.useState(0), [c, B] = h.useState("all"), [w, I] = h.useState(""), [D, l] = h.useState(""), T = async (t) => {
    b(t), l("");
    try {
      const r = await t.arrayBuffer(), s = await y.load(r);
      F(s.getPageCount());
    } catch (r) {
      console.error(r), alert("Invalid PDF file"), b(null);
    }
  }, z = (t, r) => {
    const s = [], o = [];
    for (const f of t.split(",")) {
      const i = f.trim();
      if (!i) continue;
      const m = i.match(/^(\d+)\s*-\s*(\d+)$/);
      if (m) {
        const n = Number(m[1]), p = Number(m[2]), d = [];
        for (let g = n; g <= p; g++) g >= 1 && g <= r && d.push(g - 1);
        n > p || d.length === 0 ? o.push(i) : s.push({ label: `pages-${n}-${p}`, indices: d });
        continue;
      }
      const a = i.match(/^\d+$/) ? Number(i) : NaN;
      a >= 1 && a <= r ? s.push({ label: `page-${a}`, indices: [a - 1] }) : o.push(i);
    }
    return { groups: s, invalid: o };
  }, E = async () => {
    l(""), P(true);
    try {
      const t = await u.arrayBuffer(), r = await y.load(t), s = r.getPageCount();
      let o;
      if (c === "all") o = Array.from({ length: s }, (a, n) => ({ label: `page-${n + 1}`, indices: [n] }));
      else {
        const a = z(w, s);
        if (a.invalid.length > 0) {
          l(`This PDF has ${s} page${s === 1 ? "" : "s"}. Cannot use: ${a.invalid.join(", ")}`);
          return;
        }
        if (a.groups.length === 0) {
          l('Enter at least one page or page range, for example "1-3, 5".');
          return;
        }
        o = a.groups;
      }
      const f = u.name.replace(/\.pdf$/i, "");
      if (o.length === 1) {
        const a = await y.create();
        (await a.copyPages(r, o[0].indices)).forEach((d) => a.addPage(d));
        const p = await a.save();
        k.saveAs(new Blob([p], { type: "application/pdf" }), `${f}-${o[0].label}.pdf`);
        return;
      }
      const i = new $();
      for (const a of o) {
        const n = await y.create();
        (await n.copyPages(r, a.indices)).forEach((d) => n.addPage(d)), i.file(`${a.label}.pdf`, await n.save());
      }
      const m = await i.generateAsync({ type: "blob", compression: "DEFLATE" });
      k.saveAs(m, `${f}-split.zip`);
    } catch (t) {
      console.error("Split error:", t), l("Could not split this PDF. It may be corrupted or password protected \u2014 try Unlock PDF first.");
    } finally {
      P(false);
    }
  }, v = j || c === "range" && w.trim() === "";
  return e.jsx(N, { title: "Split PDF Online", description: "Extract pages from your PDF or split it into multiple files instantly.", seoTitle: "Split PDF Online - Extract Pages Free", seoDescription: "Free online PDF splitter. Extract pages, separate PDF documents, and save as individual files. No signup, secure client-side processing.", faqs: Z, children: e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [e.jsx("div", { style: { background: "white", border: "1px solid var(--border)", borderRadius: "1rem", padding: "2rem" }, children: u ? e.jsxs("div", { className: "tool-active-interface", style: { maxWidth: "600px", margin: "0 auto" }, children: [e.jsxs("div", { style: { display: "flex", alignItems: "center", gap: "1rem", padding: "1rem", background: "#f1f5f9", borderRadius: "0.75rem", marginBottom: "2rem" }, children: [e.jsx("div", { style: { background: "#cbd5e1", padding: "0.5rem", borderRadius: "0.5rem" }, children: e.jsx(W, { size: 24, color: "#475569" }) }), e.jsxs("div", { style: { flex: 1 }, children: [e.jsx("p", { style: { fontWeight: "600", color: "#334155" }, children: u.name }), e.jsxs("p", { style: { fontSize: "0.875rem", color: "#64748b" }, children: [x, " pages"] })] }), e.jsx("button", { onClick: () => {
    b(null), l(""), F(0);
  }, "aria-label": "Remove file", style: { background: "none", border: "none", padding: "0.5rem", cursor: "pointer", color: "#ef4444" }, children: "\xD7" })] }), e.jsxs("fieldset", { style: { border: "none", padding: 0, margin: "0 0 1.5rem" }, children: [e.jsx("legend", { style: { marginBottom: "0.75rem", fontWeight: "600", color: "#334155" }, children: "How should we split it?" }), e.jsx("div", { style: { display: "grid", gap: "0.75rem" }, children: [{ id: "all", title: "Extract every page", desc: `Creates ${x || "one"} separate PDF${x === 1 ? "" : "s"}, delivered as a ZIP.` }, { id: "range", title: "Custom range", desc: "Pick exactly the pages or ranges you need." }].map((t) => e.jsxs("label", { style: { display: "flex", alignItems: "flex-start", gap: "0.75rem", padding: "1rem", border: `1px solid ${c === t.id ? "var(--primary)" : "var(--border)"}`, background: c === t.id ? "var(--primary-light)" : "white", borderRadius: "0.75rem", cursor: "pointer" }, children: [e.jsx("input", { type: "radio", name: "split-mode", value: t.id, checked: c === t.id, onChange: () => {
    B(t.id), l("");
  }, style: { marginTop: "0.25rem" } }), e.jsxs("span", { children: [e.jsx("span", { style: { display: "block", fontWeight: "600", color: "#334155" }, children: t.title }), e.jsx("span", { style: { display: "block", fontSize: "0.875rem", color: "#64748b" }, children: t.desc })] })] }, t.id)) })] }), c === "range" && e.jsxs("div", { style: { marginBottom: "2rem" }, children: [e.jsxs("label", { htmlFor: "split-range", style: { display: "block", marginBottom: "0.5rem", fontWeight: "600", color: "#334155" }, children: ["Page Ranges to Extract ", e.jsx("span", { style: { fontWeight: "400", color: "#64748b" }, children: "(e.g. 1-3, 5, 8-10)" })] }), e.jsx("input", { id: "split-range", type: "text", value: w, onChange: (t) => {
    I(t.target.value), l("");
  }, placeholder: "Enter page ranges separated by comma", style: { width: "100%", padding: "1rem", borderRadius: "0.5rem", border: "1px solid var(--border)", fontSize: "1rem" } })] }), D && e.jsx("p", { role: "alert", style: { marginBottom: "1rem", padding: "0.875rem 1rem", borderRadius: "0.5rem", background: "#fef2f2", color: "#b91c1c", border: "1px solid #fecaca" }, children: D }), e.jsx("button", { onClick: E, disabled: v, className: "tool-btn-primary", style: { width: "100%", padding: "1rem", borderRadius: "0.5rem", background: v ? "#94a3b8" : "var(--primary)", color: "white", fontWeight: "bold", fontSize: "1.1rem", border: "none", cursor: v ? "not-allowed" : "pointer", display: "flex", alignItems: "center", justifyContent: "center", gap: "0.75rem" }, children: j ? e.jsxs(e.Fragment, { children: [e.jsx(R, { className: "spin", size: 20 }), " Processing..."] }) : e.jsxs(e.Fragment, { children: [e.jsx(S, { size: 20 }), " Split PDF"] }) }), e.jsx("style", { children: "@keyframes spin { 100% { transform: rotate(360deg); } } .spin { animation: spin 1s linear infinite; }" })] }) : e.jsx(C, { onFileSelect: (t) => T(t), accept: { "application/pdf": [".pdf"] }, icon: S, label: "Drop PDF to Split", subLabel: "or click to browse files" }) }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(A, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About Split PDF" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Splitting takes one PDF apart into smaller PDFs. Drop a file in and the page count appears beside its name, then pick one of two modes: ", e.jsx("strong", { children: "extract every page" }), ", which writes one document per page, or ", e.jsx("strong", { children: "custom range" }), ", where you type the pages you want. Everything happens in this tab \u2014 the file is read with the browser File API and never uploaded."] }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "The range syntax, in full" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Entries are separated by commas and counted from 1, the way the page numbers appear in a reader. A bare number extracts that single page; two numbers joined by a hyphen extract an inclusive span. Whitespace is ignored, so ", e.jsx("strong", { children: "1-3,5" }), " and ", e.jsx("strong", { children: "1 - 3, 5" }), " behave identically. The important thing to internalise is that each entry becomes a separate output file. ", e.jsx("strong", { children: "2-4, 9" }), " gives you a three-page document and a one-page document, not one four-page document; if you wanted the latter, ask for the span you actually want or delete the surplus pages in Organize PDF instead."] }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Bad entries stop the whole job rather than being dropped. A reversed span such as ", e.jsx("strong", { children: "9-4" }), ", anything that is not a number, or an entry that falls entirely outside the document \u2014 a bare ", e.jsx("strong", { children: "50" }), " or a span of ", e.jsx("strong", { children: "50-60" }), " on a 42-page file \u2014 produces a message naming the offending entry and reminding you how many pages the file has. That is deliberate: silently skipping an entry is how people end up emailing a set of extracts with a chapter missing. The one exception is a span that begins inside the document and overshoots the end: ", e.jsx("strong", { children: "1-50" }), " on a 42-page file is clamped to page 42 and goes through, so check the page count shown beside the file name if you want the number in the filename to match what is inside."] }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "What you get back" }), e.jsxs("ul", { style: { lineHeight: "1.7", color: "var(--text-secondary)", marginBottom: "1rem", paddingLeft: "1.25rem" }, children: [e.jsxs("li", { children: ["A single range produces a plain PDF named after the source, for example ", e.jsx("strong", { children: "contract-pages-4-9.pdf" }), " or ", e.jsx("strong", { children: "contract-page-3.pdf" }), "."] }), e.jsxs("li", { children: ["Two or more pieces are collected into ", e.jsx("strong", { children: "contract-split.zip" }), ", DEFLATE-compressed, with each piece inside under the same naming scheme."] }), e.jsx("li", { children: "Every page mode always produces a ZIP unless the document is genuinely one page long." })] }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "How the pages are moved" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "A new empty document is created for each output, and the requested page objects are deep-copied into it along with everything they reference \u2014 content streams, embedded font programs, image XObjects, annotations. Nothing is rasterised or re-encoded, so text remains selectable and searchable and a scan keeps its original DPI. The side effect is duplication: shared resources are copied into every piece that needs them, which is why a hundred single-page files can add up to more bytes than the document they came from." }), e.jsx("h3", { style: { fontSize: "1.15rem", marginTop: "1.75rem", marginBottom: "0.75rem" }, children: "When a different tool is the right one" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: ["Reach for ", e.jsx("strong", { children: "Organize PDF" }), " when you want one document with some pages removed or reordered, since it shows page thumbnails and saves a single file. Reach for ", e.jsx("strong", { children: "Merge PDF" }), " to go the other way and glue documents together. If what you actually want is a picture of each page rather than a PDF of each page, use ", e.jsx("strong", { children: "PDF to JPG" }), " or ", e.jsx("strong", { children: "PDF to PNG" }), ", which render pages at one of five fixed scales from 1x to 6x. And if dropping the file here only produces an ", e.jsx("em", { children: "Invalid PDF file" }), " alert and sends you back to the drop zone, it is probably encrypted \u2014 ", e.jsx("strong", { children: "Unlock PDF" }), " first."] })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: H.map((t, r) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: t.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: t.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: t.desc })] }, r)) })] })] }) });
};
export {
  ne as default
};
