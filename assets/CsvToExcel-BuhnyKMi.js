import { r as l, j as e, L as p } from "./index-OUpguYFg.js";
import { R as u } from "./RelatedTools-dQ1AUZ0r.js";
import { T as f } from "./ToolLayout-CuKFTkh4.js";
import { F as g } from "./FileUploader-Dhw1e8vw.js";
import { r as y, a as b } from "./xlsx-C4NO0l3T.js";
import { F as x } from "./FileSaver.min-DzHDzKVl.js";
import { c as w } from "./tools-B3OPepIK.js";
import { D as v } from "./download-DqlBxbZM.js";
import { Z as k } from "./zap-C2842ZXr.js";
import { C as j } from "./check-CzYxOQpM.js";
import { S } from "./shield-BrCBnKXk.js";
import "./index-CBYUSgtG.js";
const C = [{ title: "Finds The Delimiter Itself", desc: "Comma, semicolon, tab and pipe separated files are all detected automatically \u2014 useful because exports from European systems routinely use semicolons, which Excel then refuses to split into columns on import.", icon: e.jsx(k, { color: "var(--primary)", size: 24 }) }, { title: "Read As UTF-8, Not Guesswork", desc: "The file is decoded as UTF-8 and any byte-order mark is stripped, so accented names, CJK text and emoji survive into the workbook instead of arriving as the mojibake a code-page guess produces.", icon: e.jsx(j, { color: "var(--primary)", size: 24 }) }, { title: "Built In The Browser", desc: "The XLSX workbook is assembled in this tab and handed straight to your downloads. A customer list or a finance export is never transmitted, which matters more here than on most tools.", icon: e.jsx(S, { color: "var(--primary)", size: 24 }) }], T = [{ question: "My leading zeros are gone. What happened?", answer: "Anything that looks like a number is stored as one, so a postcode written 01234 is held as the number 1234 with 01234 kept only as its display format. It looks right on screen, but sort the column, run a formula against it or re-export and the zeros are gone. There is no import wizard here to mark a column as text. If leading zeros are load-bearing \u2014 postcodes, account numbers, product codes \u2014 prefix each value with an apostrophe in the CSV, or import the file through Excel's own Text Import wizard where you can set the column type." }, { question: "A long ID column came out with the wrong digits.", answer: "That is floating-point precision, and it is worth knowing exactly where the cliff is. Numeric values are held as doubles, which are exact only to about 15 significant digits. A 19-digit identifier is therefore rounded \u2014 the last few digits change to zeros or shift by one \u2014 and the corruption is silent. Any identifier longer than 15 digits, which includes most credit-card numbers, many order references and all 64-bit IDs, must be treated as text rather than converted here." }, { question: "How are dates handled?", answer: "Text that looks like a date is converted to an Excel date serial, and ambiguous formats are read as month-first. That means 03/04/2024 becomes 4 March rather than 3 April, which silently mangles data exported from anywhere using day-first ordering. Unambiguous ISO dates written year-month-day are safe. If your file uses day-first dates, convert them to ISO before running them through." }, { question: "Which delimiters does it recognise?", answer: "Comma, semicolon, tab and pipe are all detected automatically from the content, so you do not need to tell it which you have. This matters most for semicolon files, which are the norm in locales where the comma is the decimal separator, and which Excel will happily open as a single unsplit column if you double-click them. The picker is narrower than the parser, though: it only accepts files ending in .csv, so a tab-separated .tsv or a plain .txt export has to be renamed before you can drop it in." }, { question: "What does the resulting workbook look like?", answer: "One worksheet named Sheet1, containing your rows exactly as they were ordered in the file. There is no header styling, no frozen top row, no column auto-width, no table formatting and no formulas \u2014 it is your data placed into cells. The file is a genuine XLSX, so Excel, LibreOffice, Numbers and Google Sheets all open it without a compatibility warning." }, { question: "Are quoted fields and embedded commas handled correctly?", answer: "Yes. Standard CSV quoting is parsed properly: a field wrapped in double quotes may contain the delimiter, line breaks and doubled quote characters, and all of it lands in a single cell. The common failure is a file that is not actually valid CSV \u2014 an unclosed quote somewhere in the middle will throw the parser out of alignment for every row after it." }, { question: "Will accented characters and emoji survive?", answer: "Yes, provided the CSV really is UTF-8, which almost everything produces today. The file is decoded as UTF-8 and a byte-order mark is stripped if present. A file saved in a legacy single-byte encoding will come through with replacement characters, because there is no encoding selector \u2014 re-save it as UTF-8 first." }, { question: "How big a file can it handle?", answer: "There is no coded limit and nothing is chunked: the whole file is read into memory as a string, parsed into a workbook object and serialised again, so peak memory runs to several times the file size. Files in the low tens of megabytes are fine on a desktop; well beyond that the tab becomes unresponsive, because the work happens on the main thread rather than a worker." }, { question: "Can I convert several files at once?", answer: "No, one file per run \u2014 there is no multi-select and no way to merge several CSVs into one workbook with a sheet each. Convert them one at a time, or if you need many sheets in a single workbook, combine them in a spreadsheet application afterwards." }, { question: "Do I need Excel installed?", answer: "Not to do the conversion, which happens entirely in your browser. You will need something that reads XLSX to open the result, but that includes free options \u2014 LibreOffice Calc, Google Sheets, Numbers on macOS, and Excel's own web version." }], D = () => {
  const [o, n] = l.useState(null), [a, r] = l.useState(false), d = () => {
    if (!o) return;
    r(true);
    const t = new FileReader();
    t.onload = (i) => {
      try {
        const s = i.target.result, c = y(s, { type: "string" }), h = b(c, { bookType: "xlsx", type: "array" }), m = new Blob([h], { type: "application/octet-stream" });
        x.saveAs(m, o.name.replace(/\.csv$/i, "") + ".xlsx"), r(false);
      } catch (s) {
        console.error(s), alert("Conversion failed. Please make sure the file is a valid, non-empty CSV."), r(false);
      }
    }, t.onerror = () => {
      alert("Could not read the file. Please try selecting it again."), r(false);
    }, t.readAsText(o, "UTF-8");
  };
  return e.jsxs(f, { title: "CSV to Excel Converter", description: "Convert CSV files to Excel (XLSX) format.", seoTitle: "CSV to Excel Converter - Convert CSV to XLSX Online", seoDescription: "Convert CSV to Excel (XLSX) online for free. Transform comma-separated values into formatted spreadsheets instantly.", faqs: T, children: [e.jsx("div", { className: "tool-workspace", style: { maxWidth: "600px", margin: "0 auto" }, children: o ? e.jsxs("div", { className: "tool-file-card", style: { background: "white", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", textAlign: "center" }, children: [e.jsx("div", { style: { marginBottom: "2rem", fontWeight: "bold" }, children: o.name }), e.jsxs("button", { onClick: d, disabled: a, className: "tool-btn-primary tool-action-btn", style: { width: "100%", padding: "1rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", cursor: "pointer", fontWeight: "bold", display: "flex", alignItems: "center", justifyContent: "center", gap: "0.5rem" }, children: [a ? e.jsx(p, { className: "spin", size: 20 }) : e.jsx(v, { size: 20 }), a ? "Converting..." : "Convert to Excel"] }), e.jsx("div", { style: { marginTop: "1rem" }, children: e.jsx("button", { className: "tool-reset-btn", onClick: () => n(null), style: { background: "none", border: "none", color: "#64748b", textDecoration: "underline", cursor: "pointer" }, children: "Cancel" }) })] }) : e.jsx(g, { onFileSelect: n, accept: { "text/csv": [".csv"] }, icon: w, label: "Drop CSV file here" }) }), e.jsx("div", { className: "tool-content", style: { marginTop: "4rem" }, children: e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(u, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About CSV to Excel Converter" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Drop a ", e.jsx("code", { children: ".csv" }), " file and get a real ", e.jsx("code", { children: ".xlsx" }), " workbook back. The file is read as UTF-8, the delimiter is detected from the content \u2014 comma, semicolon, tab or pipe \u2014 the rows are parsed into a worksheet named Sheet1, and the workbook is serialised and downloaded, all inside this browser tab with the data never sent anywhere. The picker only lists ", e.jsx("code", { children: ".csv" }), " files, so a tab- or pipe-separated export saved under another extension needs renaming first."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Why convert at all, rather than just opening the CSV" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "A CSV has no types, no formatting and no second sheet \u2014 it is text with separators, and every application that opens one has to guess what the columns mean. Converting to XLSX fixes the interpretation once, into a file that opens the same way for everyone you send it to. It also sidesteps the most irritating everyday failure: double-clicking a semicolon-separated export in Excel, which loads the whole thing into a single column because Excel expects the delimiter to match your system locale. Here the delimiter is detected from the file itself, so comma, semicolon, tab and pipe files all split correctly." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Read this before converting an ID column" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Type inference is the part that quietly damages data, and it is worth being specific about how. Any field that looks numeric is stored as a number. A postcode of ", e.jsx("code", { children: "01234" }), " becomes the number 1234 \u2014 the original text is retained as the cell's display format so it still", e.jsx("em", { children: " looks" }), " right, but sorting, filtering, lookups and any re-export all see 1234. Worse, numbers are held as doubles and are exact only to roughly ", e.jsx("strong", { children: "15 significant digits" }), ", so a 19-digit order reference or a long account number is silently rounded, with the final digits changed. If a column is an identifier rather than a quantity, that column needs to be text."] }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Dates carry the same class of risk in a different form. Date-looking text becomes an Excel date serial, and ambiguous slash-separated dates are interpreted ", e.jsx("strong", { children: "month first" }), " \u2014 so", e.jsx("code", { children: " 03/04/2024" }), " lands as 4 March, not 3 April. Nothing warns you. Exports from systems outside the United States are the usual casualty. Writing dates in ISO form, year first, removes the ambiguity entirely and is worth doing at the source."] }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "There is no import wizard here to override any of this, because the conversion is a single step rather than a dialogue. Two reliable workarounds: prefix a value with an apostrophe in the CSV to force it to text, or, when a whole file is full of identifiers, use Excel's own Text Import wizard where each column's type can be set explicitly before the data lands." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "What the output workbook is and is not" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "It is a genuine XLSX \u2014 the zipped XML format Excel has used since 2007 \u2014 so it opens cleanly in Excel, LibreOffice Calc, Numbers and Google Sheets with no compatibility prompt. It contains one sheet, your rows in their original order, and nothing else: no bold header row, no frozen panes, no auto-fitted columns, no table styling, no formulas and no charts. Quoted fields are parsed properly, so a value containing commas, quotes or line breaks stays in one cell." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Encoding, size and privacy" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: "The file is decoded as UTF-8 with any byte-order mark removed, which keeps accented names, CJK text and emoji intact; a file saved in a legacy single-byte encoding will arrive with replacement characters and should be re-saved first. Everything is held in memory at once and processed on the main thread, so low tens of megabytes are comfortable and much larger files will hang the tab. Because no request is made at any stage, a file full of customer records or payroll figures stays on your machine \u2014 which is the main reason to prefer a page like this over a service that asks you to upload first. To go the other way, use the Excel to CSV converter." })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: C.map((t, i) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: t.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: t.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: t.desc })] }, i)) })] }) })] });
};
export {
  D as default
};
