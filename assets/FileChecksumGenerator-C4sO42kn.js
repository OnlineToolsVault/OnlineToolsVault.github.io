import { r as o, j as e, L as A } from "./index-OUpguYFg.js";
import { R as C } from "./RelatedTools-dQ1AUZ0r.js";
import { T as B } from "./ToolLayout-CuKFTkh4.js";
import { F as D } from "./FileUploader-Dhw1e8vw.js";
import { C as h } from "./index-Bp_hnjmk.js";
import { f as j } from "./tools-B3OPepIK.js";
import { C as M } from "./check-CzYxOQpM.js";
import "./index-CBYUSgtG.js";
import "./_commonjs-dynamic-modules-TDtrdbi3.js";
import "./___vite-browser-external_commonjs-proxy-CTUmEzTL.js";
import "./__vite-browser-external-Dk_eJUSQ.js";
import "./shield-BrCBnKXk.js";
const I = [{ title: "Three digests from one read", desc: "MD5, SHA-1 and SHA-256 are fed the same bytes as the file streams past, so you get all three for the cost of reading the file once instead of three separate passes.", icon: e.jsx(M, { color: "var(--primary)", size: 24 }) }, { title: "Streamed in 4 MB slices", desc: "The file is read a slice at a time and each slice is folded into the running digests, so memory use stays flat whether you hash a 2 KB config file or a 40 GB disk image.", icon: e.jsx(j, { color: "var(--primary)", size: 24 }) }, { title: "Content only, nothing else", desc: "The digest depends on the bytes and nothing more. Renaming the file, moving it, or changing its timestamp leaves all three values identical, which is exactly what makes them useful for verification.", icon: e.jsx(A, { color: "var(--primary)", size: 24 }) }], z = [{ question: "Which of the three should I use?", answer: "Use whichever one the source you are checking against published \u2014 a checksum is only useful compared against a reference value. If you get to choose, use SHA-256. MD5 and SHA-1 are both broken for collision resistance, meaning someone can deliberately construct two different files with the same digest, and neither should be trusted where an attacker has any influence over the file. All three remain perfectly good at catching accidental corruption." }, { question: "How do I compare the result with a published checksum?", answer: "Copy the value from the row that matches the algorithm the publisher used and compare it character by character with theirs. Case does not matter \u2014 the digest is a number written in hexadecimal, and this page prints it lowercase while Windows PowerShell prints uppercase. Comparing the first and last six characters catches essentially every real mismatch; pasting both into a text comparison tool catches the rest." }, { question: "How do I get the same value from a terminal?", answer: "On macOS and Linux, shasum -a 256 filename gives SHA-256 and shasum -a 1 gives SHA-1; md5 on macOS and md5sum on Linux give MD5. On Windows, Get-FileHash filename -Algorithm SHA256 works in PowerShell and certutil -hashfile filename SHA256 works in the classic command prompt. All of them should produce exactly the string shown here." }, { question: "Why is it not instant, and how long will a big file take?", answer: "The digests are computed in JavaScript rather than by your CPU hashing instructions, which costs roughly a factor of ten in speed. In practice all three algorithms together run in the tens of megabytes per second, so a 1 GB file takes something like half a minute and a 4 GB installer a couple of minutes. The progress percentage reflects bytes read, and the page stays responsive throughout because the work is broken into slices." }, { question: "Does renaming the file change the hash?", answer: "No. Only the contents are hashed \u2014 the file name, its folder, its modification date and its permissions are all invisible to the calculation. Two identical copies with different names produce identical digests, and that is precisely the property that lets a publisher post one checksum for a file everybody saves under a different name." }, { question: "My hash does not match the one on the download page. What now?", answer: "Re-download first: a truncated or interrupted transfer is by far the most common cause, and comparing the byte count on the download page against the file size is a quick sanity check. After that, make sure you are hashing the same thing the publisher hashed \u2014 the .zip they shipped rather than a file you extracted from it \u2014 and that you are reading the right algorithm from their page. Text files transferred through tools that rewrite line endings will also hash differently while looking identical." }, { question: "Does a matching checksum prove the file is genuine?", answer: "It proves the file matches the checksum, which is a weaker claim than it sounds. If an attacker can alter the download they can usually alter the checksum published next to it. A checksum protects against accidental corruption and against a mirror serving something stale; proving who produced a file requires a cryptographic signature, such as a GPG or code-signing check, which this tool does not perform." }, { question: "Is my file uploaded to a server?", answer: "No. The file is read in slices by your browser and never leaves the machine \u2014 nothing is transmitted, and there is no server-side component to receive it. That also means there is no upload wait, which is the main reason hashing a multi-gigabyte image is practical here at all." }, { question: "Can I hash a folder or several files at once?", answer: "No, one file per run. There is also no field for pasting an expected value, so the comparison is yours to make. If you need to fingerprint a whole directory, archive it first and hash the archive, keeping in mind that two archives of the same folder can differ in their bytes \u2014 and therefore in their digests \u2014 because of timestamps and entry ordering." }], S = 4 * 1024 * 1024, K = () => {
  const [f, p] = o.useState(null), [c, n] = o.useState(null), [y, d] = o.useState(false), [H, l] = o.useState(0), [m, u] = o.useState(null), b = () => {
    p(null), n(null), u(null), l(0);
  }, T = (t) => {
    p(t), n(null), u(null), l(0), d(true);
    const a = (s) => {
      d(false), n(null), u(s);
    }, x = h.algo.MD5.create(), w = h.algo.SHA1.create(), v = h.algo.SHA256.create();
    let i = 0;
    const k = () => {
      const s = new FileReader();
      s.onerror = () => a("Could not read this file. It may have moved or changed since you selected it."), s.onabort = () => a("Reading was cancelled."), s.onload = (g) => {
        try {
          const r = h.lib.WordArray.create(g.target.result);
          x.update(r), w.update(r), v.update(r);
        } catch (r) {
          console.error(r), a("This file is too large to hash in your browser. Try a smaller file.");
          return;
        }
        if (i += S, i < t.size) {
          l(Math.round(i / t.size * 100)), setTimeout(k, 0);
          return;
        }
        l(100);
        try {
          n({ MD5: x.finalize().toString(), "SHA-1": w.finalize().toString(), "SHA-256": v.finalize().toString() });
        } catch (r) {
          console.error(r), a("Could not finish hashing this file.");
          return;
        }
        d(false);
      };
      try {
        s.readAsArrayBuffer(t.slice(i, i + S));
      } catch (g) {
        console.error(g), a("Could not read this file.");
      }
    };
    k();
  };
  return e.jsxs(B, { title: "File Checksum Generator", description: "Generate MD5, SHA-1, and SHA-256 checksums for any file.", seoTitle: "File Checksum Generator - MD5 SHA-256 Hash", seoDescription: "Calculate file hash online. Verify file integrity with MD5, SHA-1, SHA-256 checksums.", faqs: z, children: [e.jsx("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: !f || !c && !y && !m ? e.jsx(D, { onFileSelect: T, icon: j, label: "Drop file to calculate hash" }) : e.jsxs("div", { style: { background: "white", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)" }, children: [e.jsx("div", { style: { marginBottom: "2rem", textAlign: "center", fontWeight: "bold" }, children: f.name }), y && e.jsxs("div", { style: { textAlign: "center", padding: "2rem" }, children: [e.jsx(A, { className: "spin", size: 32, style: { margin: "0 auto 1rem auto" } }), e.jsxs("p", { children: ["Calculating Hashes... ", H, "%"] })] }), m && e.jsxs("div", { style: { textAlign: "center", padding: "1rem" }, children: [e.jsx("p", { style: { color: "#dc2626", marginBottom: "1rem" }, children: m }), e.jsx("button", { onClick: b, style: { color: "#64748b", background: "none", border: "none", textDecoration: "underline", cursor: "pointer" }, children: "Try another file" })] }), c && e.jsxs("div", { style: { display: "grid", gap: "1.5rem" }, children: [Object.entries(c).map(([t, a]) => e.jsxs("div", { children: [e.jsx("div", { style: { fontWeight: "bold", marginBottom: "0.25rem", color: "var(--primary)" }, children: t }), e.jsx("div", { style: { display: "flex", gap: "0.5rem" }, children: e.jsx("input", { readOnly: true, value: a, style: { flex: 1, padding: "0.5rem", background: "#f8fafc", border: "1px solid var(--border)", borderRadius: "0.25rem", fontFamily: "monospace" } }) })] }, t)), e.jsx("div", { style: { textAlign: "center", marginTop: "1rem" }, children: e.jsx("button", { onClick: b, style: { color: "#64748b", background: "none", border: "none", textDecoration: "underline", cursor: "pointer" }, children: "Calculate Another" }) })] })] }) }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(C, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About File Checksum Generator" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Pick a file and this page reads it and prints its MD5, SHA-1 and SHA-256 digests. A digest is a fixed-length fingerprint of a file's exact bytes: change one bit anywhere and roughly half the output characters change. That is what makes it useful for confirming a download arrived intact, that a backup copy still matches the original, or that two files you suspect are duplicates really are." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "How the file is read" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "The file is sliced into 4 MB chunks and each chunk is folded into all three running digests before the next one is read, so the whole file is never held in memory at once and the progress bar can move. Hashing algorithms are designed to work this way \u2014 they consume a stream and keep only a small internal state \u2014 which is why a 40 GB disk image uses no more memory than a small text file, and only takes longer." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "All three digests are computed in the same pass rather than by reading the file three times. The arithmetic runs in JavaScript rather than in your processor's dedicated hashing instructions, so expect tens of megabytes per second rather than the hundreds a native command-line tool manages. The browser's own crypto API is faster but offers no MD5 and no streaming interface, which is why it is not used here." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Reading the three values" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "MD5 is 32 hexadecimal characters, SHA-1 is 40 and SHA-256 is 64. They are shown in lowercase; other tools may print uppercase, and the comparison is case-insensitive either way. Match the algorithm to whatever the publisher listed rather than picking a favourite, because a SHA-256 digest tells you nothing about an MD5 reference. Where you have a free choice, prefer SHA-256: MD5 and SHA-1 can both be deliberately collided, so they are fine for spotting a corrupted transfer and unsuitable for anything adversarial." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "What a checksum does not tell you" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: "A matching digest proves the bytes match the reference value. It does not prove who made the file, and if the same site serves both the download and the checksum, an attacker who can change one can usually change the other. Authenticity needs a signature, not a hash. This page also has no field for pasting an expected value and no folder mode \u2014 it produces the numbers, and comparing them is left to you. If what you actually want is a hash of some text rather than a file, the Hash Generator takes typed input directly." })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: I.map((t, a) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: t.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: t.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: t.desc })] }, a)) })] })] });
};
export {
  K as default
};
