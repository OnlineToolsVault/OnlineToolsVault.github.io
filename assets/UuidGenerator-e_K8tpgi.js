import { r as d, j as e } from "./index-BtmU1OS0.js";
import { R as b } from "./RelatedTools-GVPazTWJ.js";
import { T as v } from "./ToolLayout-CgcEif7J.js";
import { R as p } from "./tools-DOXC7sEs.js";
import { C as y } from "./check-G7hwFc4v.js";
import { C as g } from "./copy-BC22e6PQ.js";
import "./shield-C_IpXjfc.js";
const a = [];
for (let r = 0; r < 256; ++r) a.push((r + 256).toString(16).slice(1));
function w(r, t = 0) {
  return (a[r[t + 0]] + a[r[t + 1]] + a[r[t + 2]] + a[r[t + 3]] + "-" + a[r[t + 4]] + a[r[t + 5]] + "-" + a[r[t + 6]] + a[r[t + 7]] + "-" + a[r[t + 8]] + a[r[t + 9]] + "-" + a[r[t + 10]] + a[r[t + 11]] + a[r[t + 12]] + a[r[t + 13]] + a[r[t + 14]] + a[r[t + 15]]).toLowerCase();
}
let c;
const j = new Uint8Array(16);
function U() {
  if (!c) {
    if (typeof crypto > "u" || !crypto.getRandomValues) throw new Error("crypto.getRandomValues() not supported. See https://github.com/uuidjs/uuid#getrandomvalues-not-supported");
    c = crypto.getRandomValues.bind(crypto);
  }
  return c(j);
}
const I = typeof crypto < "u" && crypto.randomUUID && crypto.randomUUID.bind(crypto), u = { randomUUID: I };
function k(r, t, s) {
  var _a;
  r = r || {};
  const n = r.random ?? ((_a = r.rng) == null ? void 0 : _a.call(r)) ?? U();
  if (n.length < 16) throw new Error("Random bytes length must be >= 16");
  return n[6] = n[6] & 15 | 64, n[8] = n[8] & 63 | 128, w(n);
}
function R(r, t, s) {
  return u.randomUUID && !r ? u.randomUUID() : k(r);
}
const C = [{ question: "How do I tell a v4 UUID from the other versions by eye?", answer: "Look at two positions. The first character of the third group is the version, so a v4 always reads xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx. The first character of the fourth group is the variant and will be 8, 9, a or b. Those six fixed bits are why a v4 carries 122 random bits rather than 128. If the third group starts with a 1 or a 7 you are looking at a time-ordered identifier from a different generator." }, { question: "Where does the randomness come from?", answer: "From the operating system, by way of the browser. The generator calls crypto.randomUUID where it exists and falls back to crypto.getRandomValues, both of which are cryptographically secure sources \u2014 Math.random is never involved. That matters if an identifier ever doubles as an unguessable handle, such as a share link or a password-reset token, because a predictable generator would make those enumerable." }, { question: "How likely is a collision, really?", answer: "With 122 random bits you would need to generate on the order of 2.7 quintillion UUIDs before reaching a fifty-fifty chance of a single duplicate. At a million per second that is longer than the age of the universe. In practice the duplicates people actually hit come from a broken generator seeded identically across processes, or from a value being copied rather than generated, not from exhausting the space." }, { question: "Should I use a v4 UUID as a primary key?", answer: "In a distributed system where clients mint their own IDs, yes \u2014 that independence is the whole point. Be aware of the storage cost though. Random keys arrive in no particular order, so on a clustered index such as InnoDB's or SQL Server's every insert lands at a random page and the index fragments. Storing the value as 16 raw bytes rather than a 36-character string, or switching to a time-ordered scheme like UUIDv7 or ULID, both help." }, { question: "Can I get uppercase, braces, or a version other than 4?", answer: "Not from this page. Output is always lowercase, hyphenated and unbraced, which is the canonical form from RFC 9562 and what PostgreSQL, MySQL and most libraries expect. Windows and .NET tooling often shows the same value wrapped in braces and uppercased; those are display conventions for identical bytes, so converting is a case change. Versions 1, 5 and 7 need a MAC address, a namespace or a timestamp respectively and are not generated here." }, { question: "Do the identifiers regenerate if I change the quantity?", answer: "Not on their own. Editing the quantity field only sets how many the next run produces \u2014 press Generate to actually create them. The count is clamped between 1 and 100, and each run replaces the whole list, so copy what you need before generating again. Copy places every identifier on its own line, ready to paste into a seed file or a spreadsheet column." }], x = () => {
  const [r, t] = d.useState([]), [s, n] = d.useState(1), [h, l] = d.useState(false);
  d.useEffect(() => {
    m();
  }, []);
  const m = () => {
    const o = Array.from({ length: s }, () => R());
    t(o), l(false);
  }, f = () => {
    navigator.clipboard.writeText(r.join(`
`)), l(true), setTimeout(() => l(false), 2e3);
  };
  return e.jsx(v, { title: "UUID Generator v4", description: "Generate random version 4 UUIDs instantly.", seoTitle: "UUID Generator Online - Random v4 GUID Maker", seoDescription: "Free online UUID v4 generator. Create random, universally unique identifiers (GUIDs) instantly for your software projects.", faqs: C, children: e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [e.jsxs("div", { style: { background: "white", borderRadius: "1rem", border: "1px solid var(--border)", padding: "2rem", boxShadow: "0 4px 6px -1px rgba(0,0,0,0.05)" }, children: [e.jsxs("div", { className: "tool-controls", style: { display: "flex", flexWrap: "wrap", gap: "1.5rem", alignItems: "center", justifyContent: "center", marginBottom: "2rem", paddingBottom: "2rem", borderBottom: "1px solid var(--border)" }, children: [e.jsxs("div", { style: { display: "flex", alignItems: "center", gap: "0.5rem" }, children: [e.jsx("label", { style: { fontWeight: "500", color: "var(--foreground)" }, children: "Quantity:" }), e.jsx("input", { type: "number", "aria-label": "How many UUIDs", min: "1", max: "100", value: s, onChange: (o) => n(Math.min(100, Math.max(1, parseInt(o.target.value) || 1))), style: { width: "80px", padding: "0.5rem", borderRadius: "0.5rem", border: "1px solid var(--border)", textAlign: "center", fontWeight: "bold" } })] }), e.jsxs("div", { style: { display: "flex", gap: "1rem" }, children: [e.jsxs("button", { onClick: m, className: "tool-btn-primary", style: { padding: "0.75rem 1.5rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", fontWeight: "bold", cursor: "pointer", display: "flex", alignItems: "center", gap: "0.5rem" }, children: [e.jsx(p, { size: 18 }), " Generate"] }), e.jsxs("button", { onClick: f, className: "tool-btn-secondary", style: { padding: "0.75rem 1.5rem", borderRadius: "0.5rem", background: "white", border: "1px solid var(--border)", color: "var(--foreground)", fontWeight: "bold", cursor: "pointer", display: "flex", alignItems: "center", gap: "0.5rem" }, children: [h ? e.jsx(y, { size: 18, color: "var(--primary)" }) : e.jsx(g, { size: 18 }), h ? "Copied!" : "Copy"] })] })] }), e.jsx("div", { className: "tool-output-area", style: { maxHeight: "400px", overflowY: "auto" }, children: r.map((o, i) => e.jsxs("div", { style: { padding: "1rem", background: "#f8fafc", border: "1px solid var(--border)", marginBottom: "0.5rem", borderRadius: "0.5rem", fontFamily: "monospace", fontSize: "1.1rem", color: "#334155", display: "flex", alignItems: "center", justifyContent: "space-between" }, children: [e.jsx("span", { children: o }), e.jsxs("span", { style: { fontSize: "0.75rem", color: "#94a3b8", userSelect: "none" }, children: ["#", i + 1] })] }, i)) })] }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(b, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About UUID Generator v4" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "A version 4 UUID is 128 bits, of which 122 are drawn from a secure random source and 6 are fixed markers that record the version and variant. It is written as 32 hexadecimal digits in five hyphenated groups \u2014 8-4-4-4-12, 36 characters in total. Set a quantity, press Generate, and the list below is yours to copy." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Unique without a coordinator" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "The appeal of a random identifier is that nothing has to agree on it. An auto-incrementing integer needs a single authority to hand out the next value, which becomes awkward the moment you have several database shards, an offline-capable mobile client, or a queue consumer that must create a record before the write lands. A v4 UUID can be minted anywhere \u2014 in a test, in the browser, in a Lambda \u2014 and stays valid when the rows are merged." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "What you give up" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Randomness costs you ordering and compactness. Rows keyed by v4 cannot be sorted by creation time without a separate timestamp column, and because consecutive inserts scatter across the index, write throughput on a large clustered table degrades in a way sequential keys avoid. The text form is also 36 characters against 4 or 8 bytes for an integer, which shows up in every foreign key, every index and every log line. Version 7, which puts a millisecond timestamp in the high bits, exists precisely to recover the ordering." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Generated here, kept here" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Generation is a call to the browser's own crypto API \u2014 there is no server involved, nothing is recorded, and the same identifier is never handed to anyone else. That also means the page keeps working with the network disconnected, which is convenient when you just need a hundred keys for a fixture file on a plane." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Reasonable and unreasonable uses" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Good fits: primary keys in distributed stores, idempotency keys on payment and webhook endpoints, correlation IDs threaded through logs and traces, filenames for uploaded assets, and seed data. Poor fits: anything a person must read aloud or retype, anything needing to sort chronologically, and any long-lived credential \u2014 an API key should be revocable and scoped, which a bare identifier is not. If you need a fingerprint of some content rather than a fresh random value, the ", e.jsx("strong", { children: "Hash Generator" }), " is the tool for that."] })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: x.features.map((o, i) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: o.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: o.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: o.desc })] }, i)) })] })] }) });
};
x.features = [{ title: "One To A Hundred Per Run", desc: "Set the quantity and every press of Generate replaces the list with a fresh batch, numbered so you can keep your place while filling out a fixture or a seed script.", icon: e.jsx(p, { color: "var(--primary)", size: 24 }) }, { title: "Canonical RFC 9562 Form", desc: "Lowercase, hyphenated, unbraced, with the version 4 marker in the third group and the variant bits in the fourth \u2014 the exact shape a uuid column or a Java UUID.fromString expects.", icon: e.jsx(y, { color: "var(--primary)", size: 24 }) }, { title: "Copy Ready For A Column", desc: "The Copy button puts the whole batch on the clipboard, one identifier per line, so it pastes cleanly into a spreadsheet, a SQL insert or a test file.", icon: e.jsx(g, { color: "var(--primary)", size: 24 }) }];
export {
  x as default
};
