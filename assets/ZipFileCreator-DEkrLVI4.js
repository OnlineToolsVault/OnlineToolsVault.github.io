import { r as c, j as e, X as x, L as j } from "./index-OUpguYFg.js";
import { R as k } from "./RelatedTools-dQ1AUZ0r.js";
import { T as I } from "./ToolLayout-CuKFTkh4.js";
import { F as P } from "./FileUploader-Dhw1e8vw.js";
import { J as Z } from "./jszip.min-BycgvNgQ.js";
import { F as S } from "./FileSaver.min-DzHDzKVl.js";
import { A as z } from "./tools-B3OPepIK.js";
import { D as C } from "./download-DqlBxbZM.js";
import { S as A } from "./shield-BrCBnKXk.js";
import { F } from "./files-CmqaNgok.js";
import { Z as T } from "./zap-C2842ZXr.js";
import "./index-CBYUSgtG.js";
import "./_commonjs-dynamic-modules-TDtrdbi3.js";
const D = [{ title: "Built Entirely In The Tab", desc: "Files are read from disk by the browser, compressed in memory and handed straight back as a download. There is no upload, no queue and no temporary copy on a server, so contracts, exports and client work never travel.", icon: e.jsx(A, { color: "var(--primary)", size: 24 }) }, { title: "Add In Batches, Prune Before Packing", desc: "Drop files as many times as you like and they accumulate in a list showing each size in kilobytes. Remove individual entries or clear the lot, and set the archive name, before anything is compressed.", icon: e.jsx(F, { color: "var(--primary)", size: 24 }) }, { title: "Deflate At Level Six, Losslessly", desc: "The standard ZIP algorithm at its balanced setting \u2014 the same trade-off most archivers default to. Every byte is recoverable exactly; nothing is re-encoded, resampled or degraded on the way in.", icon: e.jsx(T, { color: "var(--primary)", size: 24 }) }], f = () => {
  const [s, o] = c.useState([]), [a, h] = c.useState(false), [m, g] = c.useState("archive"), b = (t) => {
    Array.isArray(t) ? o((r) => [...r, ...t]) : o((r) => [...r, t]);
  }, v = async () => {
    if (s.length !== 0) {
      h(true);
      try {
        const t = new Z(), r = /* @__PURE__ */ new Set();
        s.forEach((n) => {
          let i = n.name;
          if (r.has(i)) {
            const l = i.lastIndexOf("."), u = l > 0 ? i.slice(0, l) : i, y = l > 0 ? i.slice(l) : "";
            let d = 1;
            for (; r.has(`${u} (${d})${y}`); ) d += 1;
            i = `${u} (${d})${y}`;
          }
          r.add(i), t.file(i, n);
        });
        const p = await t.generateAsync({ type: "blob", compression: "DEFLATE", compressionOptions: { level: 6 } });
        S.saveAs(p, `${m}.zip`);
      } catch {
        alert("Error creating ZIP");
      } finally {
        h(false);
      }
    }
  }, w = (t) => {
    o((r) => r.filter((p, n) => n !== t));
  };
  return e.jsxs(I, { title: "ZIP File Creator", description: "Create ZIP archives from multiple files online.", seoTitle: "ZIP File Creator - Online Archiver", seoDescription: "Create ZIP files online. Combine multiple files into a single ZIP archive. Fast and secure client-side processing.", faqs: f.defaultProps.faqs, children: [e.jsxs("div", { style: { maxWidth: "1000px", margin: "0 auto", padding: "2rem", background: "white", borderRadius: "1rem", border: "1px solid var(--border)" }, children: [e.jsx("div", { style: { marginBottom: "2rem" }, children: e.jsx(P, { id: "zip-files-upload", onFileSelect: b, multiple: true, icon: z, label: "Add files to archive" }) }), s.length > 0 && e.jsxs("div", { children: [e.jsxs("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "1rem" }, children: [e.jsxs("div", { style: { fontWeight: "bold" }, children: ["Files (", s.length, ")"] }), e.jsxs("div", { style: { display: "flex", gap: "0.5rem" }, children: [e.jsx("input", { id: "zip-filename-input", type: "text", value: m, onChange: (t) => g(t.target.value), placeholder: "archive name", style: { padding: "0.5rem", borderRadius: "0.5rem", border: "1px solid var(--border)" } }), e.jsx("span", { style: { alignSelf: "center", fontWeight: "bold" }, children: ".zip" })] })] }), e.jsx("div", { style: { maxHeight: "300px", overflow: "auto", border: "1px solid var(--border)", borderRadius: "0.5rem", marginBottom: "2rem" }, children: s.map((t, r) => e.jsxs("div", { style: { display: "flex", justifyContent: "space-between", padding: "0.75rem", borderBottom: "1px solid #eee", background: "white" }, children: [e.jsxs("span", { children: [t.name, " ", e.jsxs("span", { style: { color: "#94a3b8", fontSize: "0.9rem" }, children: ["(", (t.size / 1024).toFixed(1), " KB)"] })] }), e.jsx("button", { id: `zip-remove-${r}`, onClick: () => w(r), style: { border: "none", background: "none", color: "#ef4444", cursor: "pointer" }, children: e.jsx(x, { size: 18 }) })] }, r)) }), e.jsxs("button", { id: "create-zip-main-btn", onClick: v, disabled: a, className: "tool-btn-primary", style: { width: "100%", padding: "1rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", cursor: a ? "wait" : "pointer", fontWeight: "bold", display: "flex", justifyContent: "center", alignItems: "center", gap: "0.5rem" }, children: [a ? e.jsx(j, { className: "spin", size: 20 }) : e.jsx(C, { size: 20 }), a ? "Compressing..." : "Download ZIP"] }), e.jsx("div", { style: { marginTop: "2rem", textAlign: "center" }, children: e.jsx("button", { onClick: () => o([]), style: { background: "none", border: "none", color: "#64748b", textDecoration: "underline", cursor: "pointer" }, children: "Clear All" }) })] })] }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(k, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About ZIP File Creator" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Add files in as many batches as you like, review the list, name the archive and press download. The compression runs in this tab and the finished ZIP is handed to your browser as an ordinary download \u2014 there is no upload step, no account and no server copy at any point." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "What Deflate actually does to your files" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Each file is compressed with ", e.jsx("strong", { children: "Deflate" }), " at level six \u2014 the balanced setting almost every archiver uses by default. Deflate works by finding repeated byte sequences and replacing later copies with a short back-reference, then re-encoding the result so common symbols use fewer bits. It is completely lossless: the extracted file is bit-for-bit identical to the original, which is why zipping a photo never degrades it."] }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "That mechanism also explains the results you will see. Anything full of repetition shrinks dramatically \u2014 logs, CSV exports, JSON payloads, SQL dumps and source trees routinely lose 70 to 90 percent. Anything already compressed has had its repetition removed by its own encoder, so JPEGs, PNGs, MP3s, MP4s, most PDFs and nested ZIPs come out essentially the same size, occasionally a few bytes larger once container overhead is added. Zipping a folder of holiday photos is worth doing for the convenience of a single attachment, not for the space." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "The archive is flat, and duplicates are renamed" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Two behaviours are worth knowing before you pack anything important. First, entries are stored under each file's own name with no directory path, so the result is a ", e.jsx("strong", { children: "flat archive" }), " \u2014 files gathered from different folders end up alongside each other rather than in a preserved tree. Second, because a ZIP is keyed by entry name, a second file with a name already used would ordinarily overwrite the first without a word. Instead the duplicate is renamed with a numbered suffix before it is added, so nothing is lost \u2014 but it does mean two files called ", e.jsx("code", { children: "invoice.pdf" }), " arrive as", e.jsx("code", { children: " invoice.pdf" }), " and ", e.jsx("code", { children: "invoice (1).pdf" }), ", and only you know which was which."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Working within the browser's limits" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Everything happens in memory: the source files, the compressed output and the blob handed to the download all coexist while the archive is built. That puts the practical ceiling at whatever your browser will allocate \u2014 often a couple of gigabytes on a desktop and considerably less on a phone \u2014 rather than at any limit set here. Compression also runs on the page's main thread, so a large batch will make the tab unresponsive until it finishes. For tens of gigabytes, or for anything you need to script, a desktop archiver is the right tool." }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Compatibility, and what is missing" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: "Deflate is the original ZIP method and is understood by the built-in extractor on every desktop operating system, so the archive opens for your recipient without extra software \u2014 the reason it remains the sensible default even though newer algorithms compress harder. What is not offered here: password protection or AES encryption, a choice of compression level, split or multi-volume archives, and preserved folder structure. Of those, encryption is the one to plan around. Packing a file into a ZIP hides nothing, so encrypt sensitive contents first and treat the archive purely as a container." })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: D.map((t, r) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: t.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: t.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: t.desc })] }, r)) })] })] });
}, N = [{ question: "Are my files uploaded to build the archive?", answer: "No. The browser reads each file from disk, the compressor runs as JavaScript in this tab, and the finished archive is produced as an in-memory blob that your browser saves like any other download. No request carries the contents anywhere, which is the practical reason to use this rather than a service that asks you to upload first." }, { question: "Does it keep my folder structure?", answer: "No \u2014 the archive is flat. Each entry is stored under the file's own name with no directory path, so files gathered from several folders all land side by side at the top level of the ZIP. If the structure matters, create the archive with your operating system's own compress command instead, which preserves the tree." }, { question: "What happens if two files have the same name?", answer: "Both are kept. A ZIP is keyed by entry name, so adding a second file called report.pdf would normally replace the first one silently. Instead the duplicate is renamed with a numeric suffix \u2014 report (1).pdf \u2014 before it is added, and the numbering continues if there is a third. You will not lose a file, but do check the names in the finished archive so you know which is which." }, { question: "How much smaller will my files get?", answer: "It depends almost entirely on what you are packing. Text, CSV, JSON, XML, logs and source code often compress by 70 to 90 percent because they are full of repetition. Already-compressed formats \u2014 JPEG, PNG, GIF, MP3, MP4, most PDFs, and other ZIPs \u2014 will barely move, sometimes gaining a few bytes of overhead. Packing a folder of photos is about producing one convenient file, not about saving space." }, { question: "Can I set a password on the archive?", answer: "No. ZIP encryption is not supported here, so the archive is readable by anyone who receives it. Do not rely on an extension or an obscure filename for confidentiality. If the contents are sensitive, encrypt them before packing \u2014 the File Encryption Tool will do that \u2014 or use an archiver that supports AES and send the password over a different channel." }, { question: "How big an archive can I make?", answer: "There is no coded ceiling, but every source file and the finished archive all exist in memory at once, so the practical limit is roughly what your browser will allocate \u2014 commonly a couple of gigabytes on a desktop and far less on a phone. Compressing very large files will also freeze the tab while it works, because the work happens on the main thread rather than in a background worker." }, { question: "Can I choose the compression level?", answer: "Not from this page. It uses Deflate at level six, the middle setting most archivers pick by default because the gains above it are small and the time cost is not. If you need maximum compression, or a modern algorithm such as Zstandard or LZMA, use a desktop archiver \u2014 those consistently beat Deflate on large text corpora." }, { question: "Will the ZIP open everywhere?", answer: "Yes. Deflate is the original and universally supported ZIP method, so the output opens with the built-in tools on Windows, macOS and Linux and with every third-party archiver, without anyone needing extra software. That compatibility is exactly why Deflate is still the sensible default despite better algorithms existing." }, { question: "Is there a limit on how many files I can add?", answer: "No count limit is enforced, and files accumulate across multiple drops so you can build the list up gradually. Each one shows its size in the list, and you can remove entries individually or clear everything and start again. Nothing is compressed until you press the download button." }];
f.defaultProps = { faqs: N };
export {
  f as default
};
