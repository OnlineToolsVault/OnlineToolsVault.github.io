import { c as z, r as c, j as e } from "./index-OUpguYFg.js";
import { R as q } from "./RelatedTools-dQ1AUZ0r.js";
import { T as E } from "./ToolLayout-CuKFTkh4.js";
import { u as D } from "./index-CBYUSgtG.js";
import { F as M } from "./FileSaver.min-DzHDzKVl.js";
import { W as p } from "./tools-B3OPepIK.js";
import { D as H } from "./download-DqlBxbZM.js";
import { S as O } from "./shield-BrCBnKXk.js";
/**
* @license lucide-react v0.344.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
const F = z("EyeOff", [["path", { d: "M9.88 9.88a3 3 0 1 0 4.24 4.24", key: "1jxqfv" }], ["path", { d: "M10.73 5.08A10.43 10.43 0 0 1 12 5c7 0 10 7 10 7a13.16 13.16 0 0 1-1.67 2.68", key: "9wicm4" }], ["path", { d: "M6.61 6.61A13.526 13.526 0 0 0 2 12s3 7 10 7a9.74 9.74 0 0 0 5.39-1.61", key: "1jreej" }], ["line", { x1: "2", x2: "22", y1: "2", y2: "22", key: "a6p6uj" }]]), J = [{ title: "Preview matches the export", desc: "The slider blurs the on-screen picture in CSS pixels, and the radius is scaled up by the same factor before the full-resolution export, so the downloaded file looks like what you approved.", icon: e.jsx(p, { color: "var(--primary)", size: 24 }) }, { title: "0 to 50 px of Gaussian blur", desc: "A continuous slider rather than presets. Low values soften a background for an artistic effect; the top of the range dissolves shapes completely.", icon: e.jsx(p, { color: "var(--primary)", size: 24 }) }, { title: "Clean edges, no dark frame", desc: "Border pixels are replicated outward before the blur runs, so the filter has real colour to sample at the edges instead of smearing transparency into a grey border.", icon: e.jsx(F, { color: "var(--primary)", size: 24 }) }, { title: "Nothing is uploaded", desc: "Blurring happens on a canvas in this tab. A screenshot you are blurring because it contains something private never travels anywhere to have that done to it.", icon: e.jsx(O, { color: "var(--primary)", size: 24 }) }], L = [{ question: "Does this blur only part of the image?", answer: "No \u2014 and this is the most important thing to know before you start. The blur is applied uniformly to the **entire** picture. There is no selection rectangle and no brush. It is built for softening a whole image, not for masking one region of it." }, { question: "Then how do I hide one detail in a screenshot?", answer: "Do not use blur for that. The reliable answer is to remove the pixels rather than smear them: crop the sensitive area out of the frame with the Image Cropper, or cover it with a solid opaque rectangle in any image editor. Both destroy the information outright, which blurring at a low radius does not." }, { question: "Is a blurred image safe to publish?", answer: "Treat blur as an aesthetic effect, not as redaction. A light Gaussian blur is a reversible mathematical operation in principle, and short strings like a licence plate, a postcode or a six-digit code have a small enough search space that a determined person can work backwards from a soft blur. Heavy blur destroys far more information, but if the content genuinely matters, remove it instead of obscuring it." }, { question: "Why does the exported file look blurrier than I expected?", answer: "It should look the same, and matching them is a deliberate piece of work. The preview blurs the picture at its on-screen size, which may be a quarter of the real resolution, so the export scales the radius by the same ratio before applying it. Without that step a 10 px blur that looked right on screen would be almost invisible in a full-resolution file." }, { question: "I picked a huge blur on a huge photo and it came out weaker.", answer: "That is a deliberate safety limit. A Gaussian blur needs padding around the image roughly three times the radius, and browsers cap canvas size by both the longest side and the total area \u2014 iOS Safari is the tightest at around 16 megapixels. If the padding will not fit, the tool reduces the radius to what does fit rather than failing to produce a file at all." }, { answer: "The same one you put in, wherever the browser can write it: a JPEG stays a JPEG, a PNG stays a PNG, a WebP stays a WebP. JPEG has no transparency, so the canvas is filled with white first and transparent areas come out white rather than black. Those three are the only formats a canvas can encode \u2014 anything else (a GIF, say) comes back as PNG data while the download keeps its original extension, so rename it or convert it with the Image Converter instead." }, { question: "Can the blur be undone from the downloaded file?", answer: "Not by any ordinary tool, and not at all at high radii \u2014 the information is genuinely averaged away. The honest caveat is the one above: at low radii, blur is a convolution and convolutions can be attacked. If the answer needs to be an unqualified no, crop or cover instead." }, { question: "Is there a file size limit?", answer: "No fixed limit, but very large photos are constrained by canvas memory rather than by file size. The tool works within your browser limits automatically, trimming the blur radius if necessary. On a phone, expect a large photo at maximum blur to take a moment and to be capped more tightly than on a desktop." }, { question: "Does my picture get uploaded?", answer: "No. It is decoded, padded, blurred and re-encoded entirely inside this browser tab, and the result is handed to you as a download. Given that the reason for blurring is usually that the image contains something you would rather not share, that distinction matters more here than on most tools." }], Z = () => {
  const [s, g] = c.useState(null), [l, j] = c.useState(5), [f, k] = c.useState(null), b = c.useRef(null), I = (a) => {
    g(a), k(URL.createObjectURL(a));
  }, T = () => {
    if (!s) return;
    const a = new Image();
    a.onload = () => {
      var _a;
      const r = a.naturalWidth || a.width, o = a.naturalHeight || a.height, y = (_a = b.current) == null ? void 0 : _a.getBoundingClientRect().width, G = y > 0 ? r / y : 1;
      let u = l * G;
      const P = 16384, N = 16 * 1024 * 1024, m = r + o, w = m * m - 4 * (r * o - N), R = w <= 0 ? 0 : Math.floor((Math.sqrt(w) - m) / 4), W = Math.floor((P - Math.max(r, o)) / 2), x = Math.max(0, Math.min(R, W));
      let t = Math.ceil(u * 3);
      t > x && (t = x, u = t / 3);
      const d = document.createElement("canvas");
      d.width = r + t * 2, d.height = o + t * 2;
      const i = d.getContext("2d");
      if (!i) {
        alert("This image is too large for your browser to process. Try a smaller image or a lower blur intensity.");
        return;
      }
      t > 0 && (i.drawImage(a, 0, 0, 1, 1, 0, 0, t, t), i.drawImage(a, r - 1, 0, 1, 1, t + r, 0, t, t), i.drawImage(a, 0, o - 1, 1, 1, 0, t + o, t, t), i.drawImage(a, r - 1, o - 1, 1, 1, t + r, t + o, t, t), i.drawImage(a, 0, 0, r, 1, t, 0, r, t), i.drawImage(a, 0, o - 1, r, 1, t, t + o, r, t), i.drawImage(a, 0, 0, 1, o, 0, t, t, o), i.drawImage(a, r - 1, 0, 1, o, t + r, t, t, o)), i.drawImage(a, t, t);
      const h = document.createElement("canvas");
      h.width = r, h.height = o;
      const n = h.getContext("2d");
      if (!n) {
        alert("This image is too large for your browser to process. Try a smaller image or a lower blur intensity.");
        return;
      }
      (s.type === "image/jpeg" || s.type === "image/jpg") && (n.fillStyle = "#ffffff", n.fillRect(0, 0, r, o)), n.filter = `blur(${u}px)`, n.drawImage(d, -t, -t), n.filter = "none", h.toBlob((v) => {
        if (!v) {
          alert("Could not export the blurred image. The picture may be too large for your browser to process.");
          return;
        }
        M.saveAs(v, `blurred-${s.name}`);
      }, s.type);
    }, a.onerror = () => {
      alert(`Could not read "${s.name}". This image format may not be supported by your browser.`);
    }, a.src = f;
  }, B = (a) => {
    (a == null ? void 0 : a.length) > 0 && I(a[0]);
  }, { getRootProps: A, getInputProps: S, isDragActive: C } = D({ onDrop: B, accept: { "image/*": [] }, multiple: false });
  return e.jsx(E, { title: "Blur Image", description: "Apply a Gaussian blur to a whole image, from 0 to 50 pixels. Free, and the picture never leaves your browser.", seoTitle: "Blur Image Online - Free Whole-Image Gaussian Blur", seoDescription: "Blur an entire photo online with an adjustable 0-50px Gaussian blur, for soft backgrounds and artistic effect. Free, and runs entirely in your browser.", faqs: L, children: e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [s ? e.jsx("div", { style: { maxWidth: "1000px", margin: "0 auto", padding: "2rem", background: "white", borderRadius: "1rem", border: "1px solid var(--border)" }, children: e.jsxs("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(300px, 1fr))", gap: "2rem" }, children: [e.jsx("div", { style: { textAlign: "center", overflow: "hidden", maxHeight: "500px", border: "1px solid #eee", borderRadius: "0.5rem" }, children: e.jsx("img", { ref: b, src: f, alt: "Preview", style: { maxWidth: "100%", maxHeight: "100%", filter: `blur(${l}px)`, transition: "filter 0.1s" } }) }), e.jsxs("div", { style: { display: "flex", flexDirection: "column", gap: "1.5rem" }, children: [e.jsxs("div", { children: [e.jsxs("label", { htmlFor: "blur-image-intensity-range", style: { display: "block", marginBottom: "0.5rem", fontWeight: "bold" }, children: ["Blur Intensity: ", l, "px"] }), e.jsx("input", { id: "blur-image-intensity-range", type: "range", min: "0", max: "50", value: l, onChange: (a) => j(Number(a.target.value)), style: { width: "100%", accentColor: "var(--primary)" } })] }), e.jsxs("button", { className: "tool-btn-primary", onClick: T, style: { padding: "1rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", cursor: "pointer", fontWeight: "bold", marginTop: "auto", display: "flex", justifyContent: "center", alignItems: "center", gap: "0.5rem" }, children: [e.jsx(H, { size: 20 }), " Download Blurred Image"] }), e.jsx("div", { style: { textAlign: "center" }, children: e.jsx("button", { className: "tool-reset-btn", onClick: () => g(null), style: { background: "none", border: "none", color: "#64748b", textDecoration: "underline", cursor: "pointer" }, children: "Start Over" }) })] })] }) }) : e.jsxs("div", { className: "tool-upload-area", ...A(), style: { border: "2px dashed var(--border)", borderRadius: "0.75rem", padding: "3rem 2rem", textAlign: "center", cursor: "pointer", background: C ? "var(--secondary)" : "#f8fafc", transition: "all 0.2s ease" }, children: [e.jsx("input", { ...S(), "aria-label": "Choose a file for Blur Image" }), e.jsx("div", { style: { width: "64px", height: "64px", background: "#e0f2fe", borderRadius: "50%", display: "flex", alignItems: "center", justifyContent: "center", margin: "0 auto 1rem", color: "#0284c7" }, children: e.jsx(p, { size: 32 }) }), e.jsx("h3", { style: { fontSize: "1.25rem", fontWeight: "600", marginBottom: "0.5rem" }, children: "Drag & drop image here" }), e.jsx("p", { style: { color: "#64748b" }, children: "or click to select file" })] }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(q, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About Blur Image Tool" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "This applies a Gaussian blur to a whole picture at a radius you choose between 0 and 50 pixels. It is a single global effect, not a selection tool: there is no rectangle to drag and no brush. That makes it a good fit for softening a background image so text sits readably on top of it, blurring a photo behind a login card, or turning a busy screenshot into an abstract header \u2014 and a poor fit for hiding one detail inside an otherwise sharp image." }), e.jsx("h3", { style: { fontSize: "1.15rem", margin: "1.5rem 0 0.75rem" }, children: "Why blur is not redaction" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "A blur averages each pixel with its neighbours. Mathematically that is a convolution, and convolutions can be attacked \u2014 especially when the hidden content is short and predictable, like a card number, a postcode or a name from a known list. Heavy blur genuinely destroys the information, but a light one can leave enough structure for it to be recovered. If something in the frame must not be readable, take it out of the frame: crop it away with the Image Cropper, or cover it with a solid opaque shape. Use blur when the goal is how the picture looks, not when the goal is secrecy." }), e.jsx("h3", { style: { fontSize: "1.15rem", margin: "1.5rem 0 0.75rem" }, children: "Getting the export to match the preview" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "The preview blurs the image at the size it is displayed on your screen, which for a phone photo might be a quarter of its real width. Applying the same numeric radius to the full-resolution file would produce something far weaker than what you approved, so the radius is multiplied by the ratio between the natural width and the displayed width before the export runs. The two then look the same." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Two details make the output cleaner than a naive canvas blur. Before the filter runs, the edge pixels are replicated outward into a padded border, so the blur has real colour to sample at the boundary instead of pulling in transparency and leaving a dark smeared frame once JPEG flattens the alpha. And because browsers cap canvases by both their longest side and their total area \u2014 iOS Safari at roughly 16 megapixels is the strictest \u2014 the padding is fitted to what your device can actually allocate, with the radius reduced to match if it will not fit. You get a slightly softer result rather than a failed download." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: "The download keeps your original format where the browser can write it, so a JPEG stays a JPEG, a PNG stays a PNG and a WebP stays a WebP, saved as blurred- plus your filename. Those three are the whole list a canvas can encode; drop in something else and you will get PNG data under the original extension, because the filename is reused as-is. JPEG output is drawn onto white first because it has no alpha channel. All of it runs in this tab: the image is never uploaded, which is the point when the reason you are blurring it is that it shows something you would rather not hand to a stranger." })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: J.map((a, r) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: a.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: a.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: a.desc })] }, r)) })] })] }) });
};
export {
  Z as default
};
