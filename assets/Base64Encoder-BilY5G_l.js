import { r as s, j as e } from "./index-BtmU1OS0.js";
import { R as u } from "./RelatedTools-GVPazTWJ.js";
import { T as y } from "./ToolLayout-CgcEif7J.js";
import { A as g } from "./arrow-right-DsW7BEJu.js";
import { C as b } from "./check-G7hwFc4v.js";
import { C as x } from "./copy-BC22e6PQ.js";
import { F as f, b as w } from "./tools-DOXC7sEs.js";
import { S as v } from "./shield-C_IpXjfc.js";
const j = [{ title: "UTF-8 Bytes, Not Code Units", desc: 'Text is run through TextEncoder before encoding, so "\xE9" becomes w6k= and "\u{1F602}" becomes 8J+Ygg== \u2014 byte-for-byte identical to base64 on a Linux shell or Python\u2019s b64encode.' }, { title: "Standard Alphabet, One Long Line", desc: "Output uses A\u2013Z, a\u2013z, 0\u20139, plus and slash, padded with equals signs, and is emitted as a single unbroken line with no 76-column MIME wrapping to strip out later." }, { title: "Nothing Is Uploaded", desc: "The encoder is a few lines of JavaScript running in this tab. Open DevTools and watch the Network panel while you click Encode: no request is made, because there is no endpoint to call." }], T = [{ question: "Why is my encoded text about a third larger than the original?", answer: "Base64 spends four output characters on every three input bytes, so the result is always about 33% longer, plus up to two padding characters. A 1 MB input becomes roughly 1.33 MB. That overhead is the price of moving arbitrary bytes through a channel that only guarantees safe handling of printable text, which is why Base64 belongs in email attachments and data URIs but not in a storage format you control." }, { question: "Is Base64 a form of encryption?", answer: "No, and treating it as one is a common and serious mistake. Base64 is a reversible transcoding with no key: anyone who sees the string can recover the original in one step. If you need something an observer cannot read, use the Encrypt Text tool, which applies a passphrase. If you need a value that cannot be reversed at all, use the Hash Generator instead." }, { question: "Can I paste this into a URL or a filename?", answer: "Not safely. The standard alphabet contains plus and slash, and the padding character is equals \u2014 all three carry meaning in query strings and paths. A plus sign in a URL query is commonly read as a space, which silently corrupts the value. Either percent-encode the result with the URL Encoder, or hand-convert to the URL-safe variant by replacing plus with hyphen and slash with underscore." }, { question: "Can I encode an image or a PDF here?", answer: "No. This page has a text box and no file picker, so it only encodes what you type or paste. To build a data URI from a real file you need a tool that reads the raw bytes rather than a string. Pasting the visible contents of a binary file into the box will not work either, because that text is already a lossy rendering of the bytes." }, { question: "What happens to characters my keyboard cannot produce?", answer: "Anything expressible as valid Unicode encodes cleanly, including CJK text, combining accents and emoji. The one edge case is an unpaired surrogate \u2014 half of an emoji copied out of a broken log file. TextEncoder replaces it with U+FFFD, so those three bytes (EF BF BD) appear in the output and the round trip returns a question-mark diamond rather than the original fragment." }, { question: "Will this output match what my server produced?", answer: "It will, provided both sides encoded the same bytes. Mismatches almost always come from an invisible difference in the input rather than from the encoder: a trailing newline, Windows CRLF line endings where the server had bare LF, or a byte-order mark at the start of a copied file. Compare the input lengths first \u2014 if they differ, the problem is upstream of Base64." }], I = () => {
  const [n, d] = s.useState(""), [o, c] = s.useState(""), [h, i] = s.useState(false), m = () => {
    try {
      const t = new TextEncoder().encode(n);
      let a = "";
      const l = 32768;
      for (let r = 0; r < t.length; r += l) a += String.fromCharCode.apply(null, t.subarray(r, r + l));
      c(btoa(a));
    } catch {
      alert("Unable to encode. Make sure text contains valid characters.");
    }
  }, p = () => {
    navigator.clipboard.writeText(o), i(true), setTimeout(() => i(false), 2e3);
  };
  return e.jsx(y, { title: "Base64 Encoder", description: "Encode text strings to Base64 format instantly and securely.", seoTitle: "Base64 Encoder Online - Convert Text to Base64", seoDescription: "Free online Base64 encoder. Convert text to Base64 format securely in your browser. Perfect for developers and data transmission.", faqs: T, children: e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [e.jsxs("div", { className: "tool-input-section", style: { marginBottom: "2rem" }, children: [e.jsx("label", { style: { display: "block", marginBottom: "0.5rem", fontWeight: "bold" }, children: "Text to Encode" }), e.jsx("textarea", { value: n, onChange: (t) => d(t.target.value), placeholder: "Type text here...", className: "tool-textarea", style: { width: "100%", minHeight: "120px", padding: "1rem", borderRadius: "0.5rem", border: "1px solid var(--border)", fontSize: "1rem" } })] }), e.jsx("div", { className: "tool-action-section", style: { textAlign: "center", marginBottom: "2rem" }, children: e.jsxs("button", { onClick: m, className: "btn-primary tool-btn", style: { padding: "1rem 3rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", cursor: "pointer", fontWeight: "bold", display: "inline-flex", alignItems: "center", gap: "0.5rem" }, children: ["Encode ", e.jsx(g, { size: 20 })] }) }), o && e.jsxs("div", { className: "tool-output-section", children: [e.jsxs("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "0.5rem" }, children: [e.jsx("label", { style: { fontWeight: "bold" }, children: "Base64 Output" }), e.jsxs("button", { onClick: p, className: "tool-copy-btn", style: { border: "none", background: "none", color: "var(--primary)", cursor: "pointer", fontWeight: "bold", display: "flex", alignItems: "center", gap: "0.25rem" }, children: [h ? e.jsx(b, { size: 16 }) : e.jsx(x, { size: 16 }), " Copy"] })] }), e.jsx("textarea", { readOnly: true, value: o, className: "tool-output-textarea", style: { width: "100%", minHeight: "120px", padding: "1rem", borderRadius: "0.5rem", border: "1px solid var(--border)", fontSize: "1rem", fontFamily: "monospace", background: "#f8fafc", color: "#475569" } })] }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(u, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About Base64 Encoder" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Base64 turns arbitrary bytes into a 64-character alphabet that survives systems which only promise to carry printable text. Paste text into the box above, press Encode, and you get the exact string that ", e.jsx("code", { children: "base64" }), " on a Unix shell, Python's ", e.jsx("code", { children: "base64.b64encode" }), " or Java's ", e.jsx("code", { children: "Base64.getEncoder()" }), " would produce for the same bytes."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "How the conversion actually works" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["Your text is first converted to UTF-8 bytes with the browser's ", e.jsx("code", { children: "TextEncoder" }), ". Those bytes are then read three at a time \u2014 24 bits \u2014 and re-cut into four 6-bit groups, each of which indexes into the alphabet A\u2013Z, a\u2013z, 0\u20139, plus and slash. When the input length is not a multiple of three, one or two equals signs are appended so the result still divides evenly by four."] }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["The UTF-8 step matters more than it sounds. The browser's raw ", e.jsx("code", { children: "btoa" }), " function reads a string as Latin-1 and throws on anything above U+00FF, so a naive implementation either rejects emoji outright or mangles accented characters. Encoding to bytes first is why ", e.jsx("strong", { children: "\xE9" }), " here gives ", e.jsx("code", { children: "w6k=" }), " rather than an error, and why the output matches a server that also works in UTF-8."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "Where developers actually use it" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["The common cases are HTTP Basic authentication, where the encoded value is literally ", e.jsx("code", { children: "username:password" }), "; inline data URIs of the form ", e.jsx("code", { children: "data:text/plain;base64,\u2026" }), "; embedding small payloads in YAML and JSON config where newlines and quotes would otherwise need escaping; and Kubernetes Secrets, whose ", e.jsx("code", { children: "data" }), " fields are Base64 by definition rather than by encryption."] }), e.jsx("h3", { style: { fontSize: "1.15rem", fontWeight: "600", margin: "1.75rem 0 0.75rem" }, children: "What this page does not do" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["There is no file upload, no URL-safe toggle and no line-wrapping option \u2014 the result is one continuous line using the standard alphabet. If a downstream system expects the URL-safe variant, swap plus for hyphen and slash for underscore and drop the padding. If it expects PEM-style 64-column blocks, you will need to insert the line breaks yourself. To go the other way, the ", e.jsx("strong", { children: "Base64 Decoder" }), " accepts this output unchanged."] })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: j.map((t, a) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: a === 0 ? e.jsx(f, { color: "var(--primary)", size: 24 }) : a === 1 ? e.jsx(w, { color: "var(--primary)", size: 24 }) : e.jsx(v, { color: "var(--primary)", size: 24 }) }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: t.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: t.desc })] }, a)) })] })] }) });
};
export {
  I as default
};
