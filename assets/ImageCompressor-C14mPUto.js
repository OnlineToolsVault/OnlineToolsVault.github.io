import { r, j as e } from "./index-BtmU1OS0.js";
import { T as W } from "./ToolLayout-CgcEif7J.js";
import { R } from "./RelatedTools-GVPazTWJ.js";
import { u as E } from "./index-BhP_zCBa.js";
import { i as B } from "./browser-image-compression-BVJ4p9S0.js";
import { U as z } from "./upload-PxpkBjYu.js";
import { D as S } from "./download-Cb6qc09_.js";
import { Z as q } from "./zap-DZ5cj3rv.js";
import { L as j } from "./tools-DOXC7sEs.js";
import { S as U } from "./shield-check-DWzNJDxZ.js";
import "./shield-C_IpXjfc.js";
const J = () => new URL("/vendor/browser-image-compression.js", window.location.href).href, K = () => {
  const [a, p] = r.useState(null), [s, d] = r.useState(null), [h, g] = r.useState(false), [n, k] = r.useState(0.8), [c, m] = r.useState(null), [u, y] = r.useState(null), [f, b] = r.useState(null);
  r.useEffect(() => {
    if (!a) {
      y(null);
      return;
    }
    const t = URL.createObjectURL(a);
    return y(t), () => URL.revokeObjectURL(t);
  }, [a]), r.useEffect(() => {
    if (!s) {
      b(null);
      return;
    }
    const t = URL.createObjectURL(s);
    return b(t), () => URL.revokeObjectURL(t);
  }, [s]);
  const P = r.useCallback((t) => {
    const o = t[0];
    o && (p(o), x(o, n));
  }, [n]), { getRootProps: I, getInputProps: C, isDragActive: w } = E({ onDrop: P, accept: { "image/*": [] }, multiple: false }), x = async (t, o) => {
    g(true), m(null);
    try {
      const i = { maxSizeMB: 2, maxWidthOrHeight: 1920, useWebWorker: true, libURL: J(), initialQuality: o }, l = await B(t, i);
      d(l);
    } catch (i) {
      console.error(i), d(null), m(`Could not compress "${t.name}". This format may not be supported by your browser (HEIC, AVIF and SVG often are not) or the file may be damaged. Try converting it to JPG or PNG first.`);
    } finally {
      g(false);
    }
  }, G = (t) => {
    const o = parseFloat(t.target.value);
    k(o), a && x(a, o);
  }, v = (t) => {
    if (t === 0) return "0 B";
    const o = 1024, i = ["B", "KB", "MB", "GB"], l = Math.floor(Math.log(t) / Math.log(o));
    return parseFloat((t / Math.pow(o, l)).toFixed(2)) + " " + i[l];
  }, N = () => {
    if (!s) return;
    const t = document.createElement("a"), o = URL.createObjectURL(s);
    t.href = o, t.download = `compressed-${a.name}`, document.body.appendChild(t), t.click(), document.body.removeChild(t), URL.revokeObjectURL(o);
  };
  return e.jsx(W, { title: "Image Compressor", description: "Compress images online for free without losing quality. Optimize PNG, JPG, JPEG, and WebP files.", seoTitle: "Free Image Compressor - Compress PNG, JPG, WebP Online", seoDescription: "Compress images online for free without losing quality. Optimize PNG, JPG, JPEG, and WebP files for faster website loading.", faqs: H, children: e.jsxs("div", { className: "tool-workspace", style: { maxWidth: "1000px", margin: "0 auto" }, children: [a ? e.jsx("div", { style: { background: "white", border: "1px solid var(--border)", borderRadius: "1rem", padding: "2rem" }, children: e.jsxs("div", { style: { display: "flex", flexDirection: "column", gap: "2rem" }, children: [e.jsxs("div", { style: { paddingBottom: "2rem", borderBottom: "1px solid var(--border)" }, children: [e.jsxs("div", { style: { display: "flex", justifyContent: "space-between", marginBottom: "1rem" }, children: [e.jsx("span", { style: { fontWeight: "600" }, children: "Compression Quality" }), e.jsxs("span", { style: { color: "#64748b" }, children: [Math.round(n * 100), "%"] })] }), e.jsx("input", { type: "range", min: "0.1", max: "1.0", step: "0.1", value: n, onChange: G, style: { width: "100%", cursor: "pointer" } })] }), e.jsxs("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(300px, 1fr))", gap: "2rem" }, children: [e.jsxs("div", { children: [e.jsxs("div", { style: { marginBottom: "1rem", display: "flex", justifyContent: "space-between", alignItems: "center" }, children: [e.jsx("span", { style: { fontWeight: "600", color: "#64748b" }, children: "Original" }), e.jsx("span", { style: { padding: "0.25rem 0.75rem", background: "#f1f5f9", borderRadius: "99px", fontSize: "0.875rem" }, children: v(a.size) })] }), e.jsx("div", { style: { height: "250px", background: "#f8fafc", borderRadius: "0.75rem", overflow: "hidden", display: "flex", alignItems: "center", justifyContent: "center", border: "1px solid var(--border)" }, children: u && e.jsx("img", { src: u, alt: "Original", style: { maxHeight: "100%", maxWidth: "100%", objectFit: "contain" } }) })] }), e.jsxs("div", { children: [e.jsxs("div", { style: { marginBottom: "1rem", display: "flex", justifyContent: "space-between", alignItems: "center" }, children: [e.jsx("span", { style: { fontWeight: "600", color: "var(--primary)" }, children: "Compressed" }), e.jsx("span", { style: { padding: "0.25rem 0.75rem", background: "#dbeafe", color: "#1e40af", borderRadius: "99px", fontSize: "0.875rem" }, children: s ? v(s.size) : c ? "\u2014" : "..." })] }), e.jsx("div", { style: { height: "250px", background: "#f8fafc", borderRadius: "0.75rem", overflow: "hidden", display: "flex", alignItems: "center", justifyContent: "center", border: "1px solid var(--border)", position: "relative" }, children: h ? e.jsx("div", { style: { color: "var(--primary)", fontWeight: "600" }, children: "Compressing..." }) : c ? e.jsx("div", { role: "alert", style: { color: "#b91c1c", fontSize: "0.875rem", textAlign: "center", padding: "1rem" }, children: c }) : f && e.jsx("img", { src: f, alt: "Compressed", style: { maxHeight: "100%", maxWidth: "100%", objectFit: "contain" } }) })] })] }), e.jsxs("div", { style: { display: "flex", gap: "1rem", justifyContent: "center", marginTop: "1rem" }, children: [e.jsx("button", { onClick: () => {
    p(null), d(null), m(null);
  }, className: "tool-btn-secondary", style: { padding: "0.75rem 1.5rem", borderRadius: "0.5rem", background: "white", border: "1px solid var(--border)", fontWeight: "600" }, children: "Upload New" }), e.jsxs("button", { onClick: N, className: "tool-btn-primary", disabled: !s || h, style: { padding: "0.75rem 2rem", borderRadius: "0.5rem", background: "var(--primary)", color: "white", border: "none", fontWeight: "600", display: "flex", alignItems: "center", gap: "0.5rem", opacity: !s || h ? 0.5 : 1 }, children: [e.jsx(S, { size: 20 }), " Download Compressed"] })] })] }) }) : e.jsxs("div", { className: "tool-upload-area", ...I(), style: { border: "2px dashed var(--border)", borderRadius: "1rem", padding: "4rem 2rem", textAlign: "center", cursor: "pointer", background: w ? "var(--secondary)" : "white", transition: "all 0.2s" }, children: [e.jsx("input", { ...C(), "aria-label": "Choose a file for Image Compressor" }), e.jsx("div", { style: { width: "64px", height: "64px", background: "var(--secondary)", borderRadius: "50%", display: "flex", alignItems: "center", justifyContent: "center", margin: "0 auto 1.5rem", color: "var(--primary)" }, children: e.jsx(z, { size: 32 }) }), e.jsx("h3", { style: { fontSize: "1.25rem", fontWeight: "600", marginBottom: "0.5rem" }, children: w ? "Drop image here" : "Drag & Drop image here" }), e.jsx("p", { style: { color: "#64748b" }, children: "or click to select file" }), e.jsx("p", { style: { fontSize: "0.875rem", color: "#94a3b8", marginTop: "1rem" }, children: "Supports PNG, JPG, WebP" })] }), e.jsxs("div", { className: "tool-content", style: { marginTop: "4rem" }, children: [e.jsx(R, {}), e.jsxs("div", { className: "about-section", style: { background: "var(--bg-card)", padding: "2rem", borderRadius: "1rem", border: "1px solid var(--border)", marginBottom: "2rem" }, children: [e.jsx("h2", { style: { fontSize: "1.8rem", marginBottom: "1.5rem" }, children: "About Image Compressor" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Compressing an image here means re-encoding it. The file you drop is decoded by the browser, redrawn onto a canvas, and written back out in the same format at a lower quality target. A JPEG comes back as a JPEG, a PNG as a PNG, a WebP as a WebP, so nothing that reads the file afterwards has to change." }), e.jsx("h3", { style: { fontSize: "1.15rem", margin: "1.5rem 0 0.75rem" }, children: "Three things shrink the file" }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["The slider sets the encoder quality between 10% and 100%, starting at 80%. Independently of that, the longest side is capped at ", e.jsx("strong", { children: "1920 pixels" }), ". A 3000 x 2000 photo therefore comes back at 1920 x 1280 even with quality at 100%, and on a large phone photo that resize is usually where most of the saving comes from. 1920 px is still wider than the screen most people will view it on. If you need the original pixel dimensions kept, the Bulk Image Compressor is configured to preserve resolution instead."] }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["The third is a size target. The tool aims for a result under ", e.jsx("strong", { children: "2 MB" }), " that is also no larger than the file you started with. If a single pass misses that, it re-encodes in a loop, dropping the quality a little each time and \u2014 only when the 2 MB target is the one being missed \u2014 shrinking the canvas by a further 5% per pass, up to ten passes. So a very large photo can come back below 1920 px on its longest side. That is the target being enforced, not a bug."] }), e.jsxs("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: ["The slider works on PNG too, though not in the way you might expect. PNG has no JPEG-style quality knob, so the encoder used here converts the slider into a ", e.jsx("em", { children: "colour count" }), " and quantises the image to that many colours before writing the file \u2014 roughly 4,000 colours at 100%, around 400 at 10%. That makes PNG output here lossy rather than lossless, and it is why a flat graphic can shrink dramatically while a photograph saved as PNG picks up visible banding at low settings. If your PNG is a photograph rather than a screenshot, logo or diagram, converting it to JPEG or WebP with the Image Converter is still the better move."] }), e.jsx("h3", { style: { fontSize: "1.15rem", margin: "1.5rem 0 0.75rem" }, children: "What happens to your file" }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)", marginBottom: "1rem" }, children: "Your picture never leaves the machine you are sitting at. It is read with the browser File API into an in-memory blob, encoded on your own CPU, and discarded \u2014 along with the compressed copy \u2014 when you close the tab. There is no upload step and no server to store anything. The encoding runs in a Web Worker so the page stays responsive, and that worker\u2019s script is served from this site rather than a third-party CDN, so pressing compress produces no cross-origin request at all. If that one script cannot be loaded the tool falls back to compressing on the main thread, so it still works offline \u2014 just without the worker." }), e.jsx("p", { style: { lineHeight: "1.6", color: "var(--text-secondary)" }, children: "The limit is what your browser can decode. JPEG, PNG and WebP go in and come back out in the same format, and BMP has its own writer here so it survives too. A GIF decodes, but a canvas only ever holds one frame and no browser can write GIF back, so the animation does not survive \u2014 reach for the Image Converter if you need to choose the output format explicitly. HEIC and HEIF straight from an iPhone cannot be decoded at all and give you an error rather than a silent failure; run those through the HEIC to JPG tool first. An SVG has nothing to re-encode in the first place, since it is vector markup rather than a grid of pixels." })] }), e.jsx("div", { className: "features-section", style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "2rem" }, children: T.map((t, o) => e.jsxs("div", { className: "tool-feature-block", style: { padding: "1.5rem", borderRadius: "1rem", border: "1px solid var(--border)", background: "var(--bg-card)" }, children: [e.jsx("div", { style: { width: "48px", height: "48px", background: "var(--primary-light)", borderRadius: "0.75rem", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" }, children: t.icon }), e.jsx("h3", { style: { fontSize: "1.25rem", marginBottom: "0.5rem" }, children: t.title }), e.jsx("p", { style: { color: "var(--text-secondary)" }, children: t.desc })] }, o)) })] })] }) });
}, T = [{ title: "Quality slider you can see", desc: "Every move of the slider re-encodes the picture and updates the byte count beside the preview, so you can stop at the point where the image starts to look wrong instead of guessing a number.", icon: e.jsx(q, { color: "var(--primary)", size: 24 }) }, { title: "Side-by-side comparison", desc: "Original and result are shown at the same display size with their exact file sizes, which is the only reliable way to judge whether the saving cost you anything visible.", icon: e.jsx(j, { color: "var(--primary)", size: 24 }) }, { title: "Format is never switched", desc: "A JPEG downloads as a JPEG and a PNG as a PNG, keeping the original filename behind a compressed- prefix. Nothing downstream has to be told the file changed.", icon: e.jsx(j, { color: "var(--primary)", size: 24 }) }, { title: "Encoded on your own device", desc: "Your picture is never uploaded \u2014 it is decoded and re-encoded by a Web Worker on your own CPU. The only network request involved is the one that loads that worker\u2019s script, and it comes from this site rather than a third-party CDN; if it fails the tool simply compresses on the main thread instead.", icon: e.jsx(U, { color: "var(--primary)", size: 24 }) }], H = [{ question: "Why is my downloaded image smaller in pixels than the original?", answer: "The tool caps the longest side at **1920 pixels**, so a 3000 x 2000 photo comes back as 1920 x 1280. That resize is deliberate: it is the single biggest saving available on a modern phone photo, and 1920 px is wider than most screens the image will be viewed on. It can go smaller still \u2014 if the result is over the 2 MB target the tool re-encodes in a loop and takes another 5% off each side per pass. If you need the original dimensions kept, use the Bulk Image Compressor, which preserves resolution, or the Image Resizer if you want to choose the numbers yourself." }, { question: "Does the quality slider do anything to a PNG?", answer: "Yes, but not in the JPEG sense. PNG has no quality parameter, so the encoder here turns the slider into a **colour count** and quantises the image before writing it \u2014 roughly 4,000 colours at 100% and around 400 at 10%. PNG output from this tool is therefore lossy: a logo or screenshot can shrink a lot with no visible change, while a photograph saved as PNG will start to show banding as you drag the slider down. If the PNG is really a photograph, convert it to JPEG or WebP with the Image Converter instead." }, { question: "Is my photo uploaded anywhere?", answer: "No. Your photo is read into memory by your browser, encoded on your own CPU, and handed straight back as a download. Being precise about the Network tab: the compression library runs in a Web Worker, so the first time you compress you will see one request for a JavaScript file \u2014 that worker's script, served from this site, containing code and none of your image. It used to come from a public CDN; it no longer does, so nothing cross-origin happens when you compress. Block it or go offline and the tool falls back to compressing on the main thread." }, { question: "Which file types actually work?", answer: "Anything your browser can decode. JPEG, PNG, WebP and BMP round-trip in the same format. A GIF decodes but loses its animation, because a canvas holds one frame and browsers cannot write GIF. HEIC and HEIF photos straight off an iPhone cannot be decoded and will show an error instead of failing silently \u2014 run those through the HEIC to JPG tool first, then compress the JPEG. An SVG is vector markup with no pixel grid to re-encode." }, { question: "Can I undo the compression later?", answer: "Not for JPEG or WebP \u2014 the discarded detail is gone for good. Keep your originals somewhere safe. It also means you should always compress from the original rather than re-compressing a file that has already been through the tool, because each pass throws away more." }, { question: "How much smaller will my file get?", answer: "It depends entirely on what is in the picture. Photographs with fine detail and noise compress heavily, often by 70-90%. Flat graphics, screenshots and text compress badly as JPEG and can even grow, because the encoder spends bits trying to reproduce hard edges. Content like that belongs in PNG or WebP." }, { question: "How do I compress a whole folder at once?", answer: "Use the Bulk Image Compressor. It takes a multi-file selection, shows the before and after size for each image, and gives you everything back as a single ZIP. It also keeps every image at its original resolution, which this single-file tool does not." }, { question: "Does compressing strip GPS and camera data?", answer: "Re-encoding through a canvas does drop the metadata blocks, but do not treat that as a privacy guarantee \u2014 it is a side effect, not the purpose. If your goal is to remove location data before sharing a photo, use Remove Image Metadata, which is built for it and can clean a JPEG without re-encoding the pixels at all." }, { question: "Why did my file get larger instead of smaller?", answer: "It usually will not, because the tool notices and retries: if the first pass comes back bigger than the original it re-encodes at a lower quality, up to ten times, before giving up. When it still ends up larger, the source was already compressed harder than anything the encoder can match \u2014 re-encoding at 90% a JPEG that was saved at 60% adds bytes without adding detail. Drag the slider down until the result is genuinely smaller, or keep the file you already had." }];
export {
  K as default
};
